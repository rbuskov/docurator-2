# Slice 003: See my emails listed — Plan

**Spec:** `docs/specs/003-see-my-emails-listed.md`
**Research:** `docs/research/003-see-my-emails-listed.md`

## Steps

Each step is small enough to fit in one loop iteration. Each step ends with a concrete check — a named test passing, or a specific command's output. Per-step refactor is folded into the priority-3 action. ADRs ship in the same iteration as the step that triggered them.

- [x] **Step 1: Add `react-router-dom` runtime dep + confirm `googleapis`/`tsx` already present.** Edit `package.json` to add `"react-router-dom": "^6"` to `dependencies`. Run `npm install`. Verify `node_modules/react-router-dom/package.json` resolves and the major version is 6. Confirm `googleapis` (Slice-002 added it) and `tsx` (used by `dev`/`start`) are already in deps; do not re-add. Verification: `npx vitest run` still all-green (no behavioral change yet); `node -e "console.log(require('react-router-dom/package.json').version)"` prints a `6.x` version; `node -e "console.log(require('googleapis/package.json').version, require('tsx/package.json').version)"` prints both versions. The audit warnings from Slice 001's deferred list are still acceptable. — _Done: `react-router-dom@6.30.3` installed (3 packages added: `react-router-dom`, `react-router`, `@remix-run/router`). `googleapis@171.4.0` and `tsx@4.21.0` confirmed already present — no re-add. `npx vitest run` → 16 files / 97 tests, all passing in 2.51 s. The 5 moderate audit findings from Slice 001's followups are unchanged (still in `vite-node`'s transitive chain)._

- [x] **Step 2: Build-time check script + tests + ADR.** Write `scripts/check-gmail-readonly.test.ts` covering: `scanForForbidden(rootDir, selfPath?)` returns `{ hits: Array<{ file: string, substring: string }> }`. Test fixtures created via `mkdtempSync`:
  - `clean/`: a single `clean.ts` with `export const x = 'gmail.readonly'` — zero hits.
  - `single-hit/`: a `bad.ts` with a comment `// uses messages.modify here` — exactly one hit, file basename `bad.ts`, substring `messages.modify`.
  - `multiple/`: a `bad.ts` with both `messages.modify` and `drafts.create` on different lines — two hits.
  - `nested/`: `a/b/c/x.ts` with `gmail.send` — one hit at the nested path.
  - `self-exempt/`: a `check-gmail-readonly.ts` (the script's basename) containing `messages.modify`; calling `scanForForbidden(tempDir, '<temp>/check-gmail-readonly.ts')` returns zero hits.
  - **Positive guard test:** `scanForForbidden(path.resolve(import.meta.dirname, '../src'))` returns `{ hits: [] }` against the actual codebase. Catches regressions even when the human forgets to `npm run build`.
  Run vitest — **red** (script doesn't exist). Implement `scripts/check-gmail-readonly.ts` exporting `scanForForbidden(rootDir, selfPath?)` and a `FORBIDDEN: readonly string[]` constant matching the spec verbatim (24 substrings: `messages.modify`, `messages.trash`, `messages.delete`, `messages.send`, `messages.insert`, `messages.import`, `labels.create`, `labels.delete`, `labels.update`, `labels.patch`, `drafts.create`, `drafts.update`, `drafts.delete`, `drafts.send`, `threads.modify`, `threads.trash`, `threads.delete`, `gmail.modify`, `gmail.send`, `gmail.compose`, `gmail.insert`, `gmail.metadata`, `gmail.labels`, `gmail.settings.basic`, `gmail.settings.sharing`). Walk via `readdirSync(root, { recursive: true })` filtered to `.ts/.tsx/.js/.jsx`, skip the script's own path. The CLI entry (when `import.meta.url` matches `process.argv[1]`'s file URL) calls `scanForForbidden(path.resolve('src'), fileURLToPath(import.meta.url))`, prints results, and sets `process.exitCode = hits.length === 0 ? 0 : 1`. Re-run **green**. Add `"check:gmail-readonly": "tsx scripts/check-gmail-readonly.ts"` to `package.json` and update `"build"` to run it first: `"npm run check:gmail-readonly && vite build && tsc -p tsconfig.server.json && node -e \"...\""`. Update `vitest.workspace.ts`'s server project include to `['src/server/**/*.test.ts', 'scripts/**/*.test.ts']`. Add `scripts/**/*.ts` to `tsconfig.json`'s include array. Write `docs/adr/003-build-time-readonly-gmail-check.md` (status Accepted) capturing: substring matching is the enforcement mechanism, runs as the first step of `npm run build`, deliberately picks up matches in comments (false positives are fixed by editing the comment, not loosening the rule), no AST analysis, scoped to `src/`, script self-exempts. Alternatives rejected: ESLint custom rule (heavier setup, less obvious), `eslint-plugin-no-restricted-imports` (only catches imports, misses dynamic property access strings), AST-based `ts-morph` tool (overkill for a substring guard, defers maintenance for no current win), pre-commit hook only (CI/build-time enforcement is more durable). Verification: `npx vitest run scripts/check-gmail-readonly.test.ts` passes; `npm run check:gmail-readonly` exits 0 with `OK` line; `npm run build` exits 0 and the build log shows the check ran ahead of vite. — _Done: 7 tests in `scripts/check-gmail-readonly.test.ts` (6 fixture cases + 1 positive guard against the real `src/`). The fixture set deviated slightly from the plan: collapsed the originally-planned `multiple/` and `nested/` into the natural-flow expression of each scenario, plus added an extension-filter case (`.tsx` / `.js` / `.jsx` count, `.md` does not). Implementation uses `readdirSync(root, { recursive: true, withFileTypes: true })` and `entry.parentPath` (Node 20.12+; the project's `engines` is `>=20`, so the runtime guarantee holds). The CLI `isMain()` check uses `pathToFileURL(process.argv[1]).href` so the script runs only when invoked directly, not when imported by the test (which would otherwise re-execute the scan against `src/` per test). `package.json` updated: `check:gmail-readonly` script added, `build` chain now `npm run check:gmail-readonly && vite build && tsc … && cpSync …`. `vitest.workspace.ts`'s server include extended to include `scripts/**/*.test.ts`. `tsconfig.json` includes `scripts/**/*` so the script and its test type-check. ADR-003 written, references `architecture.md` § "Read-only Gmail access" + § "Security notes — CI enforcement", explains why substring matching is the right tool (vs. AST), notes that `gmail.metadata` and `gmail.labels` are deliberately in the FORBIDDEN list as scope-narrowing tripwires. Full suite: 17 files / 104 tests passing in 2.28 s. `npx tsc --noEmit -p tsconfig.json` clean. `npm run build` clean (33 modules → 198.10 kB bundle, identical to Slice 002 since no client code changed yet)._

- [x] **Step 3: Gmail client wrapper (red→green).** Write `src/server/gmail/client.test.ts`. Test cases (all use injected fakes — never hit real Gmail):
  - `createGmailClient(accountId).listMessages({ maxResults: 50 })` invokes `gmail.users.messages.list({ userId: 'me', maxResults: 50 })` exactly once on the injected fake; returns `{ messages: [{ id: 'a', threadId: 't1' }] }` from the fake response.
  - `listMessages` normalizes a fake response missing the `messages` field to `{ messages: [] }`.
  - `listMessages` passes `q` and `pageToken` through when supplied.
  - `getMessage('msg1', { format: 'metadata', metadataHeaders: ['Subject', 'From', 'Date'] })` invokes `gmail.users.messages.get({ userId: 'me', id: 'msg1', format: 'metadata', metadataHeaders: ['Subject', 'From', 'Date'] })`.
  - Both methods route through `session.withFreshTokens(accountId, callback)`. Stub `withFreshTokens` (via `vi.spyOn`) to assert the accountId argument and confirm the wrapped callback is what calls the gmail factory.
  - When `withFreshTokens` rejects with an error (simulating `invalid_grant` post-handling), `listMessages`/`getMessage` rethrow verbatim (no swallow, no rewrap).
  Run vitest — **red**. Implement `src/server/gmail/client.ts`:
  - Imports `google` from `googleapis`, `OAuth2Client` from `google-auth-library`, `withFreshTokens` from `../auth/session.js`, types `gmail_v1` (type-only) from `googleapis`.
  - Module-level `let gmailFactory: GmailFactory = (auth) => google.gmail({ version: 'v1', auth })`. Export `setGmailFactoryForTest(f)` and `resetGmailFactoryForTest()` to swap.
  - `createGmailClient(accountId: number): { listMessages, getMessage }`:
    - `listMessages(args: { maxResults: number, q?: string, pageToken?: string })`: `return withFreshTokens(accountId, async (client) => { const gmail = gmailFactory(client as unknown as OAuth2Client); const res = await gmail.users.messages.list({ userId: 'me', maxResults: args.maxResults, q: args.q, pageToken: args.pageToken }); return { messages: res.data.messages ?? [], nextPageToken: res.data.nextPageToken, resultSizeEstimate: res.data.resultSizeEstimate } })`.
    - `getMessage(id: string, args: { format: 'metadata' | 'full' | 'minimal' | 'raw', metadataHeaders?: string[] })`: `return withFreshTokens(accountId, async (client) => { const gmail = gmailFactory(client as unknown as OAuth2Client); const res = await gmail.users.messages.get({ userId: 'me', id, format: args.format, metadataHeaders: args.metadataHeaders }); return res.data })`.
  - The `as unknown as OAuth2Client` cast is acknowledged in the wrapper with a one-line comment pointing at the research doc's "OAuth2Client shape compatibility" risk.
  Re-run **green**. Verification: `npx vitest run src/server/gmail/client.test.ts` passes (target: ~7 tests). — _Done: 7 tests passing in 4 ms — 5 for `listMessages` (happy path with `userId: 'me'` + `maxResults`, missing-messages-field normalization, `q`/`pageToken` passthrough, `withFreshTokens` accountId routing, session-error rethrow), 2 for `getMessage` (full args passthrough including `metadataHeaders`, session-error rethrow). The `withFreshTokens` spy is built via a small `makeSpy()` factory so its return type is inferred (the inline `ReturnType<typeof vi.spyOn<...>>` annotation tripped a TS2344 with vitest 2.1's generic `spyOn` typings — flagging in review). Implementation uses conditional spread (`...(args.q !== undefined ? { q: args.q } : {})`) to avoid passing `undefined` keys through to the Gmail SDK, which keeps the `Schema$Message$List` request body minimal. The `as unknown as OAuth2Client` cast at each Gmail call site has an inline comment pointing at the research doc's "OAuth2Client shape compatibility" risk. Build remains clean (no new bundle deltas — `googleapis` import is server-only). 18 files / 111 tests passing total._

- [x] **Step 4: Messages API endpoint — happy path + 404 + 409.** Write `src/server/api/messages.test.ts`. Build a Hono app via `registerMessagesRoutes(app, deps?)` with a fake `createGmailClient` injected. Seed the temp DB via `migrate()` then `accounts.insert(...)` for two accounts (one `connected`, one synthesized to `needs_reauth` via `accounts.updateStatus`). Seed `session.set(connectedId, { tokens: ... })` so the session check passes for the connected account.
  - `GET /api/accounts/999/messages` → 404 JSON `{ error: 'account_not_found' }`.
  - `GET /api/accounts/<needs_reauth_id>/messages` → 409 JSON `{ error: 'account_not_connected', status: 'needs_reauth' }`.
  - `GET /api/accounts/<connected_no_session_id>/messages` (account is `connected` per DB but `session.get(id) === undefined`) → 409 `{ error: 'account_not_connected', status: 'needs_reauth' }`. After the call, `accounts.findById(id).status === 'needs_reauth'` (route flips it).
  - `GET /api/accounts/<connected_id>/messages` happy path with a fake client returning two list ids and two get-payloads (each with `payload.headers` containing `Subject`, `From`, `Date`, plus `internalDate`): 200 JSON `{ messages: [{ id, thread_id, subject, from, date, internal_date }, { … }] }`. Assert field-by-field. Two messages keeps the test small but exercises the loop.
  - `?limit=` validation: `?limit=0` → 400 `{ error: 'invalid_limit' }`; `?limit=200` → 400 `{ error: 'invalid_limit' }`; `?limit=25` → fake list called with `maxResults: 25`; default (no `?limit`) → fake list called with `maxResults: 50`.
  - Header missing case: a getMessage payload omits the `Subject` header → response row's `subject === ''`.
  - Header case-insensitivity: a getMessage payload uses lowercase `subject` instead of `Subject` → response row's `subject` populated correctly.
  Run vitest — **red**. Implement `src/server/api/messages.ts`:
  - `export type MessagesRouteDeps = { createGmailClient?: typeof createGmailClient }`.
  - `registerMessagesRoutes(app: Hono, deps: MessagesRouteDeps = {})` registers `app.get('/api/accounts/:id/messages', handler)`.
  - The handler: parse `:id` (positive integer or 400); parse `?limit=` (1–100 integer, default 50, else 400); `accounts.findById(id)` → 404 if missing; if `account.status !== 'connected'` → 409 with `account.status`; if `session.get(id) === undefined` → flip `accounts.updateStatus(id, 'needs_reauth')` and return 409 with `status: 'needs_reauth'`; otherwise wrap the actual Gmail calls in a try/catch (Step 5 fills in the error branches; for now this step's tests cover only the happy path + the structural 404/409 validation, so the catch can be a placeholder that rethrows).
  - Header parser `extractHeader(message: gmail_v1.Schema$Message, name: string): string` — case-insensitive lookup in `message.payload?.headers ?? []`, returns `''` on miss.
  - Response shape exactly as in the spec: `{ messages: Array<{ id: string, thread_id: string, subject: string, from: string, date: string, internal_date: string }> }`. `internal_date` comes from `message.internalDate ?? ''` (Gmail returns it as a string already).
  - Update `src/server/app.ts` to call `registerMessagesRoutes(app)` between `registerOauthRoutes(app)` and the static fallback.
  Re-run **green**. Verification: `npx vitest run src/server/api/messages.test.ts` passes; the existing `app.test.ts` and `api/oauth.test.ts` and `api/accounts.test.ts` still pass. — _Done: 11 tests passing in 711 ms (one extra over the planned shape — added a "returns 400 for non-integer id" case to lock in the input-validation contract). Implementation matches the plan's shape: `id` validation → `?limit` validation → `accounts.findById` → status check → session check (with row flip on missing-session) → Gmail call → header parsing. `extractHeader` is module-private, case-insensitive, returns `''` on miss. Returns `m.id` skip-if-not-string defensively for messages where the SDK reports a null id (shouldn't happen for `users.messages.list`, but the SDK type permits it). The `id` validation error code is `invalid_id` rather than the plan's `invalid_id` text alone — kept consistent with `invalid_limit` for parallelism. `app.ts` now registers the route after `registerOauthRoutes` and before the static fallback. Full suite: 19 files / 122 tests passing in 3.00 s. `npx tsc --noEmit` clean. `npm run build` clean (bundle unchanged because no client code touched yet)._

- [x] **Step 5: Messages API endpoint — error mapping (invalid_grant → 401, generic → 502).** Add to `src/server/api/messages.test.ts`:
  - The fake `createGmailClient` is set up so `listMessages` rejects with `Error('invalid_grant')` (mirroring `withFreshTokens`'s rethrow path post-flip): handler returns 401 `{ error: 'needs_reauth', account_id: id }`.
  - Same setup but `listMessages` rejects with `Error('quotaExceeded: rate limit')`: handler returns 502 `{ error: 'gmail_error', message: 'quotaExceeded: rate limit' }`.
  - `listMessages` resolves but one of the per-row `getMessage` calls rejects with a generic error: handler returns 502 (no partial 200, per the research's "Per-row getMessage partial failures" stance).
  Run vitest — **red**. Extend `src/server/api/messages.ts`'s try/catch to discriminate via the error message: `instanceof Error && err.message.includes('invalid_grant') → 401 needs_reauth`, anything else → 502 with the message string. Use the same `isInvalidGrantError`-shaped check as `session.ts` already exposes (or re-implement the substring check inline since the export isn't currently public — pick re-implement to avoid widening session's API; flag for follow-up). Re-run **green**. Verification: `npx vitest run src/server/api/messages.test.ts` shows the new error-path tests passing alongside the Step-4 happy-path tests. — _Done: 3 new tests added (invalid_grant → 401, generic-error from listMessages → 502, partial-getMessage failure → 502); 14 tests total in the file. The handler now wraps the Gmail call block in a try/catch with a local `isInvalidGrantError(err)` helper that mirrors `session.ts`'s shape (substring on `Error.message` plus the `err.response?.data?.error === 'invalid_grant'` GoogleAuthError shape). Re-implemented the helper inline rather than widening session's exported API — flagged for review follow-up: if a third caller ever needs the same predicate, extract to `auth/invalid-grant.ts`. Full suite: 19 files / 125 tests passing in 2.79 s. `tsc --noEmit` clean._

- [x] **Step 6: Client `Message` type + `Nav` component.** Add `Message` type to `src/client/types.ts` matching the messages API response: `{ id, thread_id, subject, from, date, internal_date }`. Write `src/client/components/Nav.test.tsx` (jsdom + RTL):
  - Wraps `<Nav />` in `<MemoryRouter initialEntries={['/']}>`. Renders the heading "Docurator". Renders a link with text `Dashboard` whose href is `/`. Renders a link with text `Inbox` whose href is `/inbox`. Both links are present.
  - With `initialEntries={['/inbox']}`, the Inbox link has `aria-current="page"` (or whatever `NavLink` sets) and the Dashboard link does not.
  Run vitest — **red**. Implement `src/client/components/Nav.tsx`: imports `NavLink` from `react-router-dom`, renders `<header><h1>Docurator</h1><nav><NavLink to="/" end>Dashboard</NavLink> <NavLink to="/inbox">Inbox</NavLink></nav></header>`. The `end` prop on `/` makes `aria-current` exact-match for the Dashboard link. Re-run **green**. Verification: `npx vitest run src/client/components/Nav.test.tsx` passes. — _Done: 3 tests passing in 97 ms. Used `getAttribute()` / `textContent` plain DOM assertions rather than `toHaveAttribute` / `toHaveTextContent` jest-dom matchers — the project doesn't currently set up `@testing-library/jest-dom`, and existing tests (`AccountList.test.tsx`, `Dashboard.test.tsx`) use the plain matcher style. Adding jest-dom would be a small one-line setup-file change but is out of scope for this slice. React Router emits two future-flag warnings (`v7_startTransition` and `v7_relativeSplatPath`); these are advisory and disappear when the project opts into v7 — flagged in the review as a follow-up. `Message` type added to `src/client/types.ts` matching the API response shape verbatim. Full suite: 20 files / 128 tests passing in 2.61 s. `tsc --noEmit` clean._

- [x] **Step 7: AccountPicker component.** Write `src/client/components/AccountPicker.test.tsx`. Test fixtures use `Account[]` shapes from `types.ts`:
  - Empty list (`accounts: []`) → renders the empty-state `<p>No connected accounts. Connect one on the Dashboard.</p>`. The test asserts the text is present and no `<select>` is rendered.
  - Two `connected` accounts, `value={firstId}`, `onChange={mock}` → renders a `<select>` with two `<option>`s; the first is selected; selecting the second by `userEvent.selectOptions(...)` calls `onChange` with the second account's id (as a number).
  - Three accounts: two `connected`, one `needs_reauth`. With `includeDisconnected={true}` (default behavior), all three appear; the `needs_reauth` row's `<option>` is `disabled` and its label includes `(needs reauth — reconnect on Dashboard)`.
  - Same data with `includeDisconnected={false}` → only two options rendered.
  - Only `needs_reauth` accounts (`includeDisconnected={true}`) → renders the empty-state message because none are selectable. (The empty-state predicate is `accounts.filter(a => a.status === 'connected').length === 0`.)
  Run vitest — **red**. Implement `src/client/components/AccountPicker.tsx` with props `{ accounts: Account[], value: number | null, onChange: (id: number) => void, includeDisconnected?: boolean }`. Default `includeDisconnected = true`. Empty-state predicate as above. Render `<select value={value ?? ''} onChange={e => onChange(Number(e.target.value))}>` with one `<option>` per visible account; disabled rows get `disabled` and the suffix label. Re-run **green**. Verification: `npx vitest run src/client/components/AccountPicker.test.tsx` passes. — _Done: 6 tests passing in 109 ms — empty list, value reflection, onChange fires with numeric id, includeDisconnected=true renders all options with needs_reauth disabled + reconnect-suffix label, includeDisconnected=false filters needs_reauth out, only-needs_reauth → empty state. Implementation distinguishes "visible" (filtered by `includeDisconnected`) from "selectable" (`status === 'connected'`); the empty-state predicate is `!visible.some(a => a.status === 'connected')` so `[needsReauthC]` renders the empty state regardless of `includeDisconnected` (no point showing a single disabled option no one can pick). The label uses `display_name ?? email` to align with `AccountList.tsx`'s row text. `aria-label="Account"` on the `<select>` so RTL's `getByRole('combobox')` works without surrounding form context. Full suite: 21 files / 134 tests passing in 2.85 s. `tsc --noEmit` clean._

- [x] **Step 8: Inbox view.** Write `src/client/views/Inbox.test.tsx`. Mock `globalThis.fetch` per-test. Cases:
  - On mount with `<MemoryRouter initialEntries={['/inbox']}>`: stub `fetch('/api/accounts')` to resolve with `{ accounts: [connectedA, connectedB] }`, then stub `fetch('/api/accounts/<A>/messages?limit=50')` to resolve with `{ messages: [{...row1}, {...row2}] }`. The test waits for the table; asserts the picker is at A, the table has 2 rows with the expected Subject/From/Date cell text.
  - localStorage seeded with `connectedB.id` → on mount, picker initializes to B; messages fetch for B fires.
  - localStorage seeded with an id that's not in the returned accounts list → picker falls back to the first connected account.
  - localStorage seeded with the id of a `needs_reauth` row → picker falls back to the first connected account.
  - User changes the picker (via `userEvent.selectOptions`) → second messages fetch fires with the new account id; the table updates; localStorage is updated to the new id.
  - Messages call returns 401 with `{ error: 'needs_reauth', account_id }` → renders the "needs reauth" error state with a `<Link to="/">Dashboard</Link>`.
  - Messages call returns 502 with `{ error: 'gmail_error', message: 'quota' }` → renders "Gmail returned an error: quota."
  - `accounts: []` → renders the empty state with a Dashboard link.
  - All accounts are `needs_reauth` → renders the AccountPicker's empty-state (which is also the "no selectable accounts" surface).
  Run vitest — **red**. Implement `src/client/views/Inbox.tsx`:
  - State: `accounts: Account[] | null`, `selectedAccountId: number | null`, `messages: Message[] | null`, `loading: boolean`, `error: string | null`.
  - Effect 1: on mount, fetch `/api/accounts`; pick initial `selectedAccountId` from localStorage `docurator.lastInboxAccountId` if it maps to a `connected` account, else the first `connected` account, else `null`.
  - Effect 2: when `selectedAccountId` changes (and is not null), persist to localStorage, set `loading=true, error=null`, fetch `/api/accounts/<id>/messages?limit=50`. On non-2xx, attempt to JSON-parse the response body for the `error` discriminator; on `needs_reauth` → `setError('NEEDS_REAUTH')`, on `gmail_error` → `setError('GMAIL: ' + message)`, on parse fail → `setError('Unexpected error')`.
  - Render: `<main>` with `<AccountPicker .../>`. If accounts is null → `<p>Loading…</p>`. If accounts has zero → empty state with link to `/`. If a connected account is selected and messages is null → `<p>Loading messages…</p>`. If error starts with `NEEDS_REAUTH` → "This account needs to be reconnected" + `<Link to="/">Dashboard</Link>`. If error starts with `GMAIL:` → render the message after the prefix. Otherwise render a `<table>` with a `<thead>` of Subject/Sender/Date and a `<tbody>` of the messages.
  - Localstorage-key constant: `LAST_INBOX_ACCOUNT_KEY = 'docurator.lastInboxAccountId'` exported for testability.
  Re-run **green**. Verification: `npx vitest run src/client/views/Inbox.test.tsx` passes. — _Done: 9 tests passing in 172 ms — preselect first connected, init from localStorage, fallback when stored id missing, fallback when stored id is needs_reauth, picker change re-fetches + persists, 401 → "needs reauth" + Dashboard link, 502 → "Gmail returned an error: …", no-accounts empty state, only-needs_reauth → AccountPicker empty state. Implementation: two `useEffect`s — first fetches `/api/accounts` once on mount + picks initial id via `pickInitialAccount(accounts, readStoredId())`; second fetches messages when `selectedAccountId` changes, with cleanup-cancelled guards so an in-flight fetch doesn't write stale state if the user changes the picker again. Error-state discriminator (`{ kind: 'none' | 'needs_reauth' | 'gmail_error' | 'unexpected' }`) keeps the render branches readable. Persists `selectedAccountId` to localStorage on each change inside the second effect (also catches the cleared-storage case where the initial pick falls back). Reads `messages` from `res.json()` only on a 200, parses error bodies on 502, swallows JSON-parse errors with `.catch(() => ({}))`. The `useMemo`-cached `hasConnected` predicate gates the table rendering so the AccountPicker's empty state takes precedence over the table's "loading messages…" state when no connected accounts exist. Used `screen.getByText(/no accounts connected/i)` (one-node match) rather than the originally-planned `/no accounts connected.*dashboard/i` (multi-node) — the trailing `<Link>` lives in a separate DOM node, breaking the regex flow. Full suite: 22 files / 143 tests passing in 2.87 s. `tsc --noEmit` clean. The two React Router future-flag warnings persist; deferred to a later slice._

- [x] **Step 9: Router + App.tsx integration.** Write `src/client/router.test.tsx` (or fold into a small `App.test.tsx` if simpler). Test cases:
  - `<App />` rendered inside a test that doesn't supply its own router (App owns the `BrowserRouter`) — assert the Nav links are rendered, the Dashboard route renders by default. (Use `window.history.pushState` or `MemoryRouter`-wrap a sub-component if `App` is `BrowserRouter`-coupled — pick the cleanest path; if App is hard to test directly, extract `<AppRoutes />` and test that under `<MemoryRouter>` instead.)
  - Navigation from `/` to `/inbox` via clicking the Inbox link causes the Inbox view to mount (mock the fetches it makes; just confirm the loading state appears).
  Run vitest — **red**. Implement `src/client/router.tsx` exporting `<AppRoutes />` that returns `<Routes><Route path="/" element={<Dashboard />} /><Route path="/inbox" element={<Inbox />} /></Routes>`. Update `src/client/App.tsx` to `<BrowserRouter><Nav /><AppRoutes /></BrowserRouter>` (imports `BrowserRouter` from `react-router-dom`). Move `<h1>Docurator</h1>` out of `Dashboard.tsx` (Nav renders it now). Update existing `Dashboard.test.tsx` to wrap renders in `<MemoryRouter>` (because `Dashboard.tsx` may not contain any router-dependent code, but its parent in the integration test will). If `Dashboard.test.tsx` currently asserts the heading text, leave that test in place but expect the heading to be rendered by Nav — adapt the test to render `<Nav />` alongside `<Dashboard />` in a MemoryRouter, or move the heading assertion into `Nav.test.tsx` and drop it from Dashboard's. Pick the latter (cleaner: each component tests its own contract). Re-run **green**. Verification: all client tests pass; `npm run build` exits 0; the build's `check:gmail-readonly` step still emits OK. — _Done: 3 router-integration tests in `router.test.tsx` (Dashboard at `/`, Inbox at `/inbox`, click-Inbox-navigates). Heading moved out of `Dashboard.tsx` into `Nav.tsx`; the Dashboard test that previously asserted the heading rewritten to assert `alice@example.com` (account row) is rendered, since Nav.test.tsx already covers the heading. `App.tsx` now wraps `<Nav />` + `<AppRoutes />` in `<BrowserRouter>`. `router.tsx` exports `<AppRoutes />` (the `<Routes>`+`<Route>` block). The integration test uses `<MemoryRouter>` + `<Nav />` + `<AppRoutes />` rather than `<App />` directly so we don't double-wrap with two routers — testing the routes via the public-facing `<AppRoutes />` API is more idiomatic in v6 anyway. Full suite: 23 files / 146 tests passing in 3.08 s. `tsc --noEmit` clean. `npm run build` clean — client bundle now 224.70 kB (gzip 71.43 kB), up from 198.10 kB; the +26.6 kB raw / +9.3 kB gzipped delta is `react-router-dom@6` plus `@remix-run/router`, expected and within v1 budget._

- [x] **Step 10: typecheck + integration smoke (red→green wrap-up).** Run `npx tsc --noEmit -p tsconfig.json`. Fix any cross-tree type errors surfaced (likely candidates: the `OAuth2Client` cast in `gmail/client.ts`, the `MemoryRouter` types in tests, the `Schema$Message` import). Run `npx vitest run` — all server + client + scripts tests should be green. Run `npm run build` end-to-end: `check:gmail-readonly` → `vite build` → `tsc -p tsconfig.server.json` → `cpSync` of migrations. Confirm `dist/server/gmail/client.js`, `dist/server/api/messages.js`, `dist/client/index.html` all exist. Verification: `tsc --noEmit -p tsconfig.json` exits 0; `npm test` exits 0; `npm run build` exits 0; `ls dist/server/gmail/client.js dist/server/api/messages.js dist/client/index.html` succeeds. — _Done: all four checks pass on a clean tree (`rm -rf dist && npm run build`). 23 files / 146 tests / 3.78 s. `tsc --noEmit` exit 0. `npm run build` runs check:gmail-readonly → vite build → tsc → cpSync, all clean. Verified `dist/server/gmail/client.js`, `dist/server/api/messages.js`, `dist/client/index.html`, and `dist/server/db/migrations/0001_create_accounts.sql` all present. No further fixes needed in this step — Steps 1–9 already kept the trees consistent on each iteration._

## Smoke test recipe

The exact sequence the loop will run after all plan steps are checked. All commands run from the repo root.

1. `docker compose down -v` (idempotent reset).
2. Confirm `.env` exists with non-empty `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET`. (If absent, the Docker portion is skipped — record the skip and continue with the local-host substitute.)
3. `docker compose up --build -d`. The build runs `npm run check:gmail-readonly` first; the build log should contain `OK: no forbidden Gmail-write substrings in src/`. Poll `curl -fsS http://localhost:3737/health` until 200 (cap ~60s).
4. **AC #7 / AC #8 — build-time-check failure path.** Edit a source file under `src/` to add a comment `// uses messages.modify here`. Run `npm run build` directly (faster than re-launching Docker). Expected: exit code non-zero; stderr names the file path and the substring `messages.modify`. Revert the edit. Re-run `npm run build`: exit 0.
5. Open `http://localhost:3737/` in a browser. Confirm the nav at the top shows `Dashboard | Inbox` and the heading reads `Docurator` once (not twice). Click `Inbox` — URL becomes `/inbox` without a full reload.
6. With at least one connected account (added in Slice-002's smoke or via the live Add flow), on `/inbox`: the picker is preselected to the first connected account; a brief loading state is visible; then a table of 50 rows renders showing Subject / Sender / Date. Spot-check 3 rows against Gmail's web UI for that account — the Subject, From, and Date values should match exactly. **AC #1 / AC #2.**
7. Switch the picker to a different connected account. Confirm the table reloads with that account's messages. **AC #3.**
8. Refresh the page — the picker remembers the most recently used account (localStorage). Note: the account picker selection (a single integer id) is the only client-side persistence; no email content is in storage.
9. **AC #4 — needs_reauth disabled in picker.** `sqlite3 data/app.db "UPDATE accounts SET status='needs_reauth' WHERE id=1;"`. Refresh `/inbox`. The picker shows row 1 as a disabled option labelled `(needs reauth — reconnect on Dashboard)`. The selected account changes to the next `connected` row (or null) automatically. Click `Dashboard` — the row shows the Reconnect button.
10. **AC #5 — revoke at Google → needs_reauth flip via Inbox.** With the container still running, in another browser tab go to `https://myaccount.google.com/permissions` and revoke the app's access for one connected account. Wait for the access token to expire (or restart the container without `--build` to clear in-memory tokens, then navigate to `/inbox` and select that account). The Inbox renders the "needs reauth — reconnect on Dashboard" error state for that account; the row in the picker is now disabled. The Dashboard now shows that account's Reconnect button. Other accounts continue to work — switch the picker to a different connected account, the table loads its messages. (This also exercises Slice 002's AC #5 end-to-end for the first time.)
11. **AC #6 — no DB writes during Inbox flow.** Before the smoke run: `sqlite3 data/app.db 'SELECT * FROM accounts;'` and `sqlite3 data/app.db '.tables'`. After the smoke run: re-run the same queries. Confirm the only delta is `last_seen_at` and `status` flips from the reconnect/revoke flows; no new tables, no new rows, no rows in any table other than `accounts` and `_migrations`.
12. **AC #9 — only the three required OAuth scopes.** `grep -rn 'gmail\.[a-z]' src/ --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` should print only `gmail.readonly` references; no `gmail.modify`, `gmail.send`, `gmail.metadata`, etc.
13. `git status` clean.
14. `docker compose down`.

## Test run

- **Date:** 2026-05-09
- **Command:** `npx vitest run`
- **Result:** 23 test files, **146 tests, all passing**. Duration 2.84 s.
  - Server (12 files, 99 tests):
    - `scripts/check-gmail-readonly.test.ts` — 7 tests (clean tree, single hit, multi-substring, nested walk, self-exempt, extension filter incl. `.md` skip, positive guard against real `src/`)
    - `src/server/config.test.ts` — 11 tests (carried over from Slice 002)
    - `src/server/app.test.ts` — 3 tests (carried over)
    - `src/server/db/index.test.ts` — 3 tests (carried over)
    - `src/server/db/migrate.test.ts` — 4 tests (carried over)
    - `src/server/db/migrations.test.ts` — 4 tests (carried over)
    - `src/server/auth/slug.test.ts` — 8 tests (carried over)
    - `src/server/auth/accounts.test.ts` — 8 tests (carried over)
    - `src/server/auth/oauth.test.ts` — 7 tests (carried over)
    - `src/server/auth/session.test.ts` — 8 tests (carried over)
    - `src/server/api/accounts.test.ts` — 2 tests (carried over)
    - `src/server/api/oauth.test.ts` — 16 tests (carried over)
    - `src/server/gmail/client.test.ts` — 7 tests (listMessages userId/maxResults, missing-messages-field normalization, q/pageToken passthrough, withFreshTokens accountId routing, listMessages session-error rethrow, getMessage args passthrough, getMessage session-error rethrow)
    - `src/server/api/messages.test.ts` — 14 tests (404 missing-row, 400 non-integer id, 409 needs_reauth, 409 no-session row-flip, 200 happy path with shape assertion, missing headers → empty strings, case-insensitive header lookup, 400 limit=0, 400 limit=200, limit=25 passthrough, default limit=50, 401 invalid_grant from listMessages, 502 generic from listMessages, 502 from per-message getMessage failure)
  - Client (10 files, 47 tests):
    - `src/client/jsdom-env.test.tsx` — 1 test (carried over)
    - `src/client/api.test.ts` — 6 tests (carried over)
    - `src/client/components/AccountList.test.tsx` — 6 tests (carried over)
    - `src/client/components/AddAccountButton.test.tsx` — 4 tests (carried over)
    - `src/client/views/Dashboard.test.tsx` — 6 tests (heading assertion dropped — Nav owns the heading now; the rest carried over)
    - `src/client/components/Nav.test.tsx` — 3 tests (heading + both links, aria-current on `/`, aria-current on `/inbox`)
    - `src/client/components/AccountPicker.test.tsx` — 6 tests (empty state, value reflection, onChange numeric id, includeDisconnected=true with disabled+suffix, includeDisconnected=false filters out, only-needs_reauth → empty state)
    - `src/client/views/Inbox.test.tsx` — 9 tests (preselect first connected, init from localStorage, fallback when stored id missing, fallback when stored id is needs_reauth, picker change re-fetches + persists, 401 → reconnect message, 502 → gmail_error message, no accounts empty state, only-needs_reauth empty state)
    - `src/client/router.test.tsx` — 3 tests (Dashboard at `/`, Inbox at `/inbox`, click-Inbox navigates without full reload)
- **Coverage of acceptance criteria (per spec § "Acceptance criteria"):**
  - AC #1 (Inbox view + picker preselects first connected + 50-row table) — `Inbox.test.tsx` "preselects the first connected account…", `messages.test.ts` happy-path shape test, `messages.test.ts` default-limit=50 test.
  - AC #2 (rows match Gmail's web UI for that account) — covered by API contract: `messages.test.ts` happy-path returns the headers verbatim from the Gmail SDK fake; in-browser visual match against Gmail is part of the priority-5 smoke.
  - AC #3 (switching account reloads + no leakage) — `Inbox.test.tsx` "changing the picker fetches the new account's messages…" (asserts the previous Subject is gone after the switch).
  - AC #4 (needs_reauth disabled in picker) — `AccountPicker.test.tsx` "shows needs_reauth options as disabled with a 'reconnect' suffix…".
  - AC #5 (revoked refresh token → row flips to needs_reauth + Inbox error state, others continue) — `messages.test.ts` "returns 401 needs_reauth when listMessages rejects with invalid_grant" + `Inbox.test.tsx` 401 → reconnect message; the no-session-row-flip path is exercised by `messages.test.ts` "flips a connected-but-no-session account to needs_reauth and returns 409"; the cross-account-isolation property is structural (per-account routing in the URL + per-account session entries) and verified through the API tests.
  - AC #6 (no rows written to any SQLite table during the Inbox flow) — structural: `messages.ts` makes no DB writes other than the `accounts.updateStatus(id, 'needs_reauth')` call when a session is missing (which is the documented row-flip behavior, *not* a fresh write to a new row); the `migrations` step in the smoke recipe verifies the DB tables list is unchanged.
  - AC #7 (`npm run build` fails on a forbidden substring; passes when none) — `scripts/check-gmail-readonly.test.ts` exercises both directions; the build script wires `npm run check:gmail-readonly` as the first step.
  - AC #8 (deliberate `messages.modify` reference fails the build with a clear message) — same `scripts/check-gmail-readonly.test.ts` "reports a single hit with file path and substring" + the `npm run build` smoke step.
  - AC #9 (zero references to OAuth scopes other than `gmail.readonly`, `userinfo.email`, `openid`) — `gmail.metadata` and `gmail.labels` are explicitly in the FORBIDDEN list (ADR-003 covers why); `auth/oauth.test.ts`'s SCOPES test asserts the consent URL contains exactly those three scope strings.

## Smoke run

- **Date:** 2026-05-09
- **Result:** Pass for everything verifiable from the agent shell (build-time check, codebase scope grep, build artifacts, persistence). The browser-driven and real-Gmail portions are recorded as deferred and will be exercised by the human at acceptance.

**What was exercised**

The full Docker portion of the smoke recipe could not run because the user already had `npm run dev` (`tsx watch --env-file=.env src/server/index.ts`) listening on port 3737 — `docker compose up` would have collided on the same port mapping. Substituted a **subset of the local-host smoke** that exercises code paths Docker would, plus the build-time-check failure path which is the entire test for AC #7 and AC #8.

1. **Build-time check failure path (AC #7 + AC #8).**
   - Appended a comment line `// uses messages.modify here` to `src/server/config.ts`.
   - Ran `npm run build` → exit code **1** with stderr line: `FAIL: src/server/config.ts contains forbidden substring "messages.modify"`. The build aborted before `vite build`, before `tsc`, before the migration cpSync — first step in the chain failed cleanly.
   - Reverted the edit. Re-ran `npm run build` → exit code **0**, full build chain ran (`OK: no forbidden Gmail-write substrings in src/` → vite build (41 modules → 224.70 kB client bundle) → tsc → cpSync of migrations).
   - `dist/server/gmail/client.js`, `dist/server/api/messages.js`, `dist/client/index.html`, `dist/server/db/migrations/0001_create_accounts.sql` all present after the clean build.
2. **AC #9 — codebase contains only the three required Gmail scope strings.**
   - `grep -rn 'gmail\.[a-z]' src/ --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` → 3 hits:
     - `src/server/auth/oauth.ts:7: 'https://www.googleapis.com/auth/gmail.readonly'` — the only Gmail OAuth scope.
     - `src/server/gmail/client.ts:49: gmail.users.messages.list(...)` — read endpoint, not a scope string.
     - `src/server/gmail/client.ts:65: gmail.users.messages.get(...)` — read endpoint, not a scope string.
   - Zero references to `gmail.modify`, `gmail.send`, `gmail.metadata`, `gmail.labels`, etc. The build-time check enforces the negative; this grep confirms the positive.
3. **Test suite as a continuous smoke proxy.** The 146-test suite runs the full API contract (`messages.test.ts`'s 14 cases cover happy path + every error branch including invalid_grant→401, generic→502, no-session→409+row-flip, missing-row→404, non-integer-id→400, limit clamping). The Gmail SDK is faked via `setGmailFactoryForTest`; the network surface is structural rather than dynamic. Combined with the AC-coverage map in `## Test run`, every AC except the human-driven ones (consent screen, in-browser table render, real Google account revoke) is verified at code-contract level.
4. **Build-output integrity.** After the post-revert clean build, all expected artifacts exist:
   - `dist/server/gmail/client.js` — Gmail wrapper.
   - `dist/server/api/messages.js` — messages route.
   - `dist/server/db/migrations/0001_create_accounts.sql` — migration SQL still copied through.
   - `dist/client/index.html` + `dist/client/assets/index-*.js` (224.70 kB / 71.43 kB gzipped) — the SPA bundle including `react-router-dom`.

**Deferred (require human + real Google account + browser)**

These are not failures; they are the parts of the smoke recipe that need a person at the keyboard. Recorded here so the human knows what to verify before accepting.

- **AC #1 — Inbox view + picker preselects first connected + 50-row table.** Requires opening `http://localhost:3737/inbox` in a browser, observing the picker default, and counting the rows.
- **AC #2 — row values match Gmail's web UI verbatim.** Requires opening Gmail's web UI for the same account and spot-checking 3 rows. The unit tests assert the API returns header values verbatim from the Gmail SDK fake; the live-Gmail correspondence step is what the human confirms.
- **AC #3 — switching the picker reloads the table with no leakage.** Visible in `Inbox.test.tsx`'s "changing the picker fetches the new account's messages" case (asserts the previous Subject is gone after the switch); the in-browser version is the human's read.
- **AC #4 — needs_reauth row is disabled in the picker dropdown.** `AccountPicker.test.tsx` covers the rendering; the human confirms the visual+interactive behavior.
- **AC #5 — revoking at https://myaccount.google.com/permissions causes the next /inbox call for that account to flip its status to needs_reauth, surface the error state in the Inbox, and continue working for the other accounts.** Slice 002's smoke recipe deferred this AC's end-to-end exercise to *this* slice; with the route in place, the path is verifiable manually but requires waiting for the access token to expire (~1h) or a container restart that wipes the in-memory tokens. The unit tests in `messages.test.ts` cover both the `invalid_grant`→401 path (when refresh fails mid-request) and the missing-session→409+row-flip path (when tokens are gone).
- **AC #6 — no SQLite writes during the Inbox flow.** The handler's only write is `accounts.updateStatus(id, 'needs_reauth')` when the session is gone — which is the documented row-flip behavior, not a fresh-row insert. No `INSERT` or `CREATE` paths exist in `messages.ts`. The human can verify by running `sqlite3 data/app.db 'SELECT COUNT(*) FROM accounts; .tables'` before and after exercising the Inbox; only the `last_seen_at` and `status` columns of existing rows can change.
- **Docker layer of the smoke recipe** (`docker compose up --build -d` followed by the in-browser walkthrough). Blocked here because the user's `npm run dev` is already on :3737. The human should `docker compose down` their dev process before running the Docker smoke, or substitute the dev server (`npm run dev` already serves the UI through Vite at :5173 with the API proxied through to :3737, so the Inbox is reachable in dev too).

**Caveats**

- The agent's local-host substitute for the smoke (`node dist/server/index.js`) failed to bind because port 3737 is already in use by the user's running `tsx watch` dev server. Subsequent curls in the agent shell hit *the user's* server, not the freshly-built dist. The build-output verification (artifact presence + check-script behavior) is independent of which server is bound to :3737, so AC #7, AC #8, and AC #9 stand on their own. The agent did not stop or interfere with the user's running dev server.
- The two React Router future-flag warnings (`v7_startTransition`, `v7_relativeSplatPath`) print to stderr during client tests and during in-browser navigation. They are advisory and have no functional effect; both are silenced by passing `future={{ v7_startTransition: true, v7_relativeSplatPath: true }}` on `<BrowserRouter>` / `<MemoryRouter>`. Deferred to the spec's review as a follow-up.
- Stale `tsx watch` state for the user's running dev server is suspected but unverified — `/api/accounts/1/messages` returned `500 Internal Server Error` from that process even though my route's error handling is comprehensive. Could be a hot-reload corner case (the file's first compile not picked up cleanly), or could be a real runtime path that the unit tests don't cover. Recommend the human stop the dev server, then re-launch (`npm run dev`) before manual smoke. Flagged in the review.
