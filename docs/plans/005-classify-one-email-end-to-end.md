# Slice 005: Classify one email end-to-end — Plan

**Spec:** `docs/specs/005-classify-one-email-end-to-end.md`
**Research:** `docs/research/005-classify-one-email-end-to-end.md`

## Steps

Each step is small enough to fit in one loop iteration. Each step ends with a concrete verification — a named test passing, or a specific command's output. Per-step refactor is folded into the priority-3 action. The research doc identified three ADRs the slice should produce; each is attached to the step that triggers the decision.

- [x] **Step 1: Install five new runtime deps.** Edit `package.json` to add `zod`, `@hono/zod-validator`, `pdfjs-dist`, `@napi-rs/canvas`, `node-html-parser` to `dependencies`. Pick versions: `zod ^3.23`, `@hono/zod-validator ^0.4`, `pdfjs-dist ^4.x` (latest 4-line; the legacy/Node build lives at `pdfjs-dist/legacy/build/pdf.mjs`), `@napi-rs/canvas ^0.1.x`, `node-html-parser ^7.x`. Run `npm install`. Verification: `npm install` exits 0; `package-lock.json` shows the five new entries; `npm run typecheck` exits 0; `npm run check:gmail-readonly` exits 0; `npx vitest run` continues to pass (no new tests yet — this step only validates that the deps don't break the build). If `@napi-rs/canvas` fails to install (libc mismatch), document the actual failure mode and pick a sibling package per the research doc's "Risks" section. No source code changes — this step's only artifacts are the two manifest files.

  _Done: installed `@hono/zod-validator@^0.4.3`, `@napi-rs/canvas@^0.1.100`, `node-html-parser@^7.1.0`, `pdfjs-dist@^4.10.38`, `zod@^3.25.76` (npm picked the latest matching minor in each range; all five lines satisfy the planned semver). +145 transitive packages, 5 moderate audit findings (Vite-side, same class as Slice 001's vitest finding — accepted for a local-only dev tool). Pre-flight: the install wiped `better-sqlite3`'s rebuilt native binary (same root cause Slice 004's review noted); `npm rebuild better-sqlite3` restored it. Without that step the entire server suite fails with "Module did not self-register" — flagging for the review as a recurring devcontainer hazard. After rebuild: `check:gmail-readonly` OK, `typecheck` OK, full vitest suite 30 files / 254 tests / 0 failures in 17.58s. No source changes; only `package.json` + `package-lock.json` modified._

- [x] **Step 2: Add `ollamaUrl`, `ollamaModel`, `ollamaTimeoutMs` to `src/server/config.ts`.** Extend `src/server/config.test.ts` first with three new cases (defaults applied; env overrides honored; bogus `OLLAMA_TIMEOUT_MS` falls back to default 120000). Run vitest — **red**. Add three frozen fields to the `config` object: `ollamaUrl: process.env.OLLAMA_URL ?? 'http://host.docker.internal:11434'`, `ollamaModel: process.env.OLLAMA_MODEL ?? 'qwen2.5vl:7b'`, `ollamaTimeoutMs: <Number-or-default>`. Re-run **green**. Verification: `npx vitest run src/server/config.test.ts` passes; the three new field defaults match the spec verbatim.

  _Done: added 7 new tests (default + env-override per field × 3, plus the non-numeric fallback for timeout). The timeout parser uses `Number.isFinite(parsed) && parsed > 0` rather than the simpler `Number(...) || default` because `OLLAMA_TIMEOUT_MS=0` is a degenerate value that should also fall back (a zero-timeout AbortController would abort before fetch sends a byte). `OLLAMA_TIMEOUT_MS=banana` produces NaN → falls back to 120000. Three new constants (`DEFAULT_OLLAMA_URL`, `DEFAULT_OLLAMA_MODEL`, `DEFAULT_OLLAMA_TIMEOUT_MS`) keep the default values in named form near the top of the file. `ENV_KEYS` in the test file extended with the three new keys so the `beforeEach`/`afterEach` env clean-up covers them. Verification: `npx vitest run src/server/config.test.ts` → 20/20 green in 18ms._

- [x] **Step 3: Document the three new env vars in `.env.example`.** Append three commented sections under the existing entries: `OLLAMA_URL`, `OLLAMA_MODEL`, `OLLAMA_TIMEOUT_MS`, each with default and a one-line description (drawn from spec § "Deliverables — Env vars / configuration"). No code change. Verification: `git diff .env.example` shows only three new commented blocks; `grep -c '^# ' .env.example` reflects the new comment count. (No test framework covers `.env.example`; the spec acceptance criterion is the file's content.)

  _Done: appended three commented sections at the bottom of `.env.example`. The `OLLAMA_URL` block calls out the Linux fallback to `http://172.17.0.1:11434` per the research doc's risk note (devcontainer's `runArgs` lacks `--add-host=host.docker.internal:host-gateway`). `OLLAMA_MODEL` notes the vision-capable requirement. `OLLAMA_TIMEOUT_MS` documents the non-numeric / non-positive fallback that step 2's parser implements. All three lines are commented with the default value visible on the disabled line — pattern matches the existing `OAUTH_REDIRECT_PORT` and `NODE_ENV` blocks. Verification: `grep -E '^# OLLAMA_' .env.example` lists all three entries with their defaults._

- [x] **Step 4: Refactor — extract `isInvalidGrantError` to `src/server/auth/invalid-grant.ts`.** New file containing the exact 8-line function from `src/server/api/messages.ts` and `src/server/api/dev.ts`. New test file `src/server/auth/invalid-grant.test.ts`:
  - `Error('invalid_grant: bad token')` returns true.
  - `{ response: { data: { error: 'invalid_grant' } } }` returns true.
  - `Error('something else')` returns false.
  - `null`, `undefined`, `'string'`, `42` return false.

  Run vitest — **red** (test file exists, function doesn't). Create the function. Re-run **green**. Replace the in-file definitions in `messages.ts` and `dev.ts` with imports; delete the local declarations. Re-run the affected tests — `npx vitest run src/server/api/messages.test.ts src/server/api/dev.test.ts src/server/auth/invalid-grant.test.ts` — **all green**. Verification: the three test files pass; `grep -rn 'function isInvalidGrantError' src/` returns one hit (the new file).

  _Done: 9 new tests in `invalid-grant.test.ts` (covers Error path, structured-response path, plain Error, null, undefined, string, number, plus negative `response.data.error` and missing-`response.data` shapes). Discovered a **third** copy of the helper in `src/server/auth/session.ts` (line 97) that the research/plan missed — folded into the same step since it's the same function and the import is one line. The session.ts copy is consumed at line 87 (token-refresh failure path). Replaced all three duplicates with `import { isInvalidGrantError } from './invalid-grant.js'` (or `'../auth/invalid-grant.js'` from the api/ files). Final grep confirms only `invalid-grant.ts` defines the function. Full suite: 31 files / 270 tests / 0 failures in 15.97s. Flagging the session.ts find for the review — three duplicates is the bar for extraction; this slice closes the loop on all three._

- [x] **Step 5: Refactor — extract `requireConnectedAccount` to `src/server/auth/preconditions.ts`.** Signature:
  ```ts
  export function requireConnectedAccount(accountId: number):
    | { ok: true; account: Account }
    | { ok: false; status: 404 | 409; body: { error: string; status?: AccountStatus } }
  ```
  New test file `src/server/auth/preconditions.test.ts` (Node env, real DB via `setDbPathForTest` + `migrate`):
  - Unknown id → `{ ok: false, status: 404, body: { error: 'account_not_found' } }`.
  - Account in `needs_reauth` → `{ ok: false, status: 409, body: { error: 'account_not_connected', status: 'needs_reauth' } }`.
  - Account `connected` but `session.get` returns undefined → flips DB row to `needs_reauth` and returns 409.
  - Account `connected` with session → `{ ok: true, account }`.

  Run vitest — **red**. Implement the helper. Update `messages.ts` and `dev.ts` to call `requireConnectedAccount(id)` and short-circuit with `c.json(result.body, result.status)` when not ok. Re-run the affected handler tests — they continue to pass (the surfaced HTTP behavior is identical). Verification: `npx vitest run src/server/auth/preconditions.test.ts src/server/api/messages.test.ts src/server/api/dev.test.ts` all green; `grep -n 'findById' src/server/api/messages.ts src/server/api/dev.ts` returns no hits in those handlers (the lookup is now inside `requireConnectedAccount`).

  _Done: 4 new tests in `preconditions.test.ts` covering all four branches (404 / 409-needs-reauth / 409-no-session-with-DB-flip / ok). The needs-reauth status field is typed `AccountStatus` (not just `'needs_reauth'`) so the helper can faithfully reflect a future hypothetical third status without a type widening. Discriminated-union return shape lets handlers do `if (!pre.ok) return c.json(pre.body, pre.status)` in two lines (down from twelve). Both call sites trimmed: messages.ts dropped 17 lines + the `accounts` and `session` direct imports; dev.ts dropped 14 lines + the same imports. Status-code literal type `404 | 409` allows Hono's strict status-typed `c.json(body, status)` overload to accept the value without a cast. Final grep confirms `findById` no longer appears in either handler. Full suite: 32 files / 274 tests / 0 failures in 15.49s. Note: I did not move existing API-handler tests covering 404/409 paths — those still verify the surfaced HTTP behavior at the right level (the route returns 404 / 409 with the right body), and the new precondition tests verify the helper directly. Both layers stay independently meaningful._

- [x] **Step 6: Add `getAttachment` to `src/server/gmail/client.ts`.** Extend `src/server/gmail/client.test.ts` with a `describe('getAttachment', …)` block (3-4 tests):
  - Calls `users.messages.attachments.get` with `{ userId: 'me', messageId: 'abc', id: 'att-1' }`.
  - Decodes base64url payload (`'aGVsbG8'` → `Buffer.from('hello')`) and exposes `data: Buffer` plus `size`.
  - Network errors propagate verbatim.
  - The wrapper uses the same `withFreshTokens(accountId, …)` pattern as `getMessage`.

  Run vitest — **red**. Add `getAttachment(messageId, attachmentId): Promise<{ data: Buffer; size: number }>` to the `GmailClient` type and the implementation. Re-run **green**. Verification: `npx vitest run src/server/gmail/client.test.ts` passes; `npm run check:gmail-readonly` still exits 0 (the new substring `attachments.get` is not on the forbidden list).

  _Done: 6 new tests in the `createGmailClient.getAttachment` describe — call shape, base64url decode (round-trips `'aGVsbG8'` → `'hello'`), URL-safe alphabet decode (round-trips bytes that produce `-`/`_`), `withFreshTokens` routing with `accountId`, session-error rethrow, API-error rethrow. Implementation: `Buffer.from(raw, 'base64url')` (Node ≥ 16, no manual `+`/`-` swap or padding), `size` defaulted to `0` when the API response omits it (defensive — the spec promises a number, but the type is `number | null | undefined` from `googleapis`). Adding `getAttachment` to the `GmailClient` interface broke the inline fakes in `messages.test.ts` (5 sites) and `dev.test.ts` (3 sites including the two builder helpers `makeGmailFixture` and `makeFailingClient`); each got a `getAttachment: async () => ({ data: Buffer.alloc(0), size: 0 })` stub since none of those tests exercise attachments. Vitest didn't catch the missing property at first because it doesn't enforce TS strictness — only `npm run typecheck` did. Slice 004's review's note about `tsc --noEmit` catching the same class of error after a test file lands replays here. `check:gmail-readonly` still exits 0 (`attachments.get` is not on the forbidden list). Full suite: 32 files / 280 tests / 0 failures in 18.72s._

- [x] **Step 7: Zod schemas in `src/server/classify/schema.ts`.** New file. Define `classificationSchema`, `artifactSchema`, `classifyResponseSchema` per the research doc. Export inferred types: `Classification`, `Artifact`, `ClassifyResponse`. Test file `src/server/classify/schema.test.ts`:
  - Valid spec-shape parses; output type-checks.
  - `classification: 'spam'` rejects.
  - `confidence: 'ultra'` rejects.
  - `transaction_date: '2026-13-99'` parses (regex shape only — month/day validity is out of scope this slice).
  - `transaction_date: 'tomorrow'` rejects.
  - Negative `amount` rejects; zero accepted.
  - 4-letter `currency` rejects; 3-letter accepted.
  - Omitting all four optional fields parses.

  Run vitest — **red**. Implement the schemas. Re-run **green**. Verification: `npx vitest run src/server/classify/schema.test.ts` passes; the file uses `import { z } from 'zod'`.

  _Done: created the `src/server/classify/` directory with two files (`schema.ts` + `schema.test.ts`). Three exported schemas: `classificationSchema` (the model's JSON output), `artifactSchema` (one entry per body / attachment fed to the model), and `classifyResponseSchema = classificationSchema.extend({ model_used, artifacts })` (the full HTTP response — composes via `.extend` so the model's fields and the runtime metadata stay in one place). Inferred types `Classification`, `Artifact`, `ClassifyResponse` exported alongside. The `transaction_date` regex is a single-line constant near the top of the file — re-stating "shape only, calendar validity not enforced" in a comment. 17 new tests cover all the planned cases (minimal valid object, full valid object, every enum-mismatch case, the deliberate "regex passes / calendar would reject" case for `2026-13-99`, negative amount, zero amount, 4-letter currency, 3-letter currency, three artifact cases, three full-response cases). All tests pass in 9ms (Zod parsing is fast). The file is the slice's first use of `import { z } from 'zod'`._

- [x] **Step 8: Body extractor in `src/server/classify/extract-body.ts`.** New file. Pure function `extractBodyText(payload: gmail_v1.Schema$MessagePart): { text: string; html_was_used: boolean; inline_image_count: number }`. Test file `src/server/classify/extract-body.test.ts` with the six fixture cases from the research doc. Helpers in tests: small builders that return `Schema$MessagePart`-shaped objects with base64url-encoded body data. Use `Buffer.from('Hello world', 'utf8').toString('base64url')` as the body data for plain-text payloads. Run vitest — **red**. Implement: recursive walk of `parts`, prefer `text/plain`, fall back to `text/html` via `node-html-parser`'s `parse(html).text`. Count parts with `Content-Disposition: inline` headers. Re-run **green**. Verification: `npx vitest run src/server/classify/extract-body.test.ts` passes (≥ 6 cases).

  _Done: 9 new tests in `extract-body.test.ts` (single text/plain, single text/html, multipart/alternative preferring plain, nested multipart/mixed → multipart/alternative recursion, inline-image counting, no body returns empty without throw, html fallback when plain missing, empty body data, HTML whitespace collapsing). The implementation walks the MIME tree once, collecting the *first* plain-text part and the *first* HTML part separately; if a plain-text part exists at any depth it wins, regardless of order. Inline images are detected by `Content-Disposition: inline` header substring (case-insensitive on both name and value). Non-inline attachments (filename present) are skipped — they're handled by `extract-attachments.ts` (next step). Edge case in the implementation: an empty text/plain body (no `data` field) sets `plain = ''` rather than `plain = undefined`, so the extractor doesn't fall back to HTML for a deliberately-empty plain part. The `htmlToText` helper uses `node-html-parser`'s `root.text` plus a `\s+` collapse — sufficient for the model's prompt input. Tests pass in 12ms. The `node-html-parser` import shape is `import { parse as parseHtml } from 'node-html-parser'`._

- [x] **Step 9: Attachment metadata extractor in `src/server/classify/extract-attachments.ts`.** New file. Pure function `extractAttachmentMetadata(payload): { all: AttachmentRef[]; receipt_eligible: AttachmentRef[] }`. `AttachmentRef = { part_id: string; attachment_id: string; filename: string; mime_type: string; size: number }`. The "receipt-eligible" filter keeps mime types in `{ image/png, image/jpeg, image/gif, image/webp, application/pdf }`. Skip parts whose `Content-Disposition` is `inline` (those are inline images, handled by `extract-body`'s counter). Skip parts with empty `filename`. Test file with the cases from the research doc (mixed multipart, eligible/non-eligible split, inline exclusion, empty payload). Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/classify/extract-attachments.test.ts` passes.

  _Done: 9 new tests covering undefined payload, plain-text-only payload, mixed-multipart with eligible/non-eligible split (PDF + PNG eligible, ZIP not), full field exposure, inline-attachment exclusion (despite filename), three skip cases (no filename, empty filename, no attachment_id), JPEG/GIF/WebP/TIFF eligibility (TIFF excluded), nested multipart recursion, case-insensitive eligibility check (the spec says "image/png" but a sender might send "IMAGE/PNG" — we lowercase only for the check, store the original mime as-received). The `AttachmentRef` shape diverged slightly from the research doc — dropped `part_id` because Gmail's API doesn't expose a stable part identifier in `Schema$MessagePart` (the `partId` field exists but isn't needed downstream; the orchestrator addresses attachments by their `attachment_id`). Default `size: 0` and `mime_type: 'application/octet-stream'` for the rare case where Gmail returns a part without those fields — defensive nulls become defaults rather than throws. Tests pass in 5ms._

- [x] **Step 10: PDF renderer in `src/server/classify/render-pdf.ts` + commit a tiny PDF fixture + write ADR-004.** New file. `renderPdfToImages(pdfBytes: Uint8Array, maxPages: number): Promise<Buffer[]>` per the research doc's incantation (legacy build, `isEvalSupported: false`, `@napi-rs/canvas` for the canvas, 1.5× scale). Commit a 2-page sample PDF at `src/server/classify/__fixtures__/sample.pdf` (≤ 5 KB, generated once by hand or with a one-shot script — do **not** add a runtime dep just to generate the fixture; check the bytes into the repo). Test file `src/server/classify/render-pdf.test.ts`:
  - `renderPdfToImages(twoPagePdf, 5)` returns 2 PNG buffers; each begins with `\x89PNG\r\n\x1a\n`.
  - `renderPdfToImages(twoPagePdf, 1)` returns 1 buffer.
  - `renderPdfToImages(corruptedBytes, 5)` throws (rejection bubbles up).

  Run vitest — **red**. Implement. If `pdfjs-dist`'s legacy import path fails under Node, iterate on the import incantation per the research doc's risk note (record what was needed in the step's "Done" log). Re-run **green**. **Write `docs/adr/004-server-side-pdf-rendering.md`** (status `Accepted`, dated today, spec `005-classify-one-email-end-to-end.md`, supersedes —): justify `pdfjs-dist + @napi-rs/canvas` over poppler/pdftocairo (system dep), mupdf (system dep + heavier API), GraphicsMagick (system dep), Playwright (already a tech-stack choice for HTML→PDF; could in principle do PDF→images via a data-URL trick but adds a browser-launch overhead per render and requires the Playwright runtime that Slice 006 introduces — for Slice 005 we'd be pulling in the heavy dep early for one use case). Document consequences: image-size addition, no system deps, native binaries to manage. Verification: `npx vitest run src/server/classify/render-pdf.test.ts` passes; `docs/adr/004-server-side-pdf-rendering.md` exists and follows the template.

  _Done: 4 tests in `render-pdf.test.ts` covering 2-page render, maxPages cap to 1, maxPages=0 short-circuit, garbage-bytes rejection. Fixture is a 13.6 KB 2-page PDF generated by `__fixtures__/generate-sample.ts` (committed alongside the fixture so the bytes are reproducible — uses `@napi-rs/canvas`'s `PDFDocument` class). The 13.6 KB exceeds the plan's "≤ 5 KB" target because Skia/PDF font embedding adds ~10 KB; not a meaningful tradeoff at fixture sizes. **pdfjs-dist gotcha (per the research risk note):** the empty `workerSrc = ''` trick the research doc speculated about does **not** work — pdfjs-dist v4 throws `'No "GlobalWorkerOptions.workerSrc" specified.'` on first render. Fix: resolve the worker file's actual path via `createRequire(import.meta.url).resolve('pdfjs-dist/legacy/build/pdf.worker.mjs')` and assign it to `GlobalWorkerOptions.workerSrc` once at module load. The "fake worker" still runs on the main thread (no actual subprocess); the path just satisfies pdfjs-dist's bootstrap check. Other implementation details: `useSystemFonts: false` (hermetic font lookup), white background fill before `page.render(...)` (transparent PDFs would otherwise rasterize as black, which the model occasionally misreads), `await doc.destroy()` to release the parser's references, and an `as unknown as CanvasRenderingContext2D` cast because pdfjs-dist's context type and @napi-rs/canvas's are nominally distinct but API-compatible at runtime. ADR-004 written: 5-paragraph context, decision, 8 consequences (no system deps, image-size cost, native-binary risk, worker setup awkwardness, no incremental render, constraint on later specs, heap pressure bounded, fixture is committed), and 7 alternatives considered (poppler, mupdf-js, pdf-poppler/pdf2pic, GraphicsMagick, Playwright, hypothetical canvas.loadPDF, no rendering at all). Full classify suite: 39 tests across 4 files passing in ~1.2s. Typecheck clean._

- [x] **Step 11: Prompt builder in `src/server/classify/prompt.ts` + write ADR-005.** _(Numbered 005 because it shipped before step 12, per the plan's "either ordering works" note. Step 12's ADR will be 006.)_ New file. `buildClassificationMessages(input): { system: string; user: { content: string; images: string[] } }` returning a structure the Ollama client can serialize directly. The system message is a TypeScript constant containing the task description + the conservative-confidence instruction + the strict JSON-schema description. The user content is a multi-line string with `Subject:`, `From:`, `Date:`, `Inline images: N`, `Attachments: ...`, blank line, `Body:`, then the extracted text. The `images` array holds base64-encoded PNG strings (no data URL prefix — Ollama wire format). Test file `src/server/classify/prompt.test.ts`:
  - System message contains the JSON-schema phrase and the "if unsure, return low" instruction.
  - User content includes `Subject: ...`, `From: ...`, body text.
  - `images` length matches the sum of `attachment_images.length + pdf_pages.length`.
  - Empty body produces an empty `Body:` block but the call still works.

  Run vitest — **red**. Implement. Re-run **green**. **Write `docs/adr/006-classification-prompt-as-typescript-constant.md`** (numbered 006 because step 12 will write 005 — this is fine, ADR numbering is global and the order is incidental; if step 12 hasn't shipped yet, write this ADR as 005 and bump step 12's to 006 — either ordering works as long as numbers are unique and contiguous in the directory). Justify the choice over: external file (still version-controlled, but adds load-time I/O for no benefit), DB row (premature complexity, runtime mutation risk), env var (breaks long-multiline copy), per-request override (non-goal in v1). Document consequences: tuning is a code change + test rerun + commit, which matches a single-developer workflow. Verification: `npx vitest run src/server/classify/prompt.test.ts` passes; the ADR file exists.

  *(Note: the ADR numbering question — write 005 here and 006 in step 12, or vice versa — is decided by the implementer when the step runs. The two ADRs are independent.)*

  _Done: 8 new tests in `prompt.test.ts` (system message contains JSON-schema field names + confidence rule + JSON instruction; user content contains subject/from/date/body; inline image count formatted; attachments list with included/skipped status; "Attachments: none" when empty; images array passed through unchanged; empty body still emits `Body:` section; empty images returned). Implementation: `SYSTEM_PROMPT` template-literal at module top-level (column 0 to avoid leading whitespace in the prompt body); `formatAttachments` helper produces a bulleted block with filename, mime, size, and "included as image input" / "skipped (reason)" suffix. `BuildClassificationInput` adds `included` and `skipped_reason` fields to each attachment metadata so the prompt can faithfully describe what the model is seeing — the orchestrator (step 13) decides which attachments to include vs skip and passes that decision through. ADR-005 (chose 005 instead of the plan's 006 because nothing else has been written since ADR-004 — keeps numbering monotonic by creation order; step 12 takes ADR-006). The ADR runs through the alternatives the plan enumerated plus two more I noticed while writing (Zod-derived prompt, separate prompts package) and pins the constraint that Slice 014/015's prompt-touching work inherits this same lookup pattern. Full classify suite: 47 tests across 5 files, ~1.3s. Typecheck clean._

- [x] **Step 12: Ollama HTTP client in `src/server/classify/ollama.ts` + custom error classes + write ADR-006.** _(Numbered 006 because step 11 already took 005, per the plan's "either ordering works" note.)_ New file. Export `chat({ model, messages, format, timeoutMs })` returning `Promise<string>` (the assistant's content), `listModels({ timeoutMs })` returning `Promise<string[]>`, and the three custom error classes `OllamaUnreachableError`, `OllamaHttpError`, `OllamaParseError` (the latter is constructed by the orchestrator, but live next to the HTTP client for cohesion — alternatively house them in `errors.ts`). Implementation per the research doc: `globalThis.fetch` against `${ollamaUrl}/api/chat` and `${ollamaUrl}/api/tags`, `AbortController` with `setTimeout`, `try/finally` cleanup. The `messages` shape is the Ollama wire format: `[{ role: 'system', content }, { role: 'user', content, images? }]`. Test file `src/server/classify/ollama.test.ts` with the cases from the research doc. Mock `globalThis.fetch` via `vi.spyOn(globalThis, 'fetch').mockImplementation(...)`. Run vitest — **red**. Implement. Re-run **green**. **Write `docs/adr/005-synchronous-classify-endpoint.md`**: justify the per-message synchronous endpoint with `OLLAMA_TIMEOUT_MS=120000` over: SSE / streaming progress (machinery for batch sync, deferred to Slice 006), polling (more endpoints, more state, worse UX for one message), background job + result retrieval (premature; the spec's Observable result is "click → see verdict" which a synchronous call delivers most simply). Document consequences: browser keep-alive considerations, blocked-row UX during the wait, ergonomics of a single endpoint. Verification: `npx vitest run src/server/classify/ollama.test.ts` passes; the ADR file exists. (Number it 005 if step 11's ADR is 006; or 006 if step 11's is 005 — see step 11's note.)

  _Done: 13 new tests in `ollama.test.ts` covering chat happy path (URL/method/body shape verified, response content returned), absent `format` field omitted from the body, OllamaHttpError on 5xx, OllamaUnreachableError on `fetch` rejection, OllamaUnreachableError on AbortController timeout (mocks the abort path with a hand-rolled Promise that listens for `signal.abort`), missing `message.content` throws a generic Error, listModels returns names, returns `[]` for empty models, returns `[]` when `models` field is missing, network failure → OllamaUnreachableError, non-2xx → OllamaHttpError, plus two structural tests for the error classes themselves. Implementation: only **two** error classes shipped (`OllamaUnreachableError`, `OllamaHttpError`); `OllamaParseError` deferred to step 13 (the orchestrator constructs it next to the JSON.parse / Zod call where the raw response lives — keeping it next to the HTTP client would force the orchestrator to re-import for one usage). Both ship from the same module. `chat` and `listModels` go through a private `fetchWithTimeout` helper that wires AbortController + setTimeout + try/finally cleanup. The fetch-mock typing took an iteration: `vi.fn(async () => ...)` infers `() => Promise<Response>` (zero args), making `mock.calls[0]` a tuple of length 0 and `[1]?.body` a typecheck error (vitest doesn't enforce TS strictness; `tsc --noEmit` does, again — same hazard as Step 6's class). Fixed by introducing a `FetchMockFn` type alias matching `fetch`'s signature and using `vi.fn<FetchMockFn>(...)`. ADR-006 written: 9 consequences (simplest UI surface, browser keep-alive, connection slot occupancy, no partial progress, server-side timeout location, predictable error mapping, constraint on later specs, generous timeout default, no retry-on-timeout) and 6 alternatives (SSE, polling+job-id, WebSocket, background-job + email, shorter timeout + retry, no timeout). Full classify suite: 6 files / 60 tests / 0 failures in ~1.2s. Typecheck clean._

- [x] **Step 13: Orchestrator in `src/server/classify/index.ts`.** New file. Export `classifyMessage(args, deps?): Promise<ClassifyResponse>`. The deps object accepts overrides for testing: `createGmailClient`, `chat`, `renderPdfToImages`. Pipeline:
  1. `client = (deps.createGmailClient ?? defaultCreateGmailClient)(args.account_id)`.
  2. `message = await client.getMessage(args.message_id, { format: 'full' })`.
  3. Read `Subject`, `From`, `Date` headers via `extractHeader`.
  4. `body = extractBodyText(message.payload)`.
  5. `attachments = extractAttachmentMetadata(message.payload)`.
  6. For each `receipt_eligible` attachment with `size <= MAX_ATTACHMENT_BYTES (5 * 1024 * 1024)`: fetch via `client.getAttachment`, decode (already a Buffer), and either pass through (image/*) or `renderPdfToImages(data, MAX_PDF_PAGES=5)` (PDF). Build the `images: string[]` (base64-encoded PNG / image bytes — for non-PNG images, base64-encode the original bytes; Ollama's vision pipeline handles JPEG/PNG/GIF/WebP transparently). Build the `artifacts` array per the spec.
  7. Build messages via `buildClassificationMessages(...)`.
  8. `raw = await chat({ model: config.ollamaModel, messages, format: 'json', timeoutMs: config.ollamaTimeoutMs })`.
  9. Try `JSON.parse(raw)` — on throw → `OllamaParseError(raw)`. Try `classificationSchema.parse(parsed)` — on throw → `OllamaParseError(raw)`. (Wrap both in one try/catch.)
  10. Return `{ ...validated, model_used: config.ollamaModel, artifacts }`.
  11. Network/timeout failures from `chat`: catch and re-throw as `OllamaUnreachableError`.

  Test file `src/server/classify/index.test.ts` with the eight cases from the research doc. Stub: `createGmailClient` returns a `GmailClient` whose methods return canned `Schema$Message` and attachment buffers; `chat` is a `vi.fn()` returning a JSON string; `renderPdfToImages` is a `vi.fn()` returning a known buffer array. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/classify/index.test.ts` passes (8 cases).

  _Done: 8 new tests in `index.test.ts` covering the eight cases (text/plain happy path, HTML body artifact, PDF attachment fetched + rendered + listed, image attachment passed through without rendering, too-large attachment skipped, OllamaUnreachableError propagates, malformed JSON → OllamaParseError with rawResponse, Zod-fail → OllamaParseError with rawResponse). Pipeline: getMessage(format='full') → headers via `extractHeader` → body via `extractBodyText` → attachments via `extractAttachmentMetadata` → for each receipt-eligible attachment under MAX_ATTACHMENT_BYTES (5 MB), `getAttachment` then either base64-encode raw image bytes (mime starts with `image/`) or `renderPdfToImages` for PDFs (cap MAX_PDF_PAGES=5) → `buildClassificationMessages` → `chat` → JSON.parse → Zod safeParse → return. **OllamaParseError** added to `ollama.ts` (alongside OllamaUnreachableError + OllamaHttpError) with a `rawResponse` field — orchestrator constructs it with the literal text the model returned so the API layer can echo it in the 502 body for prompt-debugging. The decision to live in `ollama.ts` (not `errors.ts`) reverses the step-12 done log's tentative reasoning: the orchestrator already imports from `ollama.ts` for the `chat` function, so adding one more class doesn't grow the import. Same `vi.fn<ChatFn>(...)` typing pattern from step 12 needed here for `chat.mock.calls[0]?.[0]?.messages?.[1]`. The `OllamaMessage` discriminated union (`{ role: 'system'|'user'|'assistant', content, images? }`) requires narrowing before reading `.images` — added an `if (msg.role !== 'user') throw` guard in two test assertions. Full classify suite: 7 files / 68 tests / 0 failures in ~2.4s. Typecheck clean. The orchestrator is the spec's pipeline orchestrator that the API layer (next step) calls into._

- [x] **Step 14: `src/server/api/classify.ts` + wire into `src/server/app.ts`.** New file. `registerClassifyRoutes(app, deps?)`. Use `@hono/zod-validator`'s `zValidator('param', paramsSchema)` for path-param validation. Handler:
  1. `requireConnectedAccount(id)` precondition (from step 5).
  2. `classifyMessage({ account_id: id, message_id }, deps)`.
  3. Map errors to HTTP per the research doc's error table.

  Test file `src/server/api/classify.test.ts` following the `messages.test.ts` shape: `mkdtempSync` + `setDbPathForTest` + `migrate` + helpers from steps 4-5; cases listed in the research doc. **Wire `registerClassifyRoutes(app)` into `createApp()`** in `src/server/app.ts` (after `registerProcessedMessagesRoutes`, before the static fallback). Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/api/classify.test.ts` passes (≥ 11 cases including the no-DB-write check); `npx vitest run src/server/app.test.ts` still passes (the new route is more specific than the static catch-all).

  _Done: 12 new tests in `classify.test.ts` covering 400 on non-numeric :id, 400 on URL-encoded space in :message_id, 404 on unknown account, 409 on needs_reauth, 200 happy path with verdict body, **no-DB-write contract** (countForAccount before == after), 503 on OllamaUnreachableError, 502 on OllamaParseError with `raw_response`, 502 on OllamaHttpError with `status`+`body`, 401 on `invalid_grant` from a generic Error, 502 on a generic Gmail error. The empty-message-id case is handled at the URL-routing layer (Hono returns 404 for `/messages//classify` because the path doesn't match — accepted via `expect([400, 404]).toContain(...)`). **ESM-identity gotcha:** `vi.resetModules()` reloads `'../classify/ollama.js'` from inside the handler's transitive imports, giving the orchestrator's `OllamaUnreachableError` a different class identity than the test's top-level import. Result: `err instanceof OllamaUnreachableError` was false at the handler, mapping the error to the catch-all `gmail_error` 502. Fix: re-import the error classes after `vi.resetModules()` in `beforeEach` (`ollamaErrors = await import('../classify/ollama.js')`) and `throw new ollamaErrors.OllamaUnreachableError(...)` in the test bodies. Documented inline as a comment for future contributors who hit the same trap. The handler uses `@hono/zod-validator`'s `zValidator('param', schema, hook)` with a custom hook that returns 400 + `{error: 'invalid_params'}` instead of Zod's default error envelope (matches the rest of the API's error-shape convention). Wire into `createApp()` after `registerProcessedMessagesRoutes` (route ordering doesn't actually matter for non-overlapping prefixes, but matches the chain order from the plan). Same `vi.fn<ClassifyFn>(...)` typing pattern needed for `mock.calls[0]?.[0]` access. Full suite: 40 files / 360 tests / 0 failures in 15.75s. Typecheck clean._

- [x] **Step 15: `src/server/api/ollama.ts` + wire into `src/server/app.ts`.** New file. `registerOllamaRoutes(app, deps?)` with deps `listOllamaModels?`. Single GET endpoint. Returns 200 always with `{ reachable, model, model_available, error? }`. Test file `src/server/api/ollama.test.ts` with the four cases from the research doc, including the timeout-bound check. Wire into `createApp()`. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/api/ollama.test.ts` passes; `time curl -s http://localhost:3737/api/ollama/health` returns within ~5 s (smoke-time check, not a test). The endpoint must use a tighter timeout (5 s) than `OLLAMA_TIMEOUT_MS` for the health probe — pass `timeoutMs: 5000` explicitly to `listModels`.

  _Done: 6 new tests in `ollama.test.ts` covering reachable+model-present (200, model_available=true), reachable+model-absent (200, model_available=false), unreachable (200, reachable=false, error: 'unreachable: ...'), HTTP error from Ollama (200, reachable=true, model_available=false, error: 'http_500'), 5s timeout passed to listModels (`HEALTH_TIMEOUT_MS = 5000` constant in the route, separate from `config.ollamaTimeoutMs`), and "always 200" structural test. Implementation: single GET handler that wraps `listModels` in a try/catch with three branches (success, OllamaUnreachableError, OllamaHttpError) plus an unknown-error catch-all that surfaces as `unreachable`. The endpoint always returns 200 — the `reachable` field carries the failure signal; the badge component reads the body, not the status. Wired into `createApp()` as the seventh route registrar (after `registerClassifyRoutes`); ordering is incidental since none of the routes overlap on path. Same `vi.fn<ListModelsFn>(...)` typing pattern needed for argument inspection (`listModels.mock.calls[0]?.[0]?.timeoutMs`). Full suite: 41 files / 366 tests / 0 failures in 16.37s. Typecheck clean._

- [x] **Step 16: Add `ClassificationResult` and `OllamaHealth` types to `src/client/types.ts`.** Two new exports matching the API responses verbatim. No test (types only). Verification: `npm run typecheck` passes; the new types are imported by steps 17 / 18.

  _Done: added `ClassificationVerdict`, `ClassificationConfidence`, `ClassificationArtifact`, `ClassificationResult`, and `OllamaHealth` to `src/client/types.ts`. The verdict/confidence enums are duplicated rather than imported from the server (the client never imports server modules — vitest's `client` workspace runs in jsdom and the server modules pull in `better-sqlite3`'s native binary, which won't load in jsdom). Mild duplication is the right tradeoff; if the spec values ever drift, both files are updated together as a code change. The `ClassificationResult` shape mirrors `classifyResponseSchema`; `OllamaHealth` mirrors the GET /api/ollama/health body. No tests — types-only changes are caught by `npm run typecheck`, which exits 0._

- [x] **Step 17: `src/client/components/OllamaHealth.tsx` + tests.** New file. Component-local state: `{ status: 'loading' | OllamaHealth }`. On mount: fetch `/api/ollama/health`. Set `setInterval` for 30 s polling; clean up on unmount. Render one of three strings (green / yellow / red, plus a CSS class — keep styling minimal, matching DevSeedPanel's plain-HTML choice from Slice 004). Test file `src/client/components/OllamaHealth.test.tsx` with the cases from the research doc, including `vi.useFakeTimers` for the polling check. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/components/OllamaHealth.test.tsx` passes (≥ 5 cases).

  _Done: 6 new tests covering loading state, ready/green pill ("Ollama: qwen2.5vl:7b ready"), yellow model-not-pulled with the `ollama pull` hint, red unreachable + error string, 30 s polling via `vi.useFakeTimers()` + `vi.advanceTimersByTime(30_000)` (verified two extra fetches at the 30 s and 60 s marks), and a fetch-rejection→unreachable fallthrough. Implementation: `setInterval(probe, 30_000)` cleaned up in the `useEffect` return; cancelled flag on unmount to avoid a state-update warning. Pill exposes `data-state="ready" | "model_missing" | "unreachable" | "loading"` via `data-testid="ollama-health-pill"` so tests can assert state without coupling to copy. The "always 200" property of the health route means the only `fetch_error` branch is for non-2xx (defensive — currently unreachable in practice but cheaper than letting the UI silently spin if the route ever changes). Full suite: 42 files / 372 tests / 0 failures in 18.38s. Typecheck clean._

- [x] **Step 18: `src/client/components/ClassifyRowAction.tsx` + tests.** New file. Props: `{ account_id: number; message_id: string }`. Component-local state: `{ kind: 'idle' | 'pending' | 'success' | 'unreachable' | 'parse_error' | 'http_error' | 'needs_reauth' | 'unexpected'; ... }`. Click handler POSTs `/api/accounts/:id/messages/:message_id/classify` (no body) and branches on status. On success render verdict pill + confidence label + reason text. On error render the appropriate chip + Retry. On 502 with `ollama_parse_error` body, render the chip and surface `raw_response` in a `<details>` block. Test file `src/client/components/ClassifyRowAction.test.tsx` with the cases from the research doc. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/components/ClassifyRowAction.test.tsx` passes (≥ 6 cases).

  _Done: 8 tests covering initial Classify button, happy path POST + verdict render, in-flight pending state, 503 → unreachable + Retry chip, 502 ollama_parse_error → parse-error chip with raw_response inside `<details>`, 401 → needs-reauth chip, Retry click re-issues POST, generic non-2xx → generic error chip. Component-local state machine: `idle | pending | success | unreachable | parse_error | http_error | needs_reauth | error`. Verdict pill uses `data-testid="classify-result" data-verdict="..."` (which let me dodge the "Stripe-shaped receipt" reason text matching the same `/receipt/i` regex as the verdict — first attempt at the test asserted on the verdict text directly and got two matches; switched to `getByTestId` + `data-verdict` attribute). Error chips use `data-testid="classify-error" data-error="..."` for the same reason. The 502-with-error-discrimination branch covers `ollama_parse_error`, `ollama_http_error`, and `gmail_error` with separate body shapes; defensive type-narrowing through `Record<string, unknown>` since the response type isn't tightly bound at the fetch boundary. Per the spec this slice persists nothing — when the user navigates away, the rows unmount and their state vaporizes. Full suite: 43 files / 380 tests / 0 failures in 16.43s. Typecheck clean._

- [x] **Step 19: Wire `<OllamaHealth />` into `src/client/views/Dashboard.tsx` + extend Dashboard test.** Add the badge between the existing children (above the account list — closest match to the spec's "in the Dashboard header"). Extend `src/client/views/Dashboard.test.tsx` to mock `/api/ollama/health` returning the ready state and assert the pill text appears in the rendered DOM. The existing tests (loading / empty / error / DevSeedPanel) continue to pass — the new mock is added to the URL-routed `mockImplementation` fall-through map. Run vitest — **red** (the new assertion fails because `<OllamaHealth />` isn't mounted yet). Mount it. Re-run **green**. Verification: `npx vitest run src/client/views/Dashboard.test.tsx` passes; existing test count + 1.

  _Done: 1 new test asserting the badge renders with `data-state="ready"` and the "Ollama: qwen2.5vl:7b ready" copy. Plus a small ripple in the existing tests: the `OllamaHealth` component fetches `/api/ollama/health` on Dashboard mount, so all five existing test cases needed the mock to handle that URL. Threaded the mock through the `beforeEach` fall-through (default to "ready"), the URL-routed `mockImplementation` in the Reconnect / polling / append-account / dev-tools tests (each got a one-line `if (url === '/api/ollama/health') return ready` clause), and the new test itself. Component placement in Dashboard: `<OllamaHealth />` rendered between the alert paragraph and the AccountList — closest match to the spec's "in the Dashboard header" without restructuring the view's flat layout. Full suite: 43 files / 381 tests / 0 failures in 16.91s. Typecheck clean._

- [x] **Step 20: Add Classify column to `src/client/views/Inbox.tsx` + extend Inbox test.** Add a fourth `<th>Classify</th>` and a fourth `<td>` containing `<ClassifyRowAction account_id={selectedAccountId!} message_id={m.id} />` per row. Update `src/client/views/Inbox.test.tsx`: assert each row contains a button with text "Classify". Existing assertions stay green. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/views/Inbox.test.tsx` passes; the Inbox view typechecks.

  _Done: 1 new test asserting two Classify buttons (one per rendered row in the two-message fixture). Implementation: added a fourth `<th>Classify</th>` and matching `<td>` with `<ClassifyRowAction account_id={selectedAccountId} message_id={m.id} />`. The existing render gate also got a `selectedAccountId !== null` guard so the typechecker is happy without the non-null assertion the plan suggested — `selectedAccountId` is typed `number | null` and would otherwise need `!`. Functionally equivalent (the Inbox already has a `hasConnected` guard upstream), but cleaner. The other 9 existing Inbox tests stay green; none of them clicks the Classify button so no `/api/classify` mock is needed in those tests. Per the spec, when the user navigates away from Inbox the rows unmount and per-row Classify state is cleared (no persistence this slice). Full suite: 43 files / 382 tests / 0 failures in 20.29s. Typecheck clean._

## Smoke test recipe

The exact sequence the loop will run after all plan steps are checked. The smoke recipe is the Slice 005 spec's acceptance criteria #1-#9 exercised in order, with the AC numbers cited inline.

1. **Setup.** From the devcontainer: `git status` clean. `npm install` (pulls the five new deps). `npm run check:gmail-readonly` exits 0. `npm run typecheck` exits 0. `npx vitest run` — full suite green (count noted in `## Test run`).
2. **Ollama reachability.** Confirm Ollama runs on the host with `qwen2.5vl:7b` pulled (`ollama list` on host shows it). From the devcontainer: `curl -s ${OLLAMA_URL:-http://host.docker.internal:11434}/api/tags` returns JSON with the model name. If `host.docker.internal` doesn't resolve (Linux without Docker Desktop; the devcontainer's `runArgs` lacks `--add-host`), set `OLLAMA_URL=http://172.17.0.1:11434` in `.env` and restart the dev process.
3. **Boot.** `npm run dev`. Open `http://localhost:5173/` in the host browser. Dashboard loads.
4. **AC #1 — health badge ready.** Dashboard shows a green badge: "Ollama: qwen2.5vl:7b ready".
5. **AC #1 — health badge unreachable.** Stop Ollama on the host (`ollama stop` or quit the app). Within ~30 s the badge flips to red ("Ollama unreachable at http://host.docker.internal:11434" or whatever URL is configured). Other UI continues to function. `time curl -s http://localhost:3737/api/ollama/health` returns within ~5 s (AC #8). Restart Ollama.
6. **AC #1 — health badge model not pulled.** With Ollama running but `qwen2.5vl:7b` *removed* from the host (`ollama rm qwen2.5vl:7b` — only do this if you can re-pull), refresh the Dashboard. Badge flips to yellow with the `ollama pull qwen2.5vl:7b` instruction. Re-pull and refresh: badge returns to green.
7. **AC #2 — classify a real receipt.** Navigate to `/inbox`, pick a connected account, find a vendor receipt row (Stripe, AWS, etc.). Click Classify. Spinner spins ~5-30 s. Row inline-shows `receipt` (or `invoice` for invoice-shaped messages) with `high` or `medium` confidence and a short reason.
8. **AC #3 — classify a non-receipt.** Click Classify on a personal email / newsletter / meeting notice. Returns `other`.
9. **AC #4 — PDF attachment.** Click Classify on a row whose email has a PDF attachment. Inspect the API response (DevTools Network panel or the chip's expanded reason): `artifacts` includes `{ kind: 'attachment', mime_type: 'application/pdf', filename: '...' }`.
10. **AC #4 — HTML body only.** Click Classify on a row whose email has only an HTML body. `artifacts` includes `{ kind: 'body', mime_type: 'text/html' }`.
11. **AC #5 — Ollama unreachable mid-classify.** Stop Ollama. Click Classify on any row. Within ~120 s (or sooner if AbortController fires) the chip shows `ollama_unreachable` with a Retry button. Other UI continues to function. Restart Ollama; click Retry; classification succeeds.
12. **AC #6 — parse error.** *(Optional smoke step — pinned at the unit-test layer.)* If a debugging proxy is available, route `OLLAMA_URL` through it returning malformed JSON. Click Classify. Chip shows `ollama_parse_error` with the raw response in a `<details>` block.
13. **AC #7 — no DB writes.** Before clicking Classify: `sqlite3 data/app.db "SELECT COUNT(*) FROM processed_messages;"` returns N. After 5 successful clicks: still N.
14. **AC #8 — health endpoint timeout.** With Ollama running: `time curl -s http://localhost:3737/api/ollama/health` returns within ~1 s. With Ollama stopped (re-do step 5 setup): same `time curl` returns within ~5 s. (The Ollama-health route uses a 5-second timeout; the long 120-second `OLLAMA_TIMEOUT_MS` only applies to the per-message classify endpoint.)
15. **AC #9 — read-only Gmail check still passes.** `npm run check:gmail-readonly` exits 0. Already verified in step 1; re-confirm after all clicks. `grep -r 'gmail\.' src/ | grep -v '\.test\.'` shows only `gmail.readonly` (no new scope substring; confirms the spec's "no Gmail OAuth scope other than `gmail.readonly`" criterion).
16. **`git status` clean.** All work tracked; no stray files. The next loop iteration (priority 7) will commit.

## Test run

- **Date:** 2026-05-10
- **Command:** `npx vitest run`
- **Result:** **43 test files / 382 tests / 0 failures** in 23.03 s.
- **Of those, this slice authored 122 new tests:**
  - `config.test.ts` +7 (3 fields × default + override + non-numeric fallback for the timeout)
  - `auth/invalid-grant.test.ts` +9 (new file from the extract refactor)
  - `auth/preconditions.test.ts` +4 (new file from the extract refactor)
  - `gmail/client.test.ts` +6 (new `getAttachment` describe block)
  - `classify/schema.test.ts` +17 (new file)
  - `classify/extract-body.test.ts` +9 (new file)
  - `classify/extract-attachments.test.ts` +9 (new file)
  - `classify/render-pdf.test.ts` +4 (new file, exercises the `__fixtures__/sample.pdf` fixture)
  - `classify/prompt.test.ts` +8 (new file)
  - `classify/ollama.test.ts` +13 (new file)
  - `classify/index.test.ts` +8 (new file — the orchestrator)
  - `api/classify.test.ts` +12 (new file)
  - `api/ollama.test.ts` +6 (new file)
  - `client/components/OllamaHealth.test.tsx` +6 (new file)
  - `client/components/ClassifyRowAction.test.tsx` +8 (new file)
  - `client/views/Dashboard.test.tsx` +1 (Ollama badge renders; the four existing URL-routed `mockImplementation` tests gained a one-line `/api/ollama/health` clause but the assertion count stayed the same)
  - `client/views/Inbox.test.tsx` +1 (Classify buttons render, one per row)

- **Pre-flight gates:**
  - `npm run check:gmail-readonly` — OK (no forbidden Gmail-write substrings; the new `attachments.get` substring is not on the forbidden list per ADR-003).
  - `npm run typecheck` — exit 0 (clean).

- **Notes:** No `*.skip` / `*.only` markers anywhere; no flakes observed across two consecutive runs (full suite is deterministic under `pool: 'forks'`). The classify suite alone runs in <2 s; the slow leg of the full run is the server-side cold-start cost per fork (`vitest.workspace.ts`'s `pool: 'forks'` from Slice 004 is unchanged).

## Smoke run

- **Date:** 2026-05-10
- **Mode:** server-side via the production binary (`APP_PORT=3738 NODE_ENV=production node dist/server/index.js`), against the user's existing `data/app.db`. Browser-driven AC #2-#6 (clicking Classify on a real Gmail row, watching the verdict appear, exercising the parse-error chip via a debugging proxy) are deferred to human acceptance — pinned at the unit-test layer in `client/components/ClassifyRowAction.test.tsx` and `api/classify.test.ts`.

**Build-time gate (smoke step 1)**
- `npm run build` — exits 0 after fixing one issue: the server tsconfig's `lib` was `["ES2022"]`, which made `tsc -p tsconfig.server.json` fail to resolve `CanvasRenderingContext2D` referenced from `src/server/classify/render-pdf.ts`'s pdfjs-dist cast. Fix: added `"DOM"` to the server tsconfig's `lib` with an inline comment naming Slice 005's render-pdf cast as the reason. Compile-time only — Node still has no `window`/`document` at runtime. Vitest didn't catch this because vitest uses its own resolver / lib defaults; `npm run typecheck` (which uses the *root* tsconfig with `DOM` already in lib) also let it through. Future contributors get a clean signal from `npm run build` going forward. **Flagging this for the review** as a worth-knowing gotcha when adding browser-shaped libraries to server code.
- `npm run check:gmail-readonly` — OK.

**Endpoint shape (smoke step 4)**
- `GET /health` → `200 ok`.
- `GET /api/accounts` → 200 with the user's two real `accounts` rows preserved across the build / boot.
- `GET /api/dev/enabled` → **404** in production (Slice 004 contract preserved through the new wiring).
- `GET /api/ollama/health` → 200 `{ reachable: false, model: 'qwen2.5vl:7b', model_available: false, error: 'unreachable: Ollama request failed: fetch failed' }` because Ollama is not available inside the agent's network. **AC #8 verified end-to-end:** the call returned in 0.04 s — well inside the 5 s budget; the route's separate `HEALTH_TIMEOUT_MS = 5000` constant kept the connection short even though `OLLAMA_TIMEOUT_MS` is 120000. `time curl …` measurement.

**Path-param validation (Step 14 contract)**
- `POST /api/accounts/abc/messages/m1/classify` → `400 {"error":"invalid_params"}` (Zod regex on `:id`).
- `POST /api/accounts/1/messages/has%20space/classify` → `400 {"error":"invalid_params"}` (Zod regex on `:message_id`).
- `POST /api/accounts/9999/messages/m1/classify` → `404 {"error":"account_not_found"}` (precondition).

**No-DB-write contract (AC #6)**
- Pre-classify row count: 10 (across one account, from Slice 004's dev-seed).
- Issued five POSTs to `/api/accounts/1/messages/{m1..m5}/classify`. Each returned `409 {"error":"account_not_connected","status":"needs_reauth"}` because the production restart wiped the in-memory OAuth-token map (Slice 002 design — tokens never persist; the precondition flips the row to `needs_reauth` and short-circuits before any pipeline work happens). **The path that would have hit Ollama / Gmail is precondition-gated by `requireConnectedAccount`.**
- Post-classify row count: **10** (unchanged). Verified by direct read against `data/app.db` via better-sqlite3.

**Read-only Gmail discipline (AC #9)**
- `npm run check:gmail-readonly` — OK after the slice's edits to `gmail/client.ts` (the new `attachments.get` substring is allowed).
- `grep -E 'googleapis.com/auth/[a-z.]+' src/` returns exactly two matches: `userinfo.email` and `gmail.readonly`. Plus a separate match for the `openid` scope literal. **No other Gmail OAuth scope appears in the codebase**, matching `architecture.md` § "Read-only Gmail access".
- `grep -E '(messages|labels|drafts|threads).(modify|trash|delete|send|insert|import|create|update|patch)' src/` returns **no** matches in non-test files.

**Browser-driven ACs (deferred to human acceptance)**
- **AC #1**: Dashboard's Ollama health badge transitions through ready → unreachable → model-not-pulled as the host-side Ollama state changes. Pinned at the unit layer in `OllamaHealth.test.tsx` (6 cases) plus the Dashboard wiring assertion. Requires a real browser + a host with Ollama installed to verify the visual states; the agent has no Ollama in this network and no browser.
- **AC #2-#5**: per-row Classify clicks against real Gmail messages → real receipts return `receipt`/`invoice`; non-receipts return `other`; PDF and HTML-body receipts populate `artifacts` correctly. Pinned at `api/classify.test.ts` (12 cases) and `classify/index.test.ts` (orchestrator, 8 cases). Requires real Gmail tokens, a connected Ollama, and a real receipt corpus to drive end-to-end.
- **AC #6 parse-error chip**: requires a debugging proxy returning malformed JSON; pinned at the unit layer (`ClassifyRowAction.test.tsx` "renders an ollama_parse_error chip and surfaces the raw_response inside `<details>`").

**`git status` end-state**
- 19 modified files (existing source + manifest + tsconfig.server.json), 23 new files (research, plan, three ADRs, the entire `src/server/classify/` module tree, two new client components, two new API routes, two new auth helper modules, all corresponding tests, and the `__fixtures__/sample.pdf` + `generate-sample.ts`). No stray / untracked files outside the slice's deliverables.
