# Slice 002: Connect Gmail accounts — Plan

**Spec:** `docs/specs/002-connect-gmail-accounts.md`
**Research:** `docs/research/002-connect-gmail-accounts.md`

## Steps

Each step is small enough to fit in one loop iteration. Each step ends with a concrete check — a named test passing, or a specific command's output. Per-step refactor is folded into the priority-3 action. ADRs ship in the same iteration as the step that triggered them.

- [x] **Step 1: Install runtime + dev deps.** Add to `package.json`:
  - runtime: `better-sqlite3`, `googleapis`, `google-auth-library`
  - dev: `@types/better-sqlite3`, `@testing-library/react`, `@testing-library/dom`, `@testing-library/user-event`, `jsdom`
  Run `npm install`. Verification: exit 0; `node_modules/better-sqlite3/package.json`, `node_modules/googleapis/package.json`, `node_modules/google-auth-library/package.json`, `node_modules/jsdom/package.json` all exist; `npx vitest run` still 6/6 passing (Slice 001's tests untouched). — _Done: resolved versions: `better-sqlite3@12.9.0`, `googleapis@171.4.0`, `google-auth-library@10.6.2`, `jsdom@29.1.1`, `@testing-library/react@16.3.2`, `@testing-library/dom@10.4.1`, `@testing-library/user-event@14.6.1`, `@types/better-sqlite3@7.6.13`. `prebuild-install` warned as deprecated (transitive of `better-sqlite3`); the prebuilt binary still resolved cleanly on darwin/arm64 — no native compile required. `vitest run` → 2 files, 6 tests, all passing._

- [x] **Step 2: Vitest jsdom project + smoke.** Restructure `vitest.config.ts` to define two projects: `server` (`environment: 'node'`, includes `src/server/**/*.test.ts`) and `client` (`environment: 'jsdom'`, includes `src/client/**/*.test.{ts,tsx}` and `src/server/**/*.client.test.tsx` if any). Add `src/client/jsdom-env.test.tsx` that mounts a `<div>` via `document.createElement` and asserts `document.body.contains(...)` to prove jsdom is wired. Verification: `npx vitest run` runs both projects, server's 6 still pass, client's smoke test passes (1 test). Total: 7 passing. — _Done: 7 tests passing across two projects (`|server|` 6, `|client|` 1). Two small deviations from the plan text. (a) Splitting projects into a `vitest.workspace.ts` rather than packing them into `vitest.config.ts`'s `test.projects` — vitest 2.1 supports both, and the workspace-file split keeps the root config (still `vitest.config.ts`) lean for any cross-project shared settings. (b) Used a **plain array export** (no `defineWorkspace` helper) because vitest 2.1 ships with vite@5 internally while the project's `@vitejs/plugin-react` is built against vite@6 (`npm ls vite` confirms the duplication); the helper's nominal types reject the plugin assignment even though the runtime is fine. ADR-001 already anticipated the vitest 3 chain update; the comment in `vitest.workspace.ts` flags this. `tsconfig.json` extended to include `vitest.workspace.ts`._

- [x] **Step 3: Config additions (red→green).** Extend `src/server/config.test.ts` with cases for `googleClientId`, `googleClientSecret`, `oauthRedirectPort` (defaults to `port`), and `dbPath` (defaults to `./data/app.db`). Tests use `vi.resetModules()` per existing pattern. The OAuth secrets are read eagerly at module load — empty string if unset. Validation that they're non-empty is **deferred to the OAuth route handlers** (so the server can boot without `.env` for tests / dev that doesn't touch OAuth). Document this choice in the config file via a one-line comment on the field. Run vitest — **red**. Implement the additions in `src/server/config.ts`. Re-run **green**. Verification: `npx vitest run src/server/config.test.ts` passes, all cases. — _Done: 11 tests in `config.test.ts` (3 prior + 8 new), all passing. Beforeach now snapshots all four ENV_KEYS (`APP_PORT`, `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `OAUTH_REDIRECT_PORT`) and clears them; afterEach restores. `dbPath` is a fixed constant (`./data/app.db`) — no env override at config level; test overrides will live at the DB layer (`setDbPathForTest` in Step 5) so tests can swap in temp files without re-importing config. Implementation comment on the OAuth secrets explains the empty-string-when-unset stance and points at the route handlers as the validation site._

- [x] **Step 4: Slug derivation.** Write `src/server/auth/slug.test.ts` covering the algorithm from research: `slugify('alice@example.com') === 'alice-at-example-com'`; uppercase normalization (`Alice@Example.COM` → `alice-at-example-com`); plus-aliases (`bob+work@example.com` → `bob-work-at-example-com`); underscores (`a_b@x.com` → `a-b-at-x-com`); trims leading/trailing hyphens; collapses runs of `-` to a single `-`. Run vitest — **red**. Implement `src/server/auth/slug.ts` with a `slugify(email: string): string` pure function (no DB access — uniqueness is the repo's job). Re-run **green**. Verification: `npx vitest run src/server/auth/slug.test.ts` passes. — _Done: 8 tests passing (added two beyond the plan's six — `preserves digits` for `user42@host9.io` and `strips characters outside [a-z0-9-]` for a punctuation-heavy fixture, both common-enough variants to lock in). Implementation is a five-step regex chain matching the research's algorithm verbatim: lowercase → `@`→`-at-` → `[^a-z0-9-]`→`-` → collapse runs → trim. Pure function, no DB dependency — repo-layer uniqueness collision suffix lands in Step 8._

- [x] **Step 5: DB connection singleton.** Write `src/server/db/index.test.ts` covering: `getDb()` returns the same instance on repeated calls; `setDbPathForTest(tempPath)` (test-only helper) before the first `getDb()` opens at the override path; tests clean up the temp file. Run vitest — **red**. Implement `src/server/db/index.ts`: imports `Database from 'better-sqlite3'`; module-scoped `let _db: Database.Database | undefined; let _path: string | undefined`; `getDb()` opens at `_path ?? config.dbPath` on first call and caches. `setDbPathForTest(p)` sets `_path` and clears `_db` (calling `_db?.close()` if open). Export `Account` type? No — that lives next to the repository. Re-run **green**. Verification: vitest passes. — _Done: 3 tests passing — singleton identity, override-path read/write round-trip, replace-and-close behavior. Each test runs in its own `mkdtempSync` directory (clean slate per test) and uses `vi.resetModules()` so module-scoped `_db`/`_path` start fresh. The third test confirms calling `setDbPathForTest` again closes the previous handle (`first.exec(...)` throws on the closed connection)._

- [x] **Step 6: Migration runner + ADR-002.** Write `src/server/db/migrate.test.ts` covering: given a temp DB and a fixture migrations dir with two `*.sql` files (`0001_first.sql` creates table `t1`, `0002_second.sql` creates table `t2`), `migrate(db, migrationsDir)` applies both and `_migrations` ends with two rows ordered by filename; running migrate again is a no-op (still two rows); manually inserting `0001_first.sql` into `_migrations` before running causes only `0002_second.sql` to apply; if `0002_second.sql` errors (e.g. references unknown column), `_migrations` ends with only `0001_first.sql` (transaction rollback verified) and `t1` exists but `t2` does not. Run vitest — **red**. Implement `src/server/db/migrate.ts`: `export function migrate(db: Database.Database, migrationsDir: string): void`. Steps inside: ensure `_migrations(filename TEXT PRIMARY KEY, applied_at TEXT NOT NULL)` exists; `readdirSync(migrationsDir).filter(f => f.endsWith('.sql')).sort()`; query applied filenames; for each unapplied, run `db.transaction(() => { db.exec(sqlContent); db.prepare('INSERT INTO _migrations(filename, applied_at) VALUES (?, ?)').run(filename, new Date().toISOString()) })()`. Re-run **green**. Write `docs/adr/002-bare-sql-migration-runner.md` (status Accepted) capturing: pure-SQL files in lexical order, single `_migrations` table, transactional, no down-migrations or checksums. Alternatives rejected: Drizzle Kit, kysely-migrator, db-migrate, knex. Verification: `npx vitest run src/server/db/migrate.test.ts` passes; `ls docs/adr/002-bare-sql-migration-runner.md` succeeds with non-empty content. — _Done: 4 tests passing — apply-in-lexical-order, idempotent second run, skip-already-applied, rollback-on-error. Rollback fixture is `0002_second.sql` doing `CREATE TABLE t2 ...; INSERT INTO t2 (id, missing_col) VALUES (1, 2);` — the second statement throws, the transaction wrapper rolls back the CREATE; assertion confirms `_migrations` only has `0001` and `t2` doesn't exist. ADR-002 written, references `architecture.md` § "Tech stack" and § "Data retention and backup", rejects four alternatives (Drizzle Kit, kysely-migrator, db-migrate, knex-migrate) plus a "no runner" non-option. ADR also documents the four-digit zero-padded filename convention which Step 7's `0001_create_accounts.sql` adopts._

- [x] **Step 7: First migration `0001_create_accounts.sql` + build copies migrations.** Write `src/server/db/migrations/0001_create_accounts.sql` matching the spec verbatim: `CREATE TABLE accounts ( id INTEGER PRIMARY KEY, email TEXT NOT NULL UNIQUE, display_name TEXT, slug TEXT NOT NULL UNIQUE, connected_at TEXT NOT NULL, last_seen_at TEXT, status TEXT NOT NULL CHECK (status IN ('connected','needs_reauth')) );`. Add a vitest case in `src/server/db/migrate.test.ts` (or a new `src/server/db/migrations.test.ts`) that runs `migrate(db, path.resolve(import.meta.dirname, 'migrations'))` against a fresh temp DB and asserts via `pragma table_info(accounts)` that the columns exist with the expected types and constraints. Update `package.json`'s `build` script to additionally copy migrations into the emit: `"build": "vite build && tsc -p tsconfig.server.json && node -e \"require('node:fs').cpSync('src/server/db/migrations','dist/server/db/migrations',{recursive:true})\""`. Verification: vitest passes; `npm run build` exits 0 and `dist/server/db/migrations/0001_create_accounts.sql` exists. — _Done: 4 tests in `migrations.test.ts` — column shape via `PRAGMA table_info`, plus three behavior tests (UNIQUE on email, UNIQUE on slug, CHECK on status). Migration uses `display_name TEXT NULL` and `last_seen_at TEXT NULL` (explicit NULL though SQLite treats those equivalently to plain TEXT). Build script extended; `npm run build` produces `dist/server/db/migrations/0001_create_accounts.sql` and the test assertions on `pragma` columns confirmed shape verbatim. The runner test (`migrate.test.ts`) and the migration test (`migrations.test.ts`) live as separate files — keeps the runner mechanics tests focused, with per-migration shape tests living next to the migration directory._

- [x] **Step 8: Accounts repository.** Write `src/server/auth/accounts.test.ts` covering each function on a temp DB (after `migrate()` is run): `insert({ email, display_name: null, connected_at: '2026-05-09T...' })` derives slug via `slugify`, returns `{ id, slug }`, status defaults to `'connected'`; `insert` of a second email whose slug collides appends `-2` (e.g. `bob+work@example.com` and `bob.work@example.com` both reduce to `bob-work-at-example-com`, second one becomes `...-2`); `findByEmail`, `findById` round-trip rows; `updateStatus(id, 'needs_reauth')` flips the column; `touchLastSeen(id, iso)` writes to `last_seen_at`; `list()` returns rows ordered by `id ASC`. Run vitest — **red**. Implement `src/server/auth/accounts.ts` with `Account` type (matching column names verbatim) and the six functions. Use prepared statements cached via `WeakMap<Database, Statement>`. Slug-collision loop: probe with `findBySlug`-equivalent, increment suffix `-N` until unique. Re-run **green**. Verification: `npx vitest run src/server/auth/accounts.test.ts` passes. — _Done: 8 tests passing. The collision test exercises three e-mails reducing to `bob-work-at-example-com` (`bob+work@`, `bob.work@`, `bob_work@`) — produces `…`, `…-2`, `…-3` confirming the suffix loop. Two extra `findByEmail`/`findById` undefined-on-miss tests (plan called for round-trip; added the negative case to lock in API shape). Implementation exports `AccountStatus` alongside `Account` so OAuth/session modules can import the literal-union without a duplicate definition. `findBySlug` exposed because the collision loop needs it; the spec's bullet didn't list it but didn't forbid it either — useful enough to keep public._

- [x] **Step 9: OAuth wrapper.** Write `src/server/auth/oauth.test.ts`. For `buildConsentUrl({ state })`: parse the returned URL, assert `searchParams.get('client_id') === config.googleClientId`, `redirect_uri === 'http://localhost:{oauthRedirectPort}/oauth/callback'`, `scope` (space-split) equals exactly `['openid', 'https://www.googleapis.com/auth/userinfo.email', 'https://www.googleapis.com/auth/gmail.readonly']`, `access_type === 'offline'`, `prompt === 'consent'`, `state` matches input. For `exchangeCode(code)`: stub the `OAuth2Client.getToken` via `vi.spyOn` on a constructed instance (or by injecting an `OAuth2Client` factory into the function — picked because it gives cleaner tests); the stub returns a fixture id_token whose payload encodes `{ email: 'alice@example.com' }` (base64url-encoded); assert the returned `email` is `'alice@example.com'` and `tokens` includes `access_token`, `refresh_token`, `expiry_date`. Run vitest — **red**. Implement `src/server/auth/oauth.ts`: export `SCOPES = ['openid', 'https://www.googleapis.com/auth/userinfo.email', 'https://www.googleapis.com/auth/gmail.readonly']`; `buildConsentUrl({ state })` constructs `new OAuth2Client(clientId, clientSecret, redirectUri).generateAuthUrl({ access_type: 'offline', prompt: 'consent', scope: SCOPES, state })`; `exchangeCode(code)` constructs a fresh `OAuth2Client`, calls `getToken(code)`, base64url-decodes the id_token's payload segment with `Buffer.from(payload, 'base64url').toString('utf8')`, returns `{ tokens, email }`. Inject the OAuth2Client constructor as an optional argument so tests can pass a fake. Re-run **green**. Verification: vitest passes. — _Done: 7 tests passing — 3 for `buildConsentUrl` (URL params, OAUTH_REDIRECT_PORT override, SCOPES content), 4 for `exchangeCode` (happy path, incomplete-token-set throw, missing-email-claim throw, malformed-jwt throw). Factory injection lets tests pass a hand-rolled stub matching `OAuth2ClientLike` (structural subset of `google-auth-library`'s `OAuth2Client`); the default factory wraps `new OAuth2Client(...)`. Also exposed `redirectUri()` (computes `http://localhost:{oauthRedirectPort}/oauth/callback` from config) so the OAuth API routes in Step 12-14 can reuse it. Two extra error-path tests beyond the plan's three (incomplete token set, malformed JWT) — locks in the validation contract since these are the only cases where the wrapper aborts vs. returning successfully._

- [x] **Step 10: Session module.** Write `src/server/auth/session.test.ts` covering: `set(accountId, { tokens, refreshToken })` registers a `tokens` event listener that updates the cached refresh_token when refresh response includes one and preserves the previous refresh_token when it doesn't (assert via firing the listener manually with two different payloads); `get(accountId)` returns the entry; `clear(accountId)` removes it. `withFreshTokens(accountId, callback)` calls the callback with the `OAuth2Client`; on `client.getAccessToken()` throwing an error whose message includes `'invalid_grant'`, calls `accounts.updateStatus(accountId, 'needs_reauth')`, calls `clear(accountId)`, and rethrows the error. Run vitest — **red**. Implement `src/server/auth/session.ts`: module-scoped `Map<number, { client: OAuth2Client, refreshToken: string }>`. `set` constructs `new OAuth2Client(...)`, `setCredentials(tokens)`, registers `client.on('tokens', (refreshed) => { ... })` to keep our shadow refreshToken in sync. `withFreshTokens` calls `client.getAccessToken()` (which auto-refreshes if needed) inside try/catch; on `invalid_grant`, the `accounts` repo is imported and `updateStatus` is called. Use the same `OAuth2Client` factory injection as Step 9 so tests can swap it. Re-run **green**. Verification: vitest passes. — _Done: 8 tests passing — `set` stores entry & registers listener, listener updates refreshToken on refresh, listener preserves refreshToken on omit, `clear` removes, `withFreshTokens` invokes callback, `withFreshTokens` flips status + clears on invalid_grant, `withFreshTokens` rethrows non-invalid_grant errors without flipping status, `withFreshTokens` throws on missing session. Factory injection deviation: instead of an optional per-call argument (Step 9's pattern), used a module-level `setSessionClientFactoryForTest`/`resetSessionClientFactoryForTest` setter — production callers don't need to pass a factory each time, and `vi.resetModules()` cleanly resets module state per test. Stub client exposes `_fireTokens`, `_getCreds`, `_hasListener` test-only escape hatches so tests can fire the registered `tokens` event manually. `isInvalidGrantError` checks both `err.message.includes('invalid_grant')` (the message-mode case) and `err.response?.data?.error === 'invalid_grant'` (the GoogleAuthError case)._

- [x] **Step 11: `GET /api/accounts` endpoint.** Write `src/server/api/accounts.test.ts`. Build a Hono app via the about-to-exist `registerAccountsRoutes(app)` factory; seed a temp DB with two accounts; `app.fetch(new Request('http://x/api/accounts'))` returns 200 JSON with `{ accounts: [{ id, email, display_name, slug, status, connected_at, last_seen_at }, ...] }`. Run vitest — **red**. Implement `src/server/api/accounts.ts` exporting `registerAccountsRoutes(app: Hono)` that calls `app.get('/api/accounts', c => c.json({ accounts: list() }))`. Re-run **green**. Verification: vitest passes. — _Done: 2 tests passing — empty list when no accounts, full shape with two seeded rows in id-asc order. The second row deliberately leaves `last_seen_at: null` while the first uses `touchLastSeen` to populate it; assertion confirms both shapes round-trip through SQLite + Hono's `c.json` serialization. Implementation is a 4-line registrar; the entire `Account` shape is forwarded directly because every column the spec lists is a direct field of `accounts.list()`._

- [x] **Step 12: `POST /api/oauth/start` endpoint + state map.** Write `src/server/api/oauth.test.ts`. The state map is module-scoped; tests reset it via an exported `__resetStateMapForTest()` helper. `app.fetch(new Request('http://x/api/oauth/start', { method: 'POST' }))` returns 200 with `{ consent_url: string, state: string }` where `state` is a UUID-shaped string and `consent_url` starts with `https://accounts.google.com/o/oauth2/v2/auth`. The state appears in the in-memory map with `{ kind: 'add', expiresAt: > Date.now() }`. Run vitest — **red**. Implement (in `src/server/api/oauth.ts`) a `registerOauthRoutes(app: Hono)` factory plus the state map (`Map<string, { kind, accountId?, expiresAt }>`) at module scope. Pruning: lazy on every read, plus opportunistic on insert when map.size > 100. Use `crypto.randomUUID()` for state. Re-run **green**. Verification: vitest passes. — _Done: 3 tests passing — full happy path (UUID-shaped state, consent URL prefix match, state map entry shape with TTL ~10 minutes), state encoded into consent URL `?state=…` query param, and "fresh state per call" (two POSTs produce distinct states; map size 2). Pruning currently fires only when `size > 100` on insert; the fully-lazy "prune on every read" pruning was scoped down because callback handlers in Step 13 will check expiresAt themselves before consuming a state, making prune-on-read superfluous for correctness._

- [x] **Step 13: `GET /oauth/callback` endpoint.** Add to `src/server/api/oauth.test.ts`: stub the OAuth code-exchange path (via the same factory injection from Step 9 — the test bootstrap installs a fake exchange that returns `{ tokens: {...}, email: 'alice@example.com' }`); seed the state map with an `add`-kind state; `app.fetch(new Request('http://x/oauth/callback?code=abc&state=THE_STATE'))` returns 302 with `Location: /`; the `accounts` table now has a row for `alice@example.com` with `status='connected'`; the session map has an entry under that row's `id`. Repeat the call (with a fresh state, same email) — assert no duplicate row, `last_seen_at` updated, `status` reset to `'connected'`. Bad state (not in map): returns 400 with `text/html` body containing `Couldn't connect`. Expired state (`expiresAt < Date.now()`): returns 400. Code-exchange error: returns 400 HTML with the error message. Run vitest — **red**. Implement the callback in `src/server/api/oauth.ts`. Re-run **green**. Verification: vitest passes. — _Done: 7 new callback tests (10 total in this file, including the 3 from Step 12) — happy path (302 to `/`, account inserted, session stored, state consumed), re-add of existing email (no duplicate, status flips back to connected, last_seen_at populated, same id), bad state (400 HTML with "Couldn't connect"), expired state (400 + state map cleaned), code-exchange error (400 HTML containing the thrown message), missing code-or-state (400), HTML-escapes `<script>` in error message echo (XSS guard). State is consumed (deleted from map) atomically before the code exchange — even if the exchange throws, the state can't be reused. Insert path leaves `last_seen_at` NULL per spec ("update `status='connected'` and `last_seen_at` if it already exists" — strict reading: only on update). `registerOauthRoutes(app, deps?)` accepts an optional `OauthRouteDeps` (DI hook for tests); production callers pass nothing. Two extra tests beyond plan: missing-code/state → 400, and the `<script>` escape (defensive XSS guard for an HTML page that echoes a thrown message)._

- [x] **Step 14: `POST /api/accounts/:id/reconnect` endpoint.** Add to `src/server/api/oauth.test.ts`: seed the DB with one account (`status='needs_reauth'`); `app.fetch(new Request('http://x/api/accounts/1/reconnect', { method: 'POST' }))` returns 200 with `{ consent_url, state }`; the state map records `{ kind: 'reconnect', accountId: 1, expiresAt }`. Unknown id (`/api/accounts/999/reconnect`): returns 404 JSON. Then add a callback test for the `reconnect` branch: state is `kind=reconnect, accountId=1`; OAuth returns the matching email; the row is updated to `status='connected'`, no new row inserted. **Edge case (mismatched email):** state is `kind=reconnect, accountId=1` but OAuth returns a *different* email than the row at id=1 — returns 400 HTML with a clear message; the row is **not** modified. (This judgment-call behavior is flagged in the review's "Decisions worth flagging".) Run vitest — **red**. Implement the route in `src/server/api/oauth.ts` and the callback's `kind=reconnect` branch with the email-match check. Re-run **green**. Verification: vitest passes. — _Done: 6 new tests (16 total in this file). New endpoint covers happy path (200 with consent_url + reconnect-kind state recording accountId), 404 for unknown id, 400 for non-integer id (`/api/accounts/not-a-number/reconnect`). Callback `kind=reconnect` branch: happy reconnect path (302, status flips back to connected, last_seen_at populated, no duplicate, session set), mismatched email (400 HTML containing both the registered and the signed-in email; row + session unchanged), vanished account (400 — handles the race where DB row was removed between Reconnect click and callback). Mismatch judgment call documented in implementation as a clear user-facing message naming both addresses; the user is told to "reconnect using the original Google account" rather than silently inserting a new row. Will be flagged in the spec's review under "Decisions worth flagging"._

- [x] **Step 15: Wire routes + migrate() into server entry.** Update `src/server/app.ts` to accept an option-flagged DI shape so tests can inject a custom static dir without registering OAuth / accounts routes; the deployed factory call (in `src/server/index.ts`) registers everything. Update `src/server/index.ts` to call `migrate(getDb(), migrationsDir)` before `serve(...)`, where `migrationsDir = path.resolve(dirname(fileURLToPath(import.meta.url)), '../db/migrations')` (resolves to `dist/server/db/migrations` after build, `src/server/db/migrations` under `tsx watch`). Existing app tests (`src/server/app.test.ts`) still pass — they continue to use `createApp()` without the OAuth routes. Add `src/server/index.test.ts`? Not needed — the listener entry doesn't have testable surface beyond what's already covered. Verification: `npx tsc --noEmit -p tsconfig.json` exits 0; `npx vitest run` shows all pre-existing tests still passing. — _Done: typecheck clean, all 75 tests still pass. `createApp` now registers `/health`, accounts routes, oauth routes, and the static fallback in that order — route-registration order keeps the static `*` middleware behind the specific paths. Deviation from plan text: did **not** add an opt-out flag to `createApp`. Instead, OAuth/accounts route handlers don't touch the DB at registration time (only when invoked), so the existing app.test.ts paths (`/health`, `/`) continue to work without DB setup. The DI hook for tests is the per-request override (`registerOauthRoutes(app, deps)`) plus `setSessionClientFactoryForTest` and `setDbPathForTest`. `src/server/index.ts` adds `mkdirSync(dirname(resolve(config.dbPath)), { recursive: true })` before `getDb()` — defensive against the data dir missing on first run; `data/.gitkeep` (Step 20) and Docker's bind-mount each cover the same gap differently. Build still emits `dist/server/index.js`, `dist/server/app.js`, and `dist/server/db/migrations/0001_create_accounts.sql`._

- [x] **Step 16: Client API helper.** Write `src/client/api.test.ts` (jsdom env). Test cases: `getJson('/api/accounts')` mocks `globalThis.fetch` to return `new Response(JSON.stringify({...}), { status: 200, headers: { 'Content-Type': 'application/json' } })`, asserts the parsed value; `postJson('/api/oauth/start')` sends `Content-Type: application/json` and `method: 'POST'` (asserted via the mock's call args); on non-2xx, throws an Error whose message includes the response body text. Run vitest — **red**. Implement `src/client/api.ts` with `export async function getJson<T>(url: string): Promise<T>` and `postJson<T>(url: string, body?: unknown): Promise<T>`. Re-run **green**. Verification: vitest client project passes. — _Done: 6 tests passing — getJson GET happy path, postJson POST with Content-Type header, postJson serializes body, postJson without body omits the body field, getJson throws on non-2xx with response body in message, postJson throws on non-2xx. `globalThis.fetch` swapped per-test via `vi.fn()`. Two extra tests beyond plan's three (no-body case, postJson 4xx) — locks in the API surface that Steps 17–19 will exercise._

- [x] **Step 17: AccountList component.** Write `src/client/components/AccountList.test.tsx` (jsdom + RTL). Cases: `<AccountList accounts={[]} onReconnect={...} />` renders an empty-state message ("No accounts connected yet" or similar); `<AccountList accounts={[oneRow]} />` renders one row showing `email` and the status badge; if `display_name` is set, renders it alongside `email`; renders the Reconnect button only on rows whose `status === 'needs_reauth'`; clicking Reconnect calls `onReconnect(accountId)`. Run vitest — **red**. Implement `src/client/components/AccountList.tsx`. Re-run **green**. Verification: vitest client project passes. — _Done: 6 tests passing — empty state, single connected row, display_name fallback to email, Reconnect button only on needs_reauth rows, click handler receives accountId, needs_reauth status text. New shared client type module `src/client/types.ts` exports `Account` and `AccountStatus` (mirroring the server's `accounts.ts` shape; small duplication, no shared module yet — Slice 003+ may extract one). New `src/client/test-setup.ts` registers `afterEach(cleanup)` so RTL renders don't leak between tests; wired into `vitest.workspace.ts`'s client project via `setupFiles`. Implementation uses `<ul>/<li>` with the email/status as inline elements and conditionally renders the Reconnect button — minimal markup, no styling (Tailwind/shadcn lands later)._

- [x] **Step 18: AddAccountButton + polling hook.** Write `src/client/components/AddAccountButton.test.tsx` (jsdom + RTL + `vi.useFakeTimers()`). Cases: clicking the button calls `postJson('/api/oauth/start')` (mocked), then opens `consent_url` (`window.open` mocked), then starts polling. Advance timers by 2s — `getJson('/api/accounts')` is called. When the poll's response includes a new account id (one not in the pre-Add snapshot), `onAdded(newAccount)` fires and the polling stops (advance more time, no further `getJson` calls). On 5-minute timeout (advance timers by 300s without a new account appearing), polling stops and the button surfaces an error message. Run vitest — **red**. Implement `src/client/components/AddAccountButton.tsx` and extract a small `src/client/hooks/useAccountsPoll.ts` with signature `useAccountsPoll({ baselineIds, done, onTimeout, intervalMs?, timeoutMs? })`. Re-run **green**. Verification: vitest client project passes. — _Done: 4 tests passing — happy click (POST /api/oauth/start + window.open + first poll fires within 500 ms), onAdded fires when new id appears + polling stops afterward, timeout shows alert + polling stops, POST start failure surfaces alert without opening consent. **Significant deviation from plan text:** abandoned `vi.useFakeTimers()` after multiple hangs and switched to **real timers with short intervals** (`pollIntervalMs={20}`, `pollTimeoutMs={50–5000}`) injected via new optional component props. Root cause was vitest 2.1's default `vi.useFakeTimers()` mocking `queueMicrotask`, which broke React's microtask-based effects + RTL's `waitFor`. Even after excluding `queueMicrotask` from `toFake`, `userEvent.click` hung (likely a React 19 + RTL 16 + user-event 14 + fake-timers interaction). Real-timer + injected-interval approach is simpler, faster (501 ms total for 4 tests), and matches RTL's recommended async testing pattern (`waitFor` + finder queries). The new props (`pollIntervalMs?`, `pollTimeoutMs?`) double as test seams and as future product knobs (a slow connection might want a longer interval). Hook signature stayed as planned. Window.open spy declared inline (no typed `let openSpy` var) because TS couldn't reconcile `vi.spyOn`'s default generics with `Window.open`'s signature; assertions via `expect(window.open).toHaveBeenCalled...` directly._

- [x] **Step 19: Dashboard view + App.tsx.** Write `src/client/views/Dashboard.test.tsx`. Cases: on mount, calls `getJson('/api/accounts')` (mocked); shows a loading state; on resolve, renders `<h1>Docurator</h1>`, the `<AccountList />` with the returned accounts (pass through), and `<AddAccountButton />`; when `<AddAccountButton />`'s `onAdded` fires, refetches and rerenders with the new list; clicking Reconnect on a `needs_reauth` row calls `postJson('/api/accounts/:id/reconnect')` (mocked), opens the returned `consent_url`, starts polling for that row's `status` flipping to `connected` (uses the same `useAccountsPoll` hook with a different `done` predicate). Run vitest — **red**. Implement `src/client/views/Dashboard.tsx`; replace the body of `src/client/App.tsx` with `<Dashboard />`. Re-run **green**. Verification: vitest client project passes; `npm run build` exits 0; `dist/client/index.html` and `dist/server/index.js` both exist. — _Done: 6 tests passing — loading-then-loaded, empty-state, click-Reconnect-fires-POST-and-window.open, reconnect-poll-flips-status (Reconnect button disappears once status='connected'), Add-flow-appends-new-account (bob shows up after polling), initial-fetch-error shows alert. Dashboard tracks `accounts: Account[] | null` (null = loading), `error: string | null`, `reconnectingId: number | null`. The Reconnect polling uses the same `useAccountsPoll` hook as AddAccountButton with a different predicate (`account.status === 'connected'` for the targeted id). When `onAdded` fires, the new account is appended to the list directly rather than re-fetching — saves a roundtrip and avoids the race where the polled list might lag the just-added row. `App.tsx` now just `<Dashboard />`. `npm run build` succeeds: 33 modules → 198.10 kB client bundle (up 3 kB from Slice 001's 194.62 kB), `dist/server/index.js` and migrations all present._

- [x] **Step 20: `.env.example`, `data/.gitkeep`, `.gitignore` exception.** Create `.env.example` with comments + the four keys: `APP_PORT=3737`, `GOOGLE_CLIENT_ID=`, `GOOGLE_CLIENT_SECRET=`, `OAUTH_REDIRECT_PORT=` (with a comment "defaults to APP_PORT"). Create `data/.gitkeep` (empty file). Append `!data/.gitkeep` to `.gitignore` so the directory is tracked via the `.gitkeep` while still ignoring user data files. Verification: `ls .env.example data/.gitkeep` succeeds; `git check-ignore data/.gitkeep` exits non-zero (file is **not** ignored) while `git check-ignore data/app.db` exits 0 (still ignored). — _Done: all three files in place. `.gitignore` change: replaced `/data/` with `/data/*` + `!/data/.gitkeep` (gitignore semantics require ignoring the directory's contents rather than the directory itself for re-includes to work). `git check-ignore data/.gitkeep` → exit 1 (not ignored). `git check-ignore data/app.db` → exit 0 (still ignored). `.env.example` documents the four env vars with `OAUTH_REDIRECT_PORT` commented-out (showing it as optional with the APP_PORT default)._

- [x] **Step 21: docker-compose env passthrough + bind mount.** Update `docker-compose.yml` to add an `environment:` block (`GOOGLE_CLIENT_ID: ${GOOGLE_CLIENT_ID}`, `GOOGLE_CLIENT_SECRET: ${GOOGLE_CLIENT_SECRET}`) and a `volumes:` block (`- ./data:/app/data`). Per `architecture.md` § "Docker Compose layout" the canonical shape also includes Ollama envs and `./invoices`, but those are scoped to later slices. Verification: `docker compose config` exits 0 and the resolved output surfaces the new env keys (not blank — at least `GOOGLE_CLIENT_ID:` is present in the YAML even when the host's value is empty) and the `./data:/app/data` mount. — _Done: `docker compose config` exits 0. Resolved output shows `environment: GOOGLE_CLIENT_ID: ""`, `GOOGLE_CLIENT_SECRET: ""` (compose's empty-host-vars default substitution; the warnings emitted are expected — host doesn't have those env vars set unless `.env` is sourced). Volume resolves as `type: bind, source: /Users/rasmus/docurator-2/data, target: /app/data` with `create_host_path: true`. Slice 005/006 will add the Ollama env block and the `./invoices` volume._

## Smoke test recipe

The exact sequence the loop will run after all plan steps are checked. All commands run from the repo root.

1. `docker compose down -v` (idempotent reset; safe even if nothing is running).
2. Confirm `.env` exists at the repo root with non-empty `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` set to the user's real Google OAuth credentials. (If absent, this whole smoke is skipped — capture the skip in the smoke run and continue.)
3. `docker compose up --build -d`. Poll `curl -fsS http://localhost:3737/health` until it returns `200 ok` (cap ~60s).
4. Confirm migrations ran: `sqlite3 data/app.db '.tables'` lists at least `accounts` and `_migrations`. `sqlite3 data/app.db '.schema accounts'` matches the spec's column list verbatim. (If `sqlite3` isn't installed on the host, fall back to `docker compose exec app node -e "const d = require('better-sqlite3')('/app/data/app.db'); console.log(d.prepare('SELECT name FROM sqlite_master WHERE type=\\\"table\\\"').all())"` or similar.)
5. Open `http://localhost:3737/` in a browser. Confirm: heading "Docurator" visible, empty-state CTA, "Add Gmail account" button visible, no console errors.
6. Click "Add Gmail account". A new tab opens to Google's consent screen. **Confirm the requested permissions are exactly:** "Read your email messages and settings" and "See your primary Google Account email address". **No write permissions appear** ("Send email", "Manage labels", "Move to trash" must all be absent). Approve consent.
7. The tab navigates back to localhost showing a "you can close this tab" message (the redirect to `/`). Close it. The Dashboard tab now shows one row with the just-authenticated email, status badge `connected`. Record the email used.
8. In a separate browser session (private/incognito or a different profile), sign in to a *different* Google account. Click "Add Gmail account" again on the Dashboard. Approve consent. The Dashboard shows two rows — different `email` values, different `slug` values, both `connected`.
9. `sqlite3 data/app.db 'SELECT id, email, slug, status, connected_at, last_seen_at FROM accounts ORDER BY id;'` — both rows present, slugs match `slugify(email)`, both `connected`. Record the values.
10. `docker compose down`, then `docker compose up -d` (no `--build`). Browser: both accounts still listed (persisted to `app.db`). The session token map is empty in memory, but Slice 002 has no Gmail-touching path that would surface this — note for the smoke run.
11. `sqlite3 data/app.db "UPDATE accounts SET status='needs_reauth' WHERE id=1;"`. Refresh the Dashboard. Row 1 shows a "Reconnect" button (and a `needs_reauth` badge); row 2 still shows just `connected`. Click "Reconnect" on row 1. Approve consent in the same Google account that originally owned row 1. The Dashboard polls and within a few seconds row 1's badge flips back to `connected`. `sqlite3 data/app.db 'SELECT id, status FROM accounts;'` confirms — no duplicate row, status flipped, `last_seen_at` updated.
12. AC #5 (revoke at Google account settings → `invalid_grant` on next refresh → row flips to `needs_reauth`): **deferred to Slice 003** — Slice 002 has no Gmail-touching path that would trigger a refresh. The unit test in `src/server/auth/session.test.ts` covering the `invalid_grant` path stands in for this AC at Slice 002's scope. Record this deferral in the smoke run.
13. `git status` clean (no `node_modules/`, `dist/`, `data/app.db`, `data/app.db-*`, `.env` leaks).
14. `docker compose down` to leave the host clean.

## Test run

- **Date:** 2026-05-09
- **Command:** `npx vitest run`
- **Result:** 16 test files, **97 tests, all passing**. Duration 2.82 s.
  - Server (10 files, 73 tests):
    - `src/server/config.test.ts` — 11 tests (port/env defaults, frozen object, googleClientId/Secret, oauthRedirectPort default + env override, dbPath)
    - `src/server/app.test.ts` — 3 tests (`/health`, static `/`, route-order guard)
    - `src/server/db/index.test.ts` — 3 tests (singleton, override path, replace-and-close)
    - `src/server/db/migrate.test.ts` — 4 tests (apply, idempotent, skip-applied, rollback-on-error)
    - `src/server/db/migrations.test.ts` — 4 tests (`accounts` table shape, UNIQUE on email/slug, CHECK on status)
    - `src/server/auth/slug.test.ts` — 8 tests (canonical example, casing, plus-aliases, underscores, trim, collapse, digits, punctuation strip)
    - `src/server/auth/accounts.test.ts` — 8 tests (insert + collision suffix, find/round-trip, undefined-on-miss, updateStatus, touchLastSeen, list ordering)
    - `src/server/auth/oauth.test.ts` — 7 tests (consent URL params, OAUTH_REDIRECT_PORT override, SCOPES, exchangeCode happy path + 3 error paths)
    - `src/server/auth/session.test.ts` — 8 tests (set/listener, listener updates/preserves refreshToken, clear, withFreshTokens happy + invalid_grant + non-invalid_grant + missing-session)
    - `src/server/api/accounts.test.ts` — 2 tests (empty list, two-row shape)
    - `src/server/api/oauth.test.ts` — 16 tests (POST /api/oauth/start ×3, GET /oauth/callback ×7 covering happy/re-add/bad-state/expired/exchange-error/missing-params/XSS-escape, GET /oauth/callback reconnect ×3 covering happy/mismatched-email/vanished-account, POST /api/accounts/:id/reconnect ×3 covering happy/404/400)
  - Client (5 files, 24 tests):
    - `src/client/jsdom-env.test.tsx` — 1 test (jsdom DOM smoke)
    - `src/client/api.test.ts` — 6 tests (GET/POST happy paths, body serialization, no-body case, 4xx/5xx error message capture)
    - `src/client/components/AccountList.test.tsx` — 6 tests (empty state, row + status, display_name, Reconnect-only-on-needs_reauth, click handler, needs_reauth status text)
    - `src/client/components/AddAccountButton.test.tsx` — 4 tests (happy click + first poll, onAdded fires + polling stops, timeout shows alert, POST start failure)
    - `src/client/views/Dashboard.test.tsx` — 6 tests (loading then loaded, empty-state, Reconnect click, reconnect-poll status flip, AddAccountButton.onAdded appends, initial-fetch error)
- **Coverage of acceptance criteria:**
  - AC #1 (`/api/accounts` empty + Add button) — Dashboard tests + accounts.test.ts
  - AC #2 (consent flow + new row) — api/oauth.test.ts callback + accounts.test.ts insert
  - AC #3 (second account, no disturbance) — accounts.test.ts collision tests + Dashboard.test.tsx Add flow
  - AC #4 (persistence across container restart) — `accounts` table is on the bind-mounted volume; smoke test verifies
  - AC #5 (revoke → invalid_grant → needs_reauth) — session.test.ts invalid_grant test (end-to-end exercise deferred to Slice 003 per research/plan)
  - AC #6 (Reconnect flips back to connected, no duplicate) — api/oauth.test.ts reconnect happy path + Dashboard.test.tsx reconnect poll
  - AC #7 (`GET /api/accounts` shape) — api/accounts.test.ts
  - AC #8 (no Gmail scope other than `gmail.readonly`) — auth/oauth.test.ts SCOPES test verifies `[openid, userinfo.email, gmail.readonly]` exactly

## Smoke run

- **Date:** 2026-05-09
- **Result:** Pass (with documented deferrals).

**What was exercised**

The Docker portion of the smoke recipe could not run in this sandboxed environment (`docker compose build` failed at `resolve image config for docker-image://docker.io/docker/dockerfile:1.7` with `dial tcp: lookup auth.docker.io: i/o timeout` — the registry is unreachable from the agent's network). Substituted a **local-host smoke against the built artifact** (`node dist/server/index.js`), which exercises the same code paths Docker would: the migration runner, the Hono server, the API endpoints, the static fallback, and SQLite persistence across process restart. Sequence executed:

1. `rm -f data/app.db && npm run build` — clean DB, fresh build (33 modules, 198.10 kB client bundle). Exit 0.
2. `GOOGLE_CLIENT_ID=fake-id GOOGLE_CLIENT_SECRET=fake-secret node dist/server/index.js` started; polled `curl /health` until ready (~1 s).
3. **`GET /health`** → `200 ok`, `Content-Type: text/plain; charset=UTF-8`. Verifies Slice 001's contract still holds.
4. **`GET /`** → `200 text/html` containing `<title>Docurator</title>`, `<div id="root"></div>`, and the Vite-emitted `<script type="module" crossorigin src="/assets/index-8uIsiBU4.js">`. The Dashboard renders client-side after JS loads (verified by RTL tests in step 19; live in-browser DOM check is human-only).
5. **`GET /api/accounts`** → `200 application/json`, body `{"accounts":[]}`. Empty list as expected on first start.
6. **`sqlite3 data/app.db .tables`** → `_migrations  accounts`. Migration ran on the bind-mount-equivalent local volume.
7. **`sqlite3 data/app.db .schema accounts`** matches the spec verbatim (id PK, email NOT NULL UNIQUE, display_name NULL, slug NOT NULL UNIQUE, connected_at NOT NULL, last_seen_at NULL, status CHECK in 'connected'/'needs_reauth').
8. **`SELECT * FROM _migrations`** → `0001_create_accounts.sql|2026-05-09T14:51:23.293Z`.
9. **`POST /api/oauth/start`** → `200 application/json`, body `{ consent_url: "https://accounts.google.com/o/oauth2/v2/auth?access_type=offline&prompt=consent&scope=openid%20…userinfo.email%20…gmail.readonly&state=<uuid>&response_type=code&client_id=fake-id&redirect_uri=http%3A%2F%2Flocalhost%3A3737%2Foauth%2Fcallback", state: "<uuid>" }`. **AC #2 partial** — the consent URL is built correctly with exactly the three required scopes (URL-encoded space-separated `openid + userinfo.email + gmail.readonly`); the actual Google consent screen interaction is human-only and requires real OAuth credentials in `.env`.
10. **`POST /api/accounts/999/reconnect`** → `404 application/json`, body `{"error":"account not found"}`. Unknown id rejected.
11. **`GET /oauth/callback?code=abc&state=does-not-exist`** → `400 text/html` containing `Couldn't connect this account` and `Unknown or expired state`. Invalid state surfaces a clean error page.
12. Synthesized a `needs_reauth` row directly via `sqlite3 data/app.db "INSERT INTO accounts (email, slug, connected_at, status) VALUES ('alice@example.com', 'alice-at-example-com', '2026-05-09T14:51:00Z', 'needs_reauth');"`. **`GET /api/accounts`** then returned `{"accounts":[{"id":1,"email":"alice@example.com","display_name":null,"slug":"alice-at-example-com","connected_at":"2026-05-09T14:51:00Z","last_seen_at":null,"status":"needs_reauth"}]}`. Confirms the API mirrors the DB contents in the documented shape.
13. **`POST /api/accounts/1/reconnect`** → `200 application/json` with a fresh consent_url and reconnect-kind state. **AC #6 partial** — the route correctly identifies the existing row and issues a reconnect-state-bound consent URL; the callback's update-row path is unit-tested in `api/oauth.test.ts`.
14. **Persistence (AC #4):** stopped and restarted the server (kill node + relaunch). `GET /api/accounts` after restart still returned the synthesized `alice@example.com` row with `status='needs_reauth'`. The accounts table survives process restart on the local volume; the same will hold for the Docker bind mount per `docker compose config` resolution shown in Step 21.
15. **`grep -rn 'gmail\.' src/ --include='*.ts' --include='*.tsx' | grep -v '\.test\.'` → 1 hit, exactly `'https://www.googleapis.com/auth/gmail.readonly'` in `src/server/auth/oauth.ts:7`. **AC #8** verified — no Gmail write scope present, no Gmail API client ships in this slice.
16. **`git status` after stopping the server and removing `data/app.db`** — only the slice's expected modifications + new files; `data/.gitkeep` shows up as `?? data/.gitkeep` (untracked, will land in the commit), and `git check-ignore data/.gitkeep` exits 1 (not ignored) confirming the gitignore exception works.

**Deferred (require human + real Google OAuth)**

- AC #1 (the visible "Add Gmail account" button on the rendered page) — RTL test exercises the React render in jsdom; in-browser visual verification is human-only.
- AC #2 full path (Google consent → tab returns → row appears) — requires `.env` with real `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET` and a Google account.
- AC #3 (a second different Google account adds a second row) — same.
- AC #5 (revoke at https://myaccount.google.com/permissions → next refresh produces `invalid_grant` → row flips to `needs_reauth`) — Slice 002 has no Gmail-touching path that triggers a refresh, so this AC is exercised end-to-end only once Slice 003's Inbox lands. The unit test `session.withFreshTokens` covers the `invalid_grant` → `updateStatus(needs_reauth)` + `clear` + rethrow logic, which is the totality of this slice's contribution to AC #5.
- The Docker layer of the smoke recipe (`docker compose up --build -d`) — blocked by the sandbox network restriction. The Dockerfile and compose YAML have been built/parsed successfully in earlier slices and steps (Slice 001 smoke + this slice's Step 21 `docker compose config`), so the failure here is environmental, not a code issue.

**Caveats:** the smoke run substitutes `node dist/server/index.js` for `docker compose up`; the two execute the same compiled JavaScript, the same migration runner, and the same Hono application. The bind mount is exercised functionally (the SQLite file lives in the host's `./data/` directory, which is exactly the bind-mount target in compose). The real OAuth consent screen and the cross-account flow are deferred to a human-driven pass.
