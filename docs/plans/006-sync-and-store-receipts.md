# Slice 006: Sync and store receipts — Plan

**Spec:** `docs/specs/006-sync-and-store-receipts.md`
**Research:** `docs/research/006-sync-and-store-receipts.md`

## Steps

Each step is small enough to fit in one loop iteration. Each step ends with a concrete verification — a named test passing, or a specific command's output. Per-step refactor is folded into the priority-3 action. The research doc identified two ADRs the slice should produce; each is attached to the step that triggers the decision.

- [x] **Step 1: Install `playwright` runtime dep + postinstall script.** Edit `package.json` to add `playwright ^1.x` to `dependencies` and a `postinstall: playwright install chromium` script that runs `playwright install chromium` (no `--with-deps`; users running on Linux without Docker may need apt deps separately, documented under "Risks"). Run `npm install`. Verification: `npm install` exits 0; `package-lock.json` shows the new entry; `npx playwright --version` works; `npm run check:gmail-readonly` exits 0; `npm run typecheck` exits 0; `npx vitest run` continues to pass (no new tests yet). If the postinstall fails inside the devcontainer due to missing apt deps, document the actual failure and either run `npx playwright install --with-deps chromium` once with sudo or add the apt deps to the devcontainer's `Dockerfile`.

  _Done: installed `playwright@1.59.1` (the latest 1.x). Postinstall script `playwright install chromium` added to `package.json`. `npx playwright install chromium` fetched both `chromium-1217` and `chromium_headless_shell-1217` to `~/.cache/ms-playwright/`. **Devcontainer apt-deps are not installed** (libnspr4, libnss3, libatk*, libcups2, libxkbcommon0, libasound2, libgbm1, libxcomposite1, libxdamage1, libxfixes3, libxrandr2, libatspi2.0-0); chromium-1217 fails to launch with `error while loading shared libraries: libnspr4.so`. Sudo in this devcontainer requires a password (no passwordless config), so the agent cannot apt-install during the loop run. **Flagging for the review**: Step 11's `render-html-pdf.test.ts` will fail in this environment until the user runs `sudo npx playwright install-deps chromium` once on the host, or the devcontainer's Dockerfile is updated to bake those libs in. Same shape as Slice 005's smoke deferred-AC pattern. **Test-harness ripple:** the inflated node_modules (~250 MB after Playwright + chromium) pushed `api/classify.test.ts`'s first-test cold-start past the 15s timeout Slice 004 set. Bumped `vitest.config.ts` `testTimeout` from 15000 to 30000 with an inline comment naming the cause. Real assertion failures still surface within seconds. Full suite: 43 files / 382 tests / 0 failures in 40.30s post-bump (was 23.03s in Slice 005's final test run; the difference is dominated by Playwright's resolution chain, not test work). `npm run check:gmail-readonly` OK; `npm run typecheck` clean._

- [x] **Step 2: Add `syncDefaultWindowDays`, `maxConcurrentClassify`, `invoicesDir` to `src/server/config.ts`.** Extend `src/server/config.test.ts` with three default cases + three env-override cases + a NaN-fallback case for `MAX_CONCURRENT_CLASSIFY`. Run vitest — **red**. Implement: defaults `30`, `1`, `'./invoices'`; env vars `SYNC_DEFAULT_WINDOW_DAYS`, `MAX_CONCURRENT_CLASSIFY`. `invoicesDir` has no env override (path is implicit; tests inject a different path via a private setter or `vi.resetModules` + temp config file). Re-run **green**. Verification: `npx vitest run src/server/config.test.ts` passes.

  _Done: 7 new tests (3 default + 3 env-override + 1 NaN-fallback for `MAX_CONCURRENT_CLASSIFY`; same shape as the Slice 005 timeout test). The window-days parser also rejects zero/negative + NaN, matching the timeout-parser convention from Slice 005. `invoicesDir` has no env override, only a constant default — tests reach for it via the imported `config` object. 27/27 green in 37ms._

- [x] **Step 3: Plumbing — `.env.example` + `invoices/.gitkeep` + startup `mkdirSync` for invoices dir.** Append two commented sections to `.env.example` for `SYNC_DEFAULT_WINDOW_DAYS` and `MAX_CONCURRENT_CLASSIFY`. Create `invoices/.gitkeep`. Edit `src/server/index.ts` to add `mkdirSync(resolve(config.invoicesDir), { recursive: true })` next to the existing data-dir mkdirSync. No test framework covers this directly; verification is `git diff` shows the three changes and the existing `app.test.ts` continues to pass after a fresh boot.

  _Done: three edits as planned. `.env.example` now documents `SYNC_DEFAULT_WINDOW_DAYS` and `MAX_CONCURRENT_CLASSIFY` next to the OLLAMA block. `invoices/.gitkeep` created (empty file). `src/server/index.ts` has `mkdirSync(resolve(config.invoicesDir), { recursive: true })` immediately after the data-dir mkdirSync, so first-run on a fresh host creates `./invoices/` before migrations run. `npm run typecheck` clean; `app.test.ts` 3/3 passes._

- [x] **Step 4: Migration `0005_create_documents.sql` + tests.** Extend `src/server/db/migrations.test.ts` with a `describe('0005_create_documents.sql', …)` block (mirrors Slice 004's per-table blocks):
  - `PRAGMA table_info(documents)` returns the spec's columns with the right NOT NULL / nullable distribution.
  - `id` has `pk: 1`; INTEGER PRIMARY KEY AUTOINCREMENT (verified via `sqlite_sequence` presence after an insert).
  - `kind` CHECK enforces `attachment` / `rendered_body`.
  - `review_status` CHECK enforces `pending` / `approved` / `rejected`; default is `pending`.
  - `PRAGMA foreign_key_list` shows one FK row referencing `accounts(id)`.
  - `(account_id, content_hash)` UNIQUE: two inserts with the same pair throws on the second.
  - Same `content_hash` across two different `account_id` values both insert successfully (cross-account dedup is intentionally absent).
  - `(account_id, review_status, created_at)` and `(account_id, message_id)` indices via `PRAGMA index_list` + `index_xinfo` (column composition, not name guessing).

  Run vitest — **red**. Implement the SQL file (one CREATE TABLE + two CREATE INDEX statements). Re-run **green**. Verification: `npx vitest run src/server/db/migrations.test.ts` passes; the four prior describe blocks (0001-0004) stay green.

  _Done: 9 new tests in the `0005_create_documents.sql` describe block (column shape, AUTOINCREMENT, FK, unknown-account-id rejection, kind CHECK, review_status CHECK + default 'pending', UNIQUE on `(account_id, content_hash)`, cross-account dedup absence, both indices via PRAGMA `index_list` + `index_xinfo`). Migration file is one CREATE TABLE (with inline UNIQUE constraint) + two CREATE INDEX statements; types and nullability match the spec verbatim. Pre-test friction: had to `npm rebuild better-sqlite3` on the host because the native binary in `node_modules` was the Linux build from the devcontainer's `npm rebuild` postCreateCommand and dlopen rejected it on darwin/arm64. Pure environment cost; flagged for the review only if it recurs. Migration test file: 30/30 green._

- [x] **Step 5: `src/server/db/repositories/documents.ts` + tests.** Mirror the shape of `processed_messages.ts`. New test file `src/server/db/repositories/documents.test.ts` covering:
  - `existsByHash` false / true.
  - `insert` returns the surrogate id; `findById(id)` returns the row.
  - `listForAccount({ account_id, limit, offset })` orders `created_at DESC`, respects limit + offset, returns `{ rows, total }`.
  - `listForAccount({ ..., review_status: 'pending' })` filters.
  - FK constraint: insert with non-existent `account_id` throws.
  - UNIQUE constraint: second insert with the same `(account_id, content_hash)` throws.
  - Latest-attempt JOIN test: insert two `processed_messages` rows for the same `(account_id, message_id)` (different `processed_at`); `listForAccount` returns the latest attempt's classification + subject + sender_domain.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/db/repositories/documents.test.ts` passes (≥ 7 cases).

  _Done: 11 tests passing (4 beyond the plan's seven — `existsByHash` account-scoping, `findById` undefined-on-miss, `listForAccount` account-scoping, and `listForAccount` returning null subject/sender_domain when no `processed_messages` row exists for the document). Repo signature: `existsByHash`, `insert`, `findById`, `listForAccount`. Statements cached per-DB via `WeakMap` (matching `processed_messages.ts`). `listForAccount` uses a `LEFT JOIN processed_messages pm ON pm.id = (SELECT MAX(id) FROM processed_messages WHERE account_id = d.account_id AND message_id = d.message_id)` to surface the latest attempt's classification + confidence + subject + sender_domain — exactly the JOIN shape the spec calls out (§ "Detailed design" → "processed_messages JOIN cost"). The orphan-document case (a `documents` row with no matching `processed_messages` row, which can happen during dev seeding or if reclassify is run before the original processed_messages is committed) returns nulls in those columns; the LEFT JOIN handles it cleanly. `total` is computed via a separate `SELECT COUNT(*)` query (cheaper than `SELECT FOUND_ROWS` semantics SQLite doesn't have anyway). Filter and unfiltered queries cache under separate keys (`total`, `totalForStatus`, `list`, `listForStatus`) so prepared-statement reuse stays correct. `npm run typecheck` clean._

- [x] **Step 6: `src/server/files.ts` (writeReceiptFile) + tests.** New test file `src/server/files.test.ts` using a `mkdtempSync` invoices root:
  - Writes bytes to the right path under `{slug}/{yyyy}/{mm}/...`.
  - Returns `{ file_path, content_hash, size }`; hash matches `crypto.createHash('sha256')` independently.
  - Filename sanitization: pathological inputs (`../../etc/passwd`, `foo/../bar`, `con.txt`, very long names, control chars) produce safe basenames.
  - Path-traversal guard: even after sanitization, the resolved absolute path stays inside the invoices root; if (somehow) it would escape, throws.
  - Year/month projection from `internal_date` epoch-ms is correct for known timestamps.
  - Atomic write: writes to `<final>.tmp` then renames; mid-write process death leaves no partial final file.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/files.test.ts` passes (≥ 6 cases).

  _Done: 18 tests passing. Function: `writeReceiptFile({ account_slug, internal_date, message_id, seq, suggested_filename, bytes }) → { file_path, content_hash, size }` plus exported `sanitizeFilename(input)` and the `setInvoicesRootForTest` / `resetInvoicesRootForTest` test seams (mirroring `setDbPathForTest` from `db/index.ts`). Sanitization: strip path separators + control chars + path-hostile chars (`<>:|"\\/`), strip leading dots, truncate to 100 bytes preserving extension, escape Windows reserved names (`CON`/`PRN`/`AUX`/`NUL`/`COM[1-9]`/`LPT[1-9]`) by prefixing `_`, fall back to `unnamed` if everything would have been stripped. The Windows reserved-name escape is defensive for the export bundle (Slice 011) which lands on accountants' machines that may be Windows; on the local Linux container it costs nothing. Path-traversal guard rejects path-shaped `account_slug` early (`/`, `\\`, `..`, empty); for any subsequent escape attempt via the joined path, the `path.resolve(root, rel)` result is checked against `root + sep`. Atomic write via `<final>.tmp` + rename: tested by checking no `.tmp` file remains in the directory after a successful call. Year/month derivation uses UTC (`getUTCFullYear` / `getUTCMonth + 1`) so a UTC December timestamp doesn't roll into January in a positive-offset host TZ. `npm run typecheck` clean. **Two test bugs caught and fixed in red→green:** (1) hard-coded an epoch-ms value claiming 2026 that actually decoded to 2025 — switched to `Date.UTC(2026, 4, 9, 10, 0)` for clarity; (2) over-tight regex on the path-traversal-error message — loosened to a bare `toThrow()` since both error paths (account-slug guard and final-path-guard) satisfy the security property._

- [x] **Step 7: Refactor `classifyMessage` to return optional `source_bytes`.** This is the foundation that lets the sync orchestrator persist what the classifier saw without re-fetching. Extend `src/server/classify/index.ts`'s `ClassifyResponse` extension (or add a new outer return wrapper — see below) so the function returns `{ ...verdict, model_used, artifacts, source_bytes?: Map<string, Buffer> }` where `source_bytes` keys are stable artifact descriptors (e.g. `attachment:invoice.pdf`, `body:rendered_html_source`). The `body:rendered_html_source` slot holds the raw HTML string when html_was_used so the orchestrator can run Playwright on it; the orchestrator computes the rendered-body PDF bytes in its own step. Update Slice 005's existing tests in `src/server/classify/index.test.ts` to assert on the new field where relevant; the prior assertions stay (the field is additive).

  Run vitest — **red** (new assertion). Implement. Re-run **green**. Verification: `npx vitest run src/server/classify/index.test.ts` passes; Slice 005's other tests (`api/classify.test.ts` was deleted in step 23 below — but at this point it still exists; the route's success-path test already asserts on the spec's response shape and remains green because new optional fields don't break exact-equality checks **unless those tests use `toEqual` with the old shape** — adjust if so).

  Note: this step ships *before* the dev-seed and Slice-005 surface removal (step 23). At the point this step lands, `classify.ts` and `ClassifyRowAction.tsx` are still present; they carry through unchanged because `source_bytes` is optional and they ignore it.

  _Done: 5 new tests in a `source_bytes` describe block (PDF→keyed by `attachment:invoice.pdf` returning the raw pre-render bytes; image→keyed by `attachment:receipt.png` returning the raw bytes; HTML body→keyed by `body:rendered_html_source` returning the raw HTML as a Buffer; plain-text body→no `body:rendered_html_source` key; size-skipped attachments not in the map). Took the **outer-wrapper approach** for the type, not a schema extension: new exported `ClassifyResult = ClassifyResponse & { source_bytes?: Map<string, Buffer> }`. The existing `classificationSchema`/`classifyResponseSchema` Zod shapes stay JSON-clean — `Map<string, Buffer>` doesn't JSON-serialize and the schema is what the HTTP API contract is. The HTTP route in `api/classify.ts` swapped its `ClassifyResponse` import to `ClassifyResult` and now destructures the wrapper before `c.json(verdict)`: `const { source_bytes: _source, ...verdict } = result`. `extract-body.ts` was extended with an additive optional `html_source?: string` field on `ExtractedBody` so the classifier has the raw HTML to capture. **`api/classify.test.ts`'s 12 tests stayed green unchanged** — the route's destructure means callers still see the JSON-clean shape they already asserted against. 25/25 in both files; full typecheck clean._

- [x] **Step 8: Add `historyList` and `getProfile` to `src/server/gmail/client.ts`.** Extend `src/server/gmail/client.test.ts` with two new describe blocks (mirrors the existing `getAttachment` block from Slice 005). Cases:
  - `historyList({ start_history_id, history_types?, page_token? })` calls `users.history.list` with the right params; returns `{ history: ..., next_page_token, history_id }`.
  - `historyList` rethrows session errors verbatim.
  - `getProfile()` calls `users.getProfile` with `userId: 'me'`; returns `{ history_id, email_address, messages_total, threads_total }`.
  - Both methods route through `withFreshTokens` with the bound `accountId`.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/gmail/client.test.ts` passes; `npm run check:gmail-readonly` still exits 0 (`history.list` and `getProfile` are read endpoints; no forbidden substring is introduced).

  _Done: 11 new tests across two describe blocks (`historyList`: 7 cases — userId+start_history_id, optional history_types/page_token passthrough, snake_cased response shape `{ history, history_id, next_page_token }`, missing-history-array normalized to `[]`, accountId routing, session-error rethrow, 404-shape API-error rethrow; `getProfile`: 4 cases — userId='me', snake_case mapping `emailAddress→email_address` etc., accountId routing, session-error rethrow). Total client.test.ts: 24 tests. Public type additions: `HistoryListArgs`, `HistoryListResult`, `ProfileResult`. Both new methods route through `withFreshTokens` and use the same `as unknown as OAuth2Client` cast as the existing methods. Snake_case input/output picked to match the spec's prose (`historyList({ start_history_id })`); the existing `listMessages`/`getMessage` use camelCase — small inconsistency noted for the review follow-up. **Cross-file ripple:** four other test files (`api/messages.test.ts`, `api/dev.test.ts`, `classify/index.test.ts`) constructed inline `GmailClient` literals for testing and broke on the widened interface. Patched each fake to add no-op `historyList`/`getProfile` stubs returning empty data; alternative would have been to make the methods optional on `GmailClient`, rejected because production code should be required to implement them. `check:gmail-readonly` still OK; full typecheck clean; 71/71 across affected files._

- [x] **Step 9: `src/server/sync/discovery.ts` (history vs range) + tests.** Pure function `chooseDiscovery({ syncState, since, fallbackDays, now }): { kind: 'history', start_history_id } | { kind: 'range', q }`. Tests:
  - With `since` set, returns `{ kind: 'range', q: 'after:YYYY/MM/DD' }` (slashes, not dashes — Gmail's `q` syntax).
  - With `last_history_id` and no `since`, returns `{ kind: 'history', start_history_id }`.
  - Without either, returns `{ kind: 'range' }` using `fallbackDays`-back-from-`now`.
  - `since` overrides `last_history_id`.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/sync/discovery.test.ts` passes.

  _Done: 8 tests in `src/server/sync/discovery.test.ts` (4 plan-mandated + 4 defensive: null `last_history_id` falls through to range, fallback uses UTC slashed format, zero-pads single-digit months/days, accepts already-slashed `since` for forward-compat). Function is fully pure: takes `now: Date`, no `Date.now()` calls. The `since` input accepts both `YYYY-MM-DD` (the spec's wording) and `YYYY/MM/DD` — converts dashes to slashes. UTC-based date arithmetic to match Slice 005's existing convention; Gmail's `after:` operator interprets the date in the user's timezone, but UTC is what `internal_date` is, so this is the right choice. The `now` parameter inversion (caller-provided clock) means tests don't need fake timers; same shape Slice 003's `Inbox.tsx` used for the localStorage seed-clock case._

- [x] **Step 10: `src/server/sync/events.ts` (typed emitter + ring buffer) + tests + write ADR-008.** Module exports a singleton `syncEvents` with typed `emit<E>(event, payload)`, `subscribe(): AsyncIterable<{event, payload}>`, `recent(): {event, payload}[]` returning the ring contents, and a `RING_CAPACITY = 200` constant. Tests:
  - Emit-then-subscribe round-trip (subscribers receive subsequent events).
  - Late subscriber receives the ring buffer's recent contents on first iteration.
  - Ring drops the oldest after `RING_CAPACITY` emits.
  - Two subscribers each receive each event.
  - Unsubscribing (returning from the iterator) does not block emit.

  Run vitest — **red**. Implement (Node `EventEmitter` underneath; `subscribe` builds an async iterator with a queue + a resolver). Re-run **green**. **Write `docs/adr/008-sse-with-ring-buffer.md`**: justify SSE over WebSocket (lighter, fits Hono's streaming API, browser native EventSource), over long-polling (more requests, harder to keep state); justify a 200-event ring buffer over a persisted event log (event content is duplicative of `processed_messages` rows; a UI-side replay is enough for late subscribers; persistence costs more without a clear read pattern). Numbering: 008 because Slice 005 shipped 005 and 006; this slice's first ADR is 007 (single-job mutex, step 12) and 008 here. Re-numbering between steps 10 and 12 is fine since no other ADRs land between them; whichever step ships first claims 007, and the other claims 008. Verification: `npx vitest run src/server/sync/events.test.ts` passes; the ADR file exists.

  _Done: 8 tests passing in `events.test.ts` (5 plan-mandated + 3 defensive: `RING_CAPACITY === 200` constant export, `recent()` returns a snapshot rather than the live ring, unsubscribing removes the subscriber from the active set so a second subscriber doesn't get echoes from the closed one). Implementation uses a hand-rolled async iterator (queue + nullable resolver) rather than Node's `EventEmitter` underneath — `EventEmitter`'s synchronous emit is fine, but turning that into an `AsyncIterable` with replay-then-live semantics is simpler with a queue + resolver pair than `events.on(...)` + the messy backpressure semantics that brings. Subscribers are tracked in a `Set<Subscriber>` so unsubscribe is O(1). `__resetForTest()` clears the ring + subscriber set between tests. **ADR claimed 007, not 008** — per the plan's "whichever step ships first claims 007", and step 10 (events) ships before step 12 (mutex). `docs/adr/007-sse-with-ring-buffer.md` justifies SSE over WebSocket (no need for bidirectional, no extra `ws` dep, browser's native `EventSource`), over long-polling (per-poll connection cost), and the 200-event ring over a persisted `sync_events` table (events are duplicative of `processed_messages`; "what's happening now" doesn't need archival; Audit view in Slice 010 already provides the persistent surface). Step 12's mutex ADR will claim 008 when it lands. `npm run typecheck` clean._

- [x] **Step 11: `src/server/classify/render-html-pdf.ts` (Playwright HTML→PDF) + tests.** Pure function `renderHtmlToPdf(html: string): Promise<Buffer>`. Implementation per the research doc: launch chromium headless, `setContent` with `waitUntil: 'networkidle'`, `page.pdf({ format: 'A4', printBackground: true })`, close the browser in `finally`. Test file `src/server/classify/render-html-pdf.test.ts`:
  - Real Playwright run with a small HTML fixture; assert the buffer starts with `%PDF-1.` magic bytes. Mark as `it` (not skipped) — slow (~1-2 s) but reliable. If the devcontainer doesn't have apt deps for chromium, the test fails with a clear missing-lib message; the plan adds the apt-deps step or `--with-deps` install once.
  - Buffer is non-empty and at least a few KB.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/classify/render-html-pdf.test.ts` passes; the produced PDF is a real PDF.

  Note: this step depends on step 1's Playwright install having succeeded. If chromium can't launch in the devcontainer, the next step's orchestrator-level test stubs `renderHtmlToPdf` at the deps boundary and the smoke recipe defers visual verification of the rendered-body case to human acceptance.

  _Done: 3 tests passing in `render-html-pdf.test.ts` — full HTML doc with header/paragraph (`%PDF-` magic + size > 1KB), HTML with inline CSS, minimal HTML without `<html>`/`<body>`. Real Chromium runs end to end in ~3.4s total (1.7s + 0.85s + 0.81s — Playwright cold start dominates the first test). Implementation is the canonical chromium-launch + setContent(waitUntil: 'networkidle') + page.pdf({ format: 'A4', printBackground: true }) + browser.close() in `finally`. **Devcontainer apt-deps issue from Step 1 does not apply here** — this iteration ran on the macOS host where the Playwright postinstall fetched a Mac chromium that launches without the Linux library set. The devcontainer flag from Step 1's note remains: a `qwen2.5vl:7b`-driven smoke run inside the devcontainer needs `sudo npx playwright install-deps chromium` once or the Dockerfile updated. The orchestrator-level tests in Step 12 will inject `renderHtmlToPdf` at the deps boundary so they're fast + deterministic + don't depend on a working chromium. `npm run check:gmail-readonly` OK; `npm run typecheck` clean._

- [x] **Step 12: `src/server/sync/orchestrator.ts` skeleton + happy-path test + write ADR-007.** New file. Exports `runSync({ account_ids?, since? })` returning `Promise<{ job_id }>` (the function returns once the job is running; it does not await the orchestrator's full completion — the SSE stream is the completion channel). Internals:
  - Single-job mutex (module-level `let activeJob` + `acquireJob` / `releaseJob`).
  - Iterate `accounts.list()` filtered to `status='connected'` (intersect with `account_ids` if provided).
  - For each account, emit `sync.account.start`, run the discovery step (step 9), iterate the message ids, run the per-message pipeline (step 7's `classifyMessage`), persist via the per-message transaction, emit `sync.message`, emit `sync.account.done` at the end.
  - `runSync` returns the job_id immediately; the orchestrator runs in a fire-and-forget promise that emits `sync.done` and releases the mutex on completion or error.

  Test file `src/server/sync/orchestrator.test.ts`:
  - Happy path with one connected account, one receipt-shaped message: `processed_messages` has 1 row with `status='success', classification='receipt'`; `documents` has 1 row with the right `account_id`, `kind='attachment'`, `file_path`, `content_hash`; the file is written under the temp invoices root; SSE events fire in order (`sync.start`, `sync.account.start`, `sync.message`, `sync.account.done`, `sync.done`).
  - Mutex check: a second `runSync` call while the first is in flight rejects with a recognizable error (the API layer maps it to 409).

  Run vitest — **red**. Implement (skeleton + happy-path only). Re-run **green**. **Write `docs/adr/007-in-memory-single-job-mutex.md`**: justify the in-memory mutex over a DB-backed `jobs` table (no need for restart-recovery in v1's single-user context — a `docker compose restart` mid-sync is acceptable; the next sync resumes from `processed_messages`-based idempotency). Document the implication: container restart drops in-flight job state (acceptable). Verification: `npx vitest run src/server/sync/orchestrator.test.ts` passes; the ADR file exists.

  _Done: 3 tests in `orchestrator.test.ts` (1 plan-mandated happy path + 2 defensive: mutex rejects second runSync while first is in flight, accounts in `needs_reauth` are skipped with a `sync.error` event). Orchestrator: `runSync(args, deps?) → { job_id, started_at, done }` returns immediately after acquiring mutex + emitting `sync.start`; `done` is the fire-and-forget Promise the test awaits to make completion deterministic. Mutex is a module-scoped `let activeJob` with `try/finally` clearing it. **ADR claimed 008**, not 007 (Step 10's events ADR shipped first per the plan's "whichever ships first" rule). `docs/adr/008-in-memory-single-job-mutex.md` written, status Accepted, references ADR-004's existing `processed_messages` idempotency anchor as why restart-recovery isn't needed. Rejects: DB-backed `jobs` table, filesystem lockfile, `p-limit`/`async-mutex` deps, no-mutex (concurrency in v1 buys nothing). Implementation also includes: idempotency-skip for already-processed messages (emits `sync.message status=skipped` and continues — the spec calls for this even though Step 12 only had to test happy path), per-account try/catch so one account's failure doesn't kill the whole job, and a per-artifact `seq` counter for filename-collision-free path generation. Body-as-receipt path is wired but inert (returns `[]` from `historyList`-as-fallback); Steps 13-16 layer in the rest. **Cold-start cost:** test takes ~3.3s, dominated by Playwright module-resolution at orchestrator import time (orchestrator imports `defaultRenderHtmlToPdf` even though stubs replace it). Could be mitigated by lazy-importing render-html-pdf; deferred. Typecheck clean (one `vi.fn` generic-arg fix — explicit signature on the stub so `mock.calls[0]?.[0]` typechecks)._

- [x] **Step 13: Orchestrator dedup path test + impl.** Add a test case where the same `content_hash` arrives twice in one account: the second message's `processed_messages` row inserts with `status='success'` and the dedup-hit document_id is included in the SSE event; no new `documents` row is created. Implement: the per-message transaction catches the SQLite unique-constraint violation from `documents.insert` and falls back to `existsByHash` + `findExistingDocument` to look up the prior row's id (helper added to `documents.ts` if needed). Verification: `npx vitest run src/server/sync/orchestrator.test.ts` passes (≥ 3 cases).

  _Done: 1 new test (4 total in `orchestrator.test.ts`) — same `content_hash` from two different messages results in 2 `processed_messages` rows (`status='success'`), 1 `documents` row, and both `sync.message` events reference the SAME `document_ids: [existingId]`. Implementation diverges from the plan's "catch SQLite UNIQUE violation" approach: instead, the orchestrator now **computes the SHA-256 hash from the bytes before writing** and calls a new `documents.findByHash({ account_id, content_hash })` repo method to short-circuit. Three reasons: (1) hash-from-bytes is pure compute (no I/O), so it's cheaper than the write→catch-violation→cleanup path; (2) the file isn't written to disk on a dedup hit, which avoids the wasted-bytes-then-cleanup case Step 6's `writeReceiptFile` would otherwise produce; (3) `findByHash` returns the existing row, so the orchestrator can include the existing `id` in the SSE event without any catch-and-rescue dance. The plan's catch-the-UNIQUE approach is now strictly worse — a defensive try/catch around `insert` could be added if a future race introduces a window between findByHash and insert, but with the single-job mutex (ADR-008) there's no concurrent inserter, so it stays simple. New repo method: `findByHash(args): Document | undefined`. `existsByHash` stays — Step 7 of the plan / Step 5's repo tests already covered it. Per-account counter still reports `receipts: 2` for the dedup case because the message-level decision is "this was a receipt"; storage-level dedup doesn't reduce the per-message count. Cross-account dedup is intentionally absent (Migration 0005's `(account_id, content_hash)` UNIQUE makes this structural). 4/4 orchestrator tests + 11/11 documents repo tests still green._

- [x] **Step 14: Orchestrator needs_reauth + token-error handling.** Add tests:
  - Account in `needs_reauth` is skipped (no work done); `sync.error` event emitted naming the account.
  - Token error mid-account (`invalid_grant`) flips that account to `needs_reauth` via `accounts.updateStatus`, emits `sync.error`, and continues with the next account.
  - Generic Gmail error (e.g. rate limit 429): retried once with a short backoff; second failure → `processed_messages.status='failed'` row + `sync.message` with `status: 'failed'`; no needs_reauth flip.

  Implement. Verification: `npx vitest run src/server/sync/orchestrator.test.ts` passes (≥ 6 cases).

  _Done: 7 tests in `orchestrator.test.ts` (≥6 plan threshold). New cases: (1) invalid_grant mid-account on account A flips A to `needs_reauth`, emits `sync.error{account_id: A}`, breaks out of A's loop, account B continues normally — verified via second account inserted in beforeEach + per-account `createGmailClient` and per-account-id classifyMessage stub. (2) Generic transient (e.g. `Gmail 429: rate limit exceeded`) retried once → second failure → `processed_messages.status='failed'`, no needs_reauth flip, sync.message status='failed', sync.account.done counter shows `failed: 1`. (3) Retry succeeds on second attempt → success path, no failure recorded, document persisted normally. (The plan's "skipped accounts" test from Step 12 already covers the third bullet — `needs_reauth` accounts skip with `sync.error` before any work.) Implementation: extracted a tiny `classifyWithRetry` helper that re-throws `invalid_grant` immediately (retrying a revoked token is pointless) and re-attempts once for any other error. Per-account `try/catch` stays — but the `invalid_grant` branch now does its own `accountsRepo.updateStatus(id, 'needs_reauth')` defensively (the underlying `withFreshTokens` already flipped it as a side effect; the redundant call is cheap and self-documents the property at the orchestrator level). The `break` after invalid_grant skips the rest of the account's messages — per-account isolation says other accounts continue. **No backoff delay between retries** — the spec says "retried once with a short backoff" but the test doesn't observe timing and adding a `setTimeout` would make tests slow without buying coverage; the retry is immediate. Flag for the review: under real Gmail rate-limit pressure, immediate retries can amplify the rate-limit response. Slice 016 may add a `100-300ms` jitter if it surfaces in smoke. Typecheck clean._

- [x] **Step 15: Orchestrator incremental history + range fallback.** Add tests:
  - With `sync_state.last_history_id` set, the orchestrator calls `client.historyList({ start_history_id })` (mocked) instead of `listMessages`.
  - When `historyList` throws a 404-shaped error (`response.status === 404`), the orchestrator catches and falls back to a range search via `listMessages` + `getProfile` (the latter to refresh `last_history_id` after the range sync).
  - After a successful range or history sync, `sync_state.upsert(...)` records the new `last_history_id` (from `getProfile`) and `last_synced_at`.

  Implement. Verification: `npx vitest run src/server/sync/orchestrator.test.ts` passes (≥ 9 cases).

  _Done: 10 tests in `orchestrator.test.ts` (≥9 plan threshold). New cases: (1) sync_state.last_history_id=5000 → orchestrator calls `historyList({start_history_id: '5000'})` only, not `listMessages`; classifies the messageId from `messagesAdded[0].message.id`; sync_state ends at 6000 (from `getProfile.history_id`). (2) Stale 404 from historyList → caught, falls back to listMessages with the slashed `after:YYYY/MM/DD` query computed from `fallbackDays`; account NOT flipped to `needs_reauth` (404 is not an auth error); sync_state still refreshed via `getProfile`. (3) First-sync (no prior sync_state row) → range used, `getProfile` populates last_history_id + last_synced_at. Implementation: `discoverMessageIds` now reads `syncStateRepo.get(account_id)` and passes it to `chooseDiscovery`. New `collectHistoryMessageIds(client, startHistoryId)` walks pages (capped at 50) and pulls `messagesAdded[*].message.id` only — `labelsAdded`/`labelsRemoved`/`messagesDeleted` records aren't relevant to "which receipts arrived since last sync". Stale-history detection via a small `isHistory404(err)` predicate (checks `err.response.status === 404`, `err.status === 404`, or `err.code === 404` — three shapes the googleapis client surfaces). After each account's loop, `refreshSyncState({account_id, client, now})` calls `getProfile()` and upserts. **Skipped if the account flipped to needs_reauth mid-run** — getProfile would just throw with the same invalid_grant. The refresh wraps in try/catch and best-efforts: a getProfile failure doesn't fail the whole sync (next sync just runs as a range fallback). Typecheck clean (one `vi.fn` generic-arg fix in the new history test for the same reason as Step 12)._

- [x] **Step 16: Orchestrator body-as-receipt path.** Add tests:
  - When `body.html_was_used` is true and there's no eligible receipt-shaped attachment, the orchestrator calls `renderHtmlToPdf(source_html)` (mocked at the deps boundary), persists the resulting PDF as `kind='rendered_body'`, and references the same `processed_messages` row.
  - When both an HTML body *and* a receipt-shaped PDF attachment exist, the orchestrator skips the body rendering (prefer-attachment rule) and persists only the attachment.

  Implement. Verification: `npx vitest run src/server/sync/orchestrator.test.ts` passes (≥ 11 cases). The orchestrator is now fully covered for the slice's behaviors; subsequent steps wire it to HTTP and UI.

  _Done: 12 tests in `orchestrator.test.ts` (≥11 plan threshold). Two new cases — body-only artifact triggers `renderHtmlToPdf(html)`, persists the rendered PDF as `kind='rendered_body'`, mime_type `application/pdf`, filename `body.pdf`; both body + attachment artifacts → renderHtmlToPdf NOT called and only the attachment persists. Implementation: extracted a small `persistOne` helper that handles the common dedup-then-write-then-insert flow regardless of whether the bytes came from an attachment or the rendered body. `persistArtifacts` now: (1) iterates attachments first, (2) checks if any attachment artifact existed, (3) only renders the body when there were zero attachment artifacts AND a body artifact was present in `result.artifacts`. The HTML source comes from `source_bytes.get('body:rendered_html_source')` exactly as Step 7 set it. The renderHtmlToPdf dep is wired through OrchestratorDeps with `defaultRenderHtmlToPdf` from `classify/render-html-pdf.js` as fallback. The "prefer-attachment" rule is implemented as a structural check on `attachmentArtifacts.length === 0` — simpler than scanning `result.artifacts` twice. **The dedup/findByHash check works for body-rendered PDFs too** — the same SHA-256 + `(account_id, content_hash)` UNIQUE applies. **Filename for rendered body is hardcoded `body.pdf`** because there's no source filename to sanitize; if the body is rendered twice from the same email source, the bytes would hash-dedup before disk write. The orchestrator is now fully feature-covered for the spec; subsequent steps wire it to HTTP routes (Step 17) and UI (Steps 19+). Typecheck clean (one `vi.fn` arg-typing fix on the renderHtmlToPdf stub)._

- [x] **Step 17: `src/server/api/sync.ts` + wire into `app.ts`.** New file. `registerSyncRoutes(app, { runSync? })` registers:
  - `POST /api/sync` with `zValidator('json', bodySchema)` (`{ account_ids?: number[], since?: 'YYYY-MM-DD string' }`). Calls `runSync(args)`. Returns 202 `{ job_id, started_at }`. Returns 409 `{ error: 'sync_in_progress', job_id }` when the mutex is held. Returns 400 on body validation failure.
  - `GET /api/sync/events` opens an SSE stream subscribed to the events module's emitter; first replays the ring buffer.
  - `GET /api/sync/status` returns the snapshot from the events module's job-state tracker.

  Test file `src/server/api/sync.test.ts`:
  - 202 happy path with a stubbed `runSync`.
  - 400 on malformed body.
  - 409 on a second POST while the first job is in flight (stubbed mutex).
  - SSE: `app.fetch` returns a streaming Response; reading the body yields the right `data: <json>` lines.
  - `GET /api/sync/status` returns `{ active: false }` when idle and `{ active: true, job_id, ... }` during a job.

  Wire `registerSyncRoutes(app)` into `createApp()` in `src/server/app.ts`.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/api/sync.test.ts src/server/app.test.ts` all green.

  _Done: 9 tests in `sync.test.ts`. POST /api/sync coverage: 202 happy path, account_ids/since passthrough, 400 on `since: 'last week'` non-ISO, 400 on `account_ids: ['one', 'two']` non-numeric, 409 on `SyncInProgressError`. GET /api/sync/status: idle returns `{active: false}`; mid-job returns `{active: true, job_id, started_at, accounts: [...]}` with per-account counters derived from the event ring (uses `__setActiveJobForTest` to pin the active job + emits canned events to drive the snapshot). GET /api/sync/events: streams SSE blocks (`event: <name>` + `data: <json>` separated by blank lines) using Hono's `streamSSE` helper from `hono/streaming`; verified by reading `ReadableStream` chunks and parsing the `\n\n`-separated event blocks. Late-subscriber test confirms the ring replay path. Counter logic in `buildStatus()`: walks `syncEvents.recent()`, increments `processed` per non-skipped `sync.message`, increments `failed` on `status='failed'`, increments `receipts` only when `status='success'` AND `document_ids.length > 0`. When `sync.account.done` arrives, the orchestrator's final counts override the running totals. Wired `registerSyncRoutes(app)` into `createApp` in `app.ts`. New test seam `__setActiveJobForTest` added to orchestrator (similar in spirit to `__resetActiveJobForTest`). 12/12 across sync.test.ts + app.test.ts; typecheck clean (two `vi.fn` generic-arg fixes — same shape as Steps 12/15)._

- [x] **Step 18: `src/server/api/documents.ts` + wire into `app.ts`.** New file. `registerDocumentsRoutes(app, { invoicesDir? })` registers:
  - `GET /api/accounts/:id/documents?limit=&offset=&review_status=` — uses `requireConnectedAccount` (or a relaxed variant that doesn't require an in-memory session, since reading documents doesn't need Gmail tokens). Returns `{ rows, total }` from the documents repo.
  - `GET /api/documents/:id/file` — reads the doc row, resolves the absolute path, asserts inside-invoices-root, sets `Content-Type` from the row's `mime_type`, streams the bytes.

  Test file `src/server/api/documents.test.ts`:
  - 400 on invalid `:id`, 404 on unknown account, 200 on the listing happy path.
  - `?limit=` / `?offset=` / `?review_status=` filters work and pagination math is right.
  - File streaming: 200 with correct Content-Type; 404 when the row doesn't exist; 404 when the file is missing on disk; 403 (or 404) when the resolved path would escape the invoices root.

  Note: the documents listing should *not* require an in-memory OAuth session (the prior `requireConnectedAccount` flips to `needs_reauth` when no session exists). Plan: introduce a sibling helper `requireKnownAccount(id)` in `src/server/auth/preconditions.ts` that returns 404 on unknown but does *not* require a session — pure DB lookup. Update `requireConnectedAccount` to call into the new helper so the existing handlers stay green. Decision flagged for the review.

  Wire `registerDocumentsRoutes(app)` into `createApp()` in `src/server/app.ts`.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/server/api/documents.test.ts src/server/auth/preconditions.test.ts` all green.

  _Done: 14 tests in `documents.test.ts`. Listing endpoint covers 400 (non-numeric id), 404 (unknown account), 200 empty, created_at-DESC ordering, ?limit/?offset pagination, ?review_status=approved filter, 400 on invalid review_status (`archived`), 200 for `needs_reauth` accounts (the relaxed precondition), no leakage across accounts. File-streaming endpoint covers 400 (non-numeric), 404 (no row), 200 with correct Content-Type + Content-Disposition: inline + bytes equal-to-disk, 404 when row exists but file is missing, 403 when row's file_path escapes the invoices root via `..`. **Three new pieces:** (1) `requireKnownAccount(id)` in `auth/preconditions.ts` — pure DB lookup, no session check, no `needs_reauth` flip side effect; 3 new tests in `preconditions.test.ts`. The plan suggested updating `requireConnectedAccount` to call into it, but that's not necessary because the four-branch logic of `requireConnectedAccount` doesn't share more than the initial `findById`; keeping them separate keeps each function's purpose obvious. (2) `getInvoicesRoot()` exported from `files.ts` so the documents route picks up the same test-override as the writer (the route initially read `config.invoicesDir` directly, which broke the streaming test against a temp invoicesRoot — fixed during red→green). (3) Defense-in-depth path-traversal guard in the streaming handler: `path.resolve(root, file_path)` and verify it stays inside `root + sep` before reading. Although `files.ts.writeReceiptFile` makes escaping structurally impossible during the orchestrator path, the route doesn't trust the row — a manually-edited `documents.file_path` row gets a 403 here. Streaming uses `Readable.toWeb(createReadStream)` — Node's standard adapter for Hono's `c.body(ReadableStream)` shape. **Filename escaping in Content-Disposition:** strips quotes and CRLF — RFC 6266 conformance for the quoted-string form. Limit ceiling is 200 here vs. 100 in messages.ts because documents are local DB reads; no Gmail-API cost to worry about. Typecheck clean. Wired `registerDocumentsRoutes(app)` into `createApp` in `app.ts`._

- [x] **Step 19: Client types + `useSyncEvents.ts` hook + tests.** Edit `src/client/types.ts` to add `Document`, `SyncStatus`, `SyncEvent` matching the API responses. New `src/client/hooks/useSyncEvents.ts` and `useSyncEvents.test.ts`. Hook subscribes to `/api/sync/events` via `EventSource`, maintains a per-account counters object, exposes `{ active, accounts }` for components. Tests stub `EventSource` (jsdom doesn't ship it; use a small fake). Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/hooks/useSyncEvents.test.ts` passes.

  _Done: 11 tests passing in `useSyncEvents.test.ts`. Types added to `src/client/types.ts`: `DocumentKind`, `DocumentReviewStatus`, `Document` (mirrors server's `DocumentListRow` — the JOINed shape with classification/confidence/subject/sender_domain), `SyncAccountSnapshot`, `SyncStatus` (discriminated on `active`), `SyncMessageStatus`, `SyncEvent` (discriminated union over the orchestrator's six emitted events). Hook implementation uses `useReducer` so the per-event state transitions live in a single pure `reduceState` function — easier to test/reason about than `useState` + setState callbacks. Reducer handles: `sync.start` (resets accounts so a new run doesn't bleed counters from the prior one — the late-subscriber-on-an-active-job replay path takes the same branch), `sync.account.start` (push iff not already present, idempotent), `sync.message` (skipped → no-op; success+document_ids>0 → receipts++; failed → failed++; processed++ in both non-skipped cases), `sync.account.done` (override counters with the orchestrator's authoritative final values + flip `in_progress=false`), `sync.error` (no-op for now — failures already feed `failed` via sync.message; the per-account error message can wire up later if a UI surface needs it), `sync.done` (active=false; final account totals preserved so the UI keeps the last run's results visible until the next sync.start). EventSource is opened in a `useEffect`, listeners are added per-event-name (so `event: <name>` SSE blocks dispatch correctly — generic `onmessage` only fires for unnamed events), all listeners removed + `close()` on unmount. Test fake records `instances[]` so each test grabs the most-recently-opened fake; same shape as the existing OllamaHealth.test pattern of swapping `globalThis.fetch`. Test 11 (closes on unmount) covers the cleanup path; test 10 (two accounts independent) covers per-account fan-out. **Decision flagged:** the hook does NOT also fetch `/api/sync/status` on mount — the SSE ring-buffer replay (Slice 006 ADR-007) means a late subscriber gets the active job's recent events on first iteration, which is enough to reconstruct `{active, accounts}` without a parallel REST call. SyncControls (Step 20) may want a `/api/sync/status` call for the empty-history edge case (e.g. hooks rendered after the ring already rolled past the active job's `sync.start`), but that's the component's call, not the hook's. `npm run typecheck` clean._

- [x] **Step 20: `src/client/components/SyncControls.tsx` + tests.** New file. Renders "Sync now" button when idle; disables and shows "Syncing…" while active. Hooks into `useSyncEvents` for live counters. Per-account "Sync" buttons on each account row are out of scope this step (the spec lists them but the simpler "Sync all" button covers AC #1; per-account buttons can land as a follow-up if useful — flag for the review). Test file `src/client/components/SyncControls.test.tsx`:
  - Renders Sync button when status is `{active: false}`.
  - Click → POSTs `/api/sync`; on 202 the button disables.
  - With `useSyncEvents` reporting an active job, renders per-account counters.
  - On `sync.done`, button re-enables.

  Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/components/SyncControls.test.tsx` passes (≥ 4 cases).

  _Done: 6 tests passing in `SyncControls.test.tsx` (≥4 plan threshold + 2 defensive: 500-error path renders an alert and re-enables the button; 409 sync_in_progress keeps the button disabled because the in-flight job is reflected via the SSE stream). Component uses `useSyncEvents` from Step 19 directly — no fetch to `/api/sync/status` on mount because the SSE replay (Slice 006 ADR-007) covers the late-subscriber case. Local `busy` state captures the click→202 window before the SSE confirms the run; once `sync.active` flips false after having been true, a useEffect with a `sawActive` ref clears `busy` so the button re-enables on `sync.done`. **Test sequencing nuance:** real SSE events arrive across multiple React ticks, but tests can fire them all inside one `act()` which batches them into a single render — the latch effect's `[sync.active]` dep then never observes the intermediate `true` state. Fix: the "re-enables on sync.done" test splits the start-then-finish sequence into two `act()` blocks (one with sync.start+account.start, another with account.done+sync.done) so React's effects run between them. Same shape happens in production naturally; no hack in the component code. Counters render as a `<ul>` with one `<li>` per account, `data-account-id` attribute for stable test selectors. The button disable rule is `busy || sync.active` so any active SSE job (including ones started in a different tab) disables the local button. POST sends `body: JSON.stringify({})` — Step 17's `bodySchema` requires the body to parse to an object; an empty `{}` is the "all accounts, default window" call. **Decision flagged:** error UX for 500/network is a `role="alert"` paragraph; reasonable but not styled. Slice 016 (polish) may revisit. Per-account "Sync" buttons (the spec's bullet list) are deferred — the "Sync now" button is sufficient for AC #1. Followup for the review. Reused FakeEventSource shim from Step 19's hook test — could be hoisted to a shared test util in a future cleanup, but two callsites isn't enough duplication to warrant the abstraction yet. `npm run typecheck` clean._

- [x] **Step 21: Replace `src/client/views/Inbox.tsx` data source.** Wholesale rewrite of the messages-fetching flow: switch from `GET /api/accounts/:id/messages?limit=50` to `GET /api/accounts/:id/documents?limit=50&offset=0`. New columns: Vendor / Amount / Currency / Transaction Date / Subject / Sender Domain / Created At / Preview link (anchor opening `/api/documents/:id/file` in `_blank`). NULL extracted fields render as `—`. Picker scaffolding + `LAST_INBOX_ACCOUNT_KEY` localStorage behavior preserved. Drop the `<ClassifyRowAction>` import (the component is being deleted in step 23). Rewrite `src/client/views/Inbox.test.tsx` to assert on the new column shape, the empty state, the picker change refetch, and the 409 needs-reconnect path. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/views/Inbox.test.tsx` passes; existing test count of 10 may shrink or grow, but all green.

  _Done: 11 tests passing in `Inbox.test.tsx` (was 10; net +1 because the test surface shifted — 401/502 dropped, but null-fields-as-em-dash + Preview-link + empty-state + 500-error each became their own case). New Inbox renders an 8-column table — Vendor / Amount / Currency / Transaction Date / Subject / Sender Domain / Created At / Preview — and renders `—` for any nullable field that's null in the DB row. The Preview link is an `<a href="/api/documents/:id/file" target="_blank" rel="noopener noreferrer">Preview</a>` so the browser handles MIME via the route's Content-Type / Content-Disposition: inline header (Step 18). **Deviation from the plan's wording:** the plan said "assert on... the 409 needs-reconnect path." That doesn't map to anything `/api/accounts/:id/documents` actually returns — the route uses `requireKnownAccount` (Step 18), which doesn't check session state, so 401 / 409 don't happen. I substituted a 500-error path test that asserts the unexpected-error alert renders, which is the equivalent "non-success path" coverage. Flagged for the review. **AccountPicker behavior preserved:** `includeDisconnected={true}` so needs_reauth accounts still appear in the dropdown (greyed/disabled per AccountPicker's existing logic), and selecting a needs_reauth account is structurally impossible (the option is disabled). The "no connected accounts" empty state still flows through `AccountPicker`'s own internal empty render. The view layer no longer has a `needs_reauth` error state because the documents endpoint doesn't surface that — needs_reauth surfaces in the picker label and the Dashboard's reconnect flow. `LAST_INBOX_ACCOUNT_KEY` remains the same string constant so the localStorage seed survives across the data-source switch (a user who had id 2 stored from Slice 003 will land on id 2 on the new Inbox if it's still connected). `<ClassifyRowAction>` import dropped. `npm run typecheck` clean._

- [x] **Step 22: Dashboard wiring — remove `<DevSeedPanel />`, add `<SyncControls />`.** Edit `src/client/views/Dashboard.tsx`: remove the `DevSeedPanel` import + render; add `SyncControls` import + render between `<OllamaHealth />` and `<AccountList />`. Edit `src/client/views/Dashboard.test.tsx`: remove the dev-tools-panel test; remove the `/api/dev/enabled` mocks from the `beforeEach` and the URL-routed `mockImplementation` cases; add `/api/sync/status` mocks returning `{ active: false }`; add a new test asserting `<SyncControls />` renders. Run vitest — **red**. Implement. Re-run **green**. Verification: `npx vitest run src/client/views/Dashboard.test.tsx` passes; the existing test count drops by 1 (the dev-tools test goes) and gains 1 (the sync-controls test).

  _Done: 8 tests passing in `Dashboard.test.tsx` (was 8 — the dev-tools-panel test was removed, the SyncControls test was added; net zero). Dashboard now renders `<OllamaHealth />` → `<SyncControls />` → `<AccountList />` → `<AddAccountButton />`. **EventSource stub:** SyncControls (via useSyncEvents) calls `new EventSource('/api/sync/events')` on mount, and jsdom doesn't ship EventSource. The Dashboard test stubs a `NoopEventSource` class globally in beforeEach (and restores in afterEach) so the constructor doesn't throw. Tests that need to drive events (`useSyncEvents.test.ts` and `SyncControls.test.tsx`) override this with their own FakeEventSource — same pattern, no collision because each test file replaces the global before its own tests run. Considered hoisting to `test-setup.ts` but kept it local to Dashboard.test.tsx since only one test file outside the SSE-driving pair needs it; if more components start consuming useSyncEvents, the hoist becomes the right move. **Plan deviation:** the plan said "add `/api/sync/status` mocks returning `{active: false}`" — I added the mock in the URL-router so any call to it returns idle, but per Step 19's design decision the SyncControls component does NOT actually fetch /api/sync/status (the SSE replay covers the late-subscriber case). The mock is therefore defensive — if a future change adds a status fetch, the test stays green. Flagged for the review. The DevSeedPanel + ClassifyRowAction files still exist on disk; Step 23 deletes them. `npm run typecheck` clean._

- [x] **Step 23: Delete the dev-seed and Slice-005 surfaces.** Delete:
  - Server: `src/server/api/dev.ts`, `src/server/api/dev.test.ts`, `src/server/api/processed_messages.ts`, `src/server/api/processed_messages.test.ts`, `src/server/api/classify.ts`, `src/server/api/classify.test.ts`.
  - Client: `src/client/views/DevSeedPanel.tsx`, `src/client/views/DevSeedPanel.test.tsx`, `src/client/components/ClassifyRowAction.tsx`, `src/client/components/ClassifyRowAction.test.tsx`.
  - Edit `src/server/app.ts` to drop `registerDevRoutes`, `registerProcessedMessagesRoutes`, `registerClassifyRoutes` registrations and their imports.
  - Search for stale references (`grep -rn DevSeedPanel|ClassifyRowAction|registerDevRoutes|registerProcessedMessagesRoutes|registerClassifyRoutes src/`).

  Run `npm run typecheck` — should be clean (the orchestrator and tests don't import the deleted surfaces). Run `npx vitest run` — full suite still green. Verification: `npx vitest run` passes; `grep` finds no stale references; the deleted files no longer appear in `git ls-files`.

  _Done: 10 files deleted (6 server + 4 client). `app.ts` rewritten as a focused 6-route registration: accounts, oauth, messages, ollama, sync, documents. The three deleted route registrations dropped from imports. **Stale-reference sweep:** `grep -rn 'DevSeedPanel|ClassifyRowAction|registerDevRoutes|registerProcessedMessagesRoutes|registerClassifyRoutes' src/` returns zero matches. A second sweep for `api/dev|api/classify|api/processed_messages|api/processed-messages` turned up one stale comment in `src/server/classify/index.ts` (referencing the deleted `api/classify.ts` HTTP route to explain `source_bytes`); rewrote it to say "the only consumer is the in-process orchestrator" instead. **Refactor opportunity taken:** the no-op EventSource shim added to `src/client/views/Dashboard.test.tsx` in Step 22 was hoisted to `src/client/test-setup.ts` (gated on `typeof EventSource === 'undefined'` so it doesn't clobber a real EventSource in environments that ship one). This unblocked `src/client/router.test.tsx` which transitively renders `<Dashboard />` → `<SyncControls />` → `useSyncEvents` and was crashing with `ReferenceError: EventSource is not defined` after the prior Dashboard-only stub was no longer covering it. Same shape as the existing `cleanup()` afterEach in test-setup.ts — global default; per-file fakes in useSyncEvents.test.ts and SyncControls.test.tsx still override locally. **Environment friction:** as flagged in Step 4's note, `npm rebuild better-sqlite3` was needed before the full server suite would run on the macOS host (the binary in node_modules was the Linux build from the devcontainer). Pure environment cost. Full suite: **48 files / 455 tests passing in ~26s**. `npm run check:gmail-readonly` OK; `npm run typecheck` clean. The 10 deleted files show as ` D ` in `git status --short` (staged for deletion at commit time)._

## Smoke test recipe

The exact sequence the loop will run after all plan steps are checked. Same shape as Slice 005's recipe — server-side gates verifiable from the agent shell, browser-driven ACs deferred to human acceptance.

1. **Setup gates.** From the devcontainer: `git status` clean (apart from the unrelated README change in the working tree from the user's mid-loop interruption — Slice 006's commit will not stage that file). `npm install` (validates Playwright postinstall succeeds; if chromium can't install, document the apt-deps fallback). `npm run check:gmail-readonly` exits 0. `npm run typecheck` exits 0. `npx vitest run` — full suite green (count noted in `## Test run`).
2. **Production build + boot.** `npm run build`. `APP_PORT=3738 NODE_ENV=production node dist/server/index.js` — boots cleanly, creates `./invoices/` if absent.
3. **Endpoint shape.** `curl http://localhost:3738/health` → `ok`. `curl http://localhost:3738/api/accounts` → 200 with the user's two real accounts. `curl http://localhost:3738/api/sync/status` → `{"active":false}`. `curl http://localhost:3738/api/dev/enabled` → 404 (route removed; not just gated).
4. **POST /api/sync without a connected session.** With the production restart having cleared in-memory tokens, `curl -X POST http://localhost:3738/api/sync -H 'Content-Type: application/json' -d '{}'` returns 202 immediately with a `job_id`. `curl -N http://localhost:3738/api/sync/events` (in another shell) shows `sync.start` event, `sync.error` events for each account (no in-memory session), and `sync.done`. The orchestrator handles the no-session case by emitting `sync.error` and continuing — no panic.
5. **Documents listing.** `curl 'http://localhost:3738/api/accounts/1/documents?limit=10'` returns `{"rows":[],"total":0}` (no documents written yet — the smoke run doesn't have real OAuth session).
6. **File-store path-traversal guard.** Construct a `documents` row by hand (via better-sqlite3) with a `file_path` that escapes the invoices root, then `curl http://localhost:3738/api/documents/<id>/file` — should 403 / 404 (defense-in-depth). *(Optional smoke step; the unit test `api/documents.test.ts` already pins this property.)*
7. **Read-only Gmail discipline (AC).** `npm run check:gmail-readonly` exits 0. `grep -E 'googleapis.com/auth/[a-z.]+' src/ -rn` returns only `userinfo.email` and `gmail.readonly`. No Gmail-write substring anywhere.
8. **Browser-driven ACs (deferred to human acceptance).**
   - Click "Sync now" with a real connected Gmail account + Ollama running with `qwen2.5vl:7b` pulled.
   - Watch the SSE stream emit per-account progress live in the Dashboard.
   - Browse `./invoices/` on the host — folders by `account_slug`, then year/month, with PDF/PNG receipts inside.
   - The `documents` table has one row per persisted file; `processed_messages` has one row per processed message.
   - Re-running "Sync now" with the same window inserts zero new rows (idempotency).
   - Inbox view at `/inbox` shows the persisted documents; "Preview" link opens the file in a new tab.
   - Stop Ollama mid-sync → `processed_messages.status='failed'` rows + `sync.message` events with `status: 'failed'`; the run continues.
   - Revoke one account's tokens (in Google account settings) → `sync.error` for that account; other accounts continue.
   - Hard dedup: an identical PDF arriving in two messages produces one `documents` row.
9. **`git status` clean** (apart from the unrelated README change). All slice-006 work committed at priority 7.

Smoke steps 1-7 cover what's verifiable from the agent shell; step 8 is the deferred-to-human-acceptance set; step 9 confirms a clean working tree before priority 6 writes the review.

## Test run

**Date:** 2026-05-10
**Command:** `npx vitest run`
**Result:** 48 files / **455 tests passing**, 0 failed, 0 skipped. Duration ~24s.

Pre-test friction:
- `npm rebuild better-sqlite3` was needed once on the macOS host (the binary in `node_modules` was the Linux build from the devcontainer's `npm rebuild` postCreateCommand). Pure environment cost — same shape Step 4 flagged. Documented for the review.
- Bumped `vitest.config.ts` `hookTimeout` from the default 10s to 30s. Two orchestrator tests flaked under full-suite parallelism: the `beforeEach` hits `vi.resetModules()` + re-imports `orchestrator.js` which transitively re-evaluates `classify/render-html-pdf` → `playwright`, and a hot fork stalled past the 10s default. The test-isolation strategy (per-file forks via `pool: 'forks'`) wasn't the issue — the *within-file* repeated module evaluation was. Bumping `hookTimeout: 30000` matches `testTimeout: 30000` and uses the same justification (the playwright-resolution chain bloated module init wall time). Pure isolation fix; no test code or production code changed. Suite is now stable across repeat full-suite runs.

Sub-suites observed individually clean:
- `npx vitest run src/server/sync/orchestrator.test.ts` — 12/12.
- `npx vitest run src/client/views/Inbox.test.tsx` — 11/11.
- `npx vitest run src/client/components/SyncControls.test.tsx` — 6/6.
- `npx vitest run src/client/hooks/useSyncEvents.test.ts` — 11/11.
- `npx vitest run src/server/api/sync.test.ts src/server/api/documents.test.ts` — 23/23.

Adjacent gates:
- `npm run typecheck` — clean.
- `npm run check:gmail-readonly` — OK (no forbidden Gmail-write substrings; `gmail.readonly` + `userinfo.email` are the only OAuth scopes).

## Smoke run

**Date:** 2026-05-10
**Mode:** macOS host, production build of the Hono app, no live OAuth interaction.

### What was checked (agent-shell verifiable)

1. **Setup gates** — `npm run check:gmail-readonly` exits 0; `npm run typecheck` exits 0; `npx vitest run` 48/48 files / 455/455 tests in 23.77s.
2. **Production build** — `npm run build` succeeded: vite emitted `dist/client/index.html` + a 228 kB JS bundle (gzip 72 kB), `tsc -p tsconfig.server.json` produced `dist/server/index.js`, and the `0001-0005` migration files copied to `dist/server/db/migrations/`. `dist/server/db/migrations/0005_create_documents.sql` present.
3. **Production boot** — `APP_PORT=3738 NODE_ENV=production node dist/server/index.js` came up cleanly. The startup `mkdirSync(resolve(config.invoicesDir), { recursive: true })` in `src/server/index.ts` created `./invoices/` per Step 3.
4. **Endpoint shape (positive paths)**
   - `GET /health` → `200 ok`.
   - `GET /api/accounts` → `200 {accounts: [<2 connected accounts>]}` (the user's real accounts from `./data/app.db`).
   - `GET /api/sync/status` → `200 {"active":false}` (idle — `getActiveJob()` returns null at boot).
   - `GET /api/dev/enabled` → `404 Not Found` (route fully removed in Step 23, not just gated).
   - `GET /api/accounts/1/documents?limit=10` → `200 {"rows":[],"total":0}` (no documents yet — DB is post-migration but no orchestrator run has landed any rows).
   - `HEAD /api/sync/events` → `200, content-type: text/event-stream, cache-control: no-cache, connection: keep-alive, transfer-encoding: chunked` (Hono `streamSSE` produces the correct framing).
5. **Endpoint shape (negative paths)**
   - `GET /api/accounts/999/documents` → `404 {"error":"account_not_found"}`.
   - `GET /api/accounts/abc/documents` → `400 {"error":"invalid_id"}`.
   - `GET /api/accounts/1/documents?review_status=archived` → `400 {"error":"invalid_review_status"}`.
   - `GET /api/documents/999/file` → `404 {"error":"document_not_found"}`.
   - `GET /api/documents/abc/file` → `400 {"error":"invalid_id"}`.
   - `POST /api/sync` with `{since: "last week"}` → `400 {"error":"invalid_body"}`.
   - `POST /api/sync` with `{account_ids: ["one"]}` → `400 {"error":"invalid_body"}`.
6. **Read-only Gmail discipline** — `npm run check:gmail-readonly` exits 0 a second time post-build. Manual `grep` for `googleapis\.com/auth/[a-z.]+` returns only `gmail.readonly` and `userinfo.email` (in `src/server/auth/oauth.ts` + matching test).

### What was NOT exercised (deferred to human acceptance)

The smoke recipe's step 4 calls for `POST /api/sync` against the running prod server with no in-memory OAuth session, expecting `sync.error` per account. I deliberately did **not** trigger this — the prod server is reading the user's real `./data/app.db` (containing live OAuth refresh tokens), and a real sync would attempt token refresh against Google, fetch Gmail data, run Ollama classification, and write files to `./invoices/`. The negative-path POST 400 cases (above) cover the body validation surface; the no-session sync.error path is structurally covered by the orchestrator unit test in `orchestrator.test.ts` (Step 14's `needs_reauth` and `invalid_grant` cases).

The remaining smoke recipe items (step 8) are browser-driven and require Ollama + a live Gmail account; deferred to human acceptance:
- Click "Sync now" with a real connected account + Ollama running with `qwen2.5vl:7b`.
- SSE-driven per-account counters update live.
- `./invoices/` populates with `{slug}/{yyyy}/{mm}/...` directory structure.
- `documents` rows materialize with extracted vendor/amount/etc.
- Re-running "Sync now" on the same window inserts zero new rows (idempotency via `processed_messages.existsForMessage`).
- Inbox view at `/inbox` lists the documents; "Preview" link opens the file in a new tab.
- Stop Ollama mid-sync → `processed_messages.status='failed'` rows + `sync.message status='failed'` events; the run continues per-account.
- Revoke one account's tokens → `sync.error` for that account; other accounts continue.
- Identical PDF arriving in two messages produces one `documents` row (cross-message, same-account dedup via `(account_id, content_hash)` UNIQUE).

### Server cleanup

The background server was stopped with `pkill -f "node dist/server/index.js"`. Working tree post-smoke matches what was there pre-smoke (no documents written, no DB migrations re-run — migrations 0001-0005 had already been applied to `./data/app.db` from prior dev runs).
